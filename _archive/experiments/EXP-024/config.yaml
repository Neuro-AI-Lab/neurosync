# EXP-024 — F5 clinical hand-off report, 약식 functional validation
# PLAN-2026-W29-E step 8. ADR-037 Decision 1: narrative path DESCOPED this
# mission — deterministic-only, ZERO LLM calls, ZERO HTTP calls anywhere in
# this run's code path (`--f5-from-artifacts` replay CLI drives
# `src.f5.assemble_handoff_report` + `src.services.f5_report.save_f5_result`
# only; no F1-F3 session, no `HandoffGeneratorAgent`/`EvidenceVerifierAgent`
# call). No HYP — dev-validation entry, same handling as EXP-023.

purpose: >
  Generate the full F5 hand-off report set (markdown + PDF + FHIR R4 bundle)
  for VP-001 and VP-003 from EXISTING EXP-023 artifacts (no new F1-F4 session
  runs), then run the pre-registered machine-check battery (REV-046 Criteria
  0/1/2a/3-7, minus narrative-scoped arms which are moot-by-descope) and
  record raw evidence. No PASS/FAIL adjudication against the acceptance
  criteria is rendered here (critic's charter, PLAN-2026-W29-E step 10) —
  this entry reports facts only.

git:
  commit: 0771534de2f90ac6e70e57ead61321d4cc395508
  branch: feat/f5-handoff-report
  tree_status: clean (verified via `git status --porcelain`, empty, before launch)

zero_llm_zero_http_statement: >
  `--f5-from-artifacts` is a replay-only CLI branch (`continuous_test.py:1339-1374,
  _run_f5_replay_cli`) that short-circuits BEFORE any F1/F2/F3 session code
  runs (`_main`, lines 1601-1603 — checked first, before `answer_mode`/
  `forced_scale`/etc. are even read). It calls `_run_f5_report` ->
  `src.f5.assemble_handoff_report` (pure, 100% deterministic, ZERO-LLM per
  its own module docstring and `ADR-037` Decision 1 — raises ValueError if
  `narrative_enabled=True`; this CLI path always passes `narrative_enabled=
  False`) -> `src.services.f5_report.save_f5_result` (file I/O only: writes
  md/PDF/FHIR to disk, zero network calls). No `UPSTAGE_API_KEY`/
  `LG_K_EXAONE_API_KEY`/`SKT_A_X_API_KEY`-consuming code path is reachable
  from this CLI branch. Confirmed by direct code read, not merely asserted.

pre_run_checklist:
  target_hyp: "N/A — dev-validation, no HYP-ID (EXP-023 precedent, same handling)"
  critic_review: "REV-046 (F5 pre-implementation gate) verdict non-blocking-with-conditions; ADR-037 Decision 1 resolved the 4 blocking-scoped issues (Issues 1/2/3/5) by descoping the narrative path entirely for this mission — no open blocking REV against this EXP-024 run"
  qa_code_gate: "GATE:PASS on commit 0771534 (96 F5 tests green per HANDOFF); BUG-043 open but severity=major, explicitly 'test-coverage gap only — current code independently verified CORRECT; no live incorrect behavior observed' (error.md) — does not block per project gate rule (mechanical hook only blocks on severity=critical) and does not affect the A3 safety_referral-OR-critical_item_positive logic actually exercised by these two VPs' data (see EXP-023 Notes: BUG-043's blind spot is a fixture-coverage gap, not a live defect)"
  dataset: "DATASET-002/003/006 (same as EXP-023 — no new data dispatch; scenario packs are harness fixtures)"
  gate_hook: "command pattern `uv run python -m src.continuous_test --f5-from-artifacts ...` does not match EXP_LAUNCH (no run.sh/evaluate.sh/models/*.py) — same non-triggering precedent as EXP-014/016-023; no GATE_OVERRIDE needed"
  fresh_out_dirs: "verified via `find` before launch — zero pre-existing *handoff* files anywhere under experiments/EXP-023/ or docs/ai/simulation_results/ — no blend risk"

env:
  python: "3.12.13 (main, Mar 3 2026) [Clang 21.1.4] — via `uv run python -c 'import sys; print(sys.version)'`"
  reportlab: "5.0.0 — via `uv run python -c 'import reportlab; print(reportlab.Version)'`"
  pypdf: "6.14.2 — via `uv run python -c 'import pypdf; print(pypdf.__version__)'`"
  hardware: "local CPU-only orchestration host (same host as EXP-023); no GPU/inference needed (zero LLM calls)"

source_artifacts:
  vp001:
    dir: experiments/EXP-023/runs/vp001/artifacts/VP-001/
    file_count: 62
    ledger_entries: 11
    ledger_sha256: 1b600a35b64d85fda270e326ae2249dec42b4b15525016d6a844a6d067445113
    ledger_path: experiments/EXP-023/runs/vp001/artifacts/VP-001/VP-001_session_ledger.json
    latest_conversation_pointer: docs/ai/simulation_results/VP-001/VP-001_20260713_200133_conversation.json
    latest_f4_temporal: VP-001_20260713_200152_temporal.json
    f4_pngs_present: 4 (scales_ctrs_sentiment, ctrs_zoom, disease_similarity, domain_confidence)
  vp003:
    dir: experiments/EXP-023/runs/vp003/artifacts/VP-003/
    file_count: 51
    ledger_entries: 11
    ledger_sha256: 042b83bb2920059774aee7d83d8443f0ceadd79e4e46121e4795ed8a837a349a
    ledger_path: experiments/EXP-023/runs/vp003/artifacts/VP-003/VP-003_session_ledger.json
    latest_conversation_pointer: docs/ai/simulation_results/VP-003/VP-003_20260713_201643_conversation.json
    sole_scored_administration: S9 (docs/ai/simulation_results/VP-003/VP-003_20260713_201540_conversation.json companion survey.json 201550 — PHQ-9 27/27, item-9 positive)
    latest_f4_temporal: VP-003_20260713_201646_temporal.json
    f4_pngs_present: 3 (scales_ctrs_sentiment, ctrs_zoom, domain_confidence — NO disease_similarity, expected per EXP-023/design: chart 3 only plots diseases recurring in >=2 sessions, VP-003 has only 1 disease-bearing session)
  do_not_modify: "these EXP-023 artifacts are read-only inputs to this run; not touched"

commands:
  env_check:
    - "cd apps/ai-server && uv run python -c 'import sys; print(sys.version)'"
    - "cd apps/ai-server && uv run python -c 'import reportlab; print(reportlab.Version)'"
    - "cd apps/ai-server && uv run python -c 'import pypdf; print(pypdf.__version__)'"
  vp001: >
    cd apps/ai-server && uv run python -m src.continuous_test
    --f5-from-artifacts /home/neuroai/users/dhkim/aichampion/neurosync/experiments/EXP-023/runs/vp001/artifacts/VP-001
    --out /home/neuroai/users/dhkim/aichampion/neurosync/experiments/EXP-024/runs/vp001/out
  vp003: >
    cd apps/ai-server && uv run python -m src.continuous_test
    --f5-from-artifacts /home/neuroai/users/dhkim/aichampion/neurosync/experiments/EXP-023/runs/vp003/artifacts/VP-003
    --out /home/neuroai/users/dhkim/aichampion/neurosync/experiments/EXP-024/runs/vp003/out

output_layout: >
  --out writes to <out>/<persona_id>/ (continuous_test.py:1349-1355) — so
  outputs land at experiments/EXP-024/runs/vp001/out/VP-001/<VP-001>_<ts>_
  handoff.{md,pdf} + _handoff_fhir.json, and equivalently for vp003/out/VP-003/.

wording_discipline_binding: >
  REV-046 MAY/MUST-NOT wording table (discussion.md ~2354-2364) + REV-045's
  table for any F4-value citations, applied to every sentence in this EXP-024
  result.md entry. FHIR claims phrased "structurally valid per our own
  checks" only — never conformance claims. Questionnaire numbers always
  carry the non-validated-administration caveat. similarity_score never
  probability. Honest PASS/FAIL per machine check — a FAIL is reportable
  evidence, never smoothed.

# ============================================================================
# r2 — post-fix re-validation (commit 8717382)
# ============================================================================
# Reason: r1 (commit 0771534) closed 6/7 checks PASS; check 5c FAILED
# (A3's 종단위험신호 table + staleness-pointer prose lacked a section-local
# non-validated-administration caveat; document-level disclaimer self-scoped
# to "설문(A5/B1)" only, never named A3) and check 7 diverged (VP-003's
# missing disease_similarity chart was silently omitted from B5, no
# per-chart absent marker, unlike every other section's explicit-absent-
# marker discipline). Developer fixed both at commit 8717382 ("fix(f5): A3
# non-validated caveat coverage + B5 per-chart absent markers (EXP-024 r1
# checks 5c/7)"): (a) `HANDOFF_REPORT_DISCLAIMER_KO` rescoped
# "설문(A5/B1)" -> "이 문서 어디에 등장하든 시행된 설문(A3/A5/B1)"; (b) A3's
# 종단위험신호 subsection + staleness-pointer prose (md+PDF) and the FHIR
# RiskAssessment.note (only when staleness_pointer.total_score is not None)
# gained the caveat; (c) B5's markdown/PDF chart loop now emits
# "{chart title}: 생성되지 않음" per missing chart instead of silently
# skipping it (blanket "정보 없음 (차트 없음)" marker retained only when ALL
# 4 charts are absent). qa delta-gate on the 9514798..8717382 range: full
# CI-mirror suite (independently re-run this session, see below) 1772
# passed / 2 skipped; BUG-043 regression test (`0771534`) re-verified
# mutation-kill; production code outside the two touched files untouched.
# No fix was made in this dispatch (out of scope) — r2 only regenerates and
# re-checks.

r2:
  git:
    commit: 871738254970d039c9279ee8212be29bbc6e7bff
    branch: feat/f5-handoff-report
    tree_status: "clean (git status --porcelain empty, verified before launch and again after both runs)"
    fix_commit_diff_reviewed: >
      apps/ai-server/src/schemas/handoff_report.py (disclaimer wording,
      10 lines changed) + apps/ai-server/src/services/f5_report.py (A3
      caveat insertion md/PDF/FHIR + B5 per-chart absent marker md/PDF,
      35 lines changed) + 2 test files (79 new lines, test-only). Read via
      `git show 8717382` in full before launch — confirmed scope matches
      the HANDOFF's description exactly, no unrelated production change.

  qa_delta_gate_independent_reverify: >
    Not merely cited from HANDOFF — independently re-ran this session:
    `cd apps/ai-server && uv run pytest -q` at commit 8717382 ->
    "1772 passed, 2 skipped, 1 warning in 20.80s" (exact match to the
    HANDOFF's reported count). BUG-043 open-critical-BUG gate scan:
    error.md summary table has 0 open `critical`-severity BUGs whose
    component overlaps the F5 code path (BUG-001's "Handoff pipeline"
    label is a pre-F5, VER-002-era legacy critical BUG re: the old
    interactive_chatbot.py handoff feature, not `src.f5`/`src.services.
    f5_report` — same non-relevance disposition as r1, unchanged this
    round); BUG-043 itself is `resolved` (error.md summary table + full
    entry, verified by qa 2026-07-14, mutation-kill confirmed).

  gate_hook: "same non-triggering command pattern as r1 (`uv run python -m src.continuous_test --f5-from-artifacts ...` does not match EXP_LAUNCH); no GATE_OVERRIDE needed"

  source_artifacts_reverify: >
    Re-hashed both source ledgers before launch — VP-001
    `1b600a35b64d85fda270e326ae2249dec42b4b15525016d6a844a6d067445113`,
    VP-003
    `042b83bb2920059774aee7d83d8443f0ceadd79e4e46121e4795ed8a837a349a` —
    both byte-identical to r1's recorded hashes above and to EXP-023's own
    record; 11 ledger entries each, unmodified; confirmed untouched again
    after both runs completed (do_not_modify held).

  env_reverify:
    python: "3.12.13 (main, Mar 3 2026) [Clang 21.1.4] — identical to r1"
    reportlab: "5.0.0 — identical to r1"
    pypdf: "6.14.2 — identical to r1"

  fresh_out_dirs: "experiments/EXP-024/runs/{vp001_r2,vp003_r2}/out/ created and confirmed empty (`find`) immediately before launch — dedicated new dirs, not overwriting r1's runs/{vp001,vp003}/out/"

  commands:
    vp001: >
      cd apps/ai-server && uv run python -m src.continuous_test
      --f5-from-artifacts /home/neuroai/users/dhkim/aichampion/neurosync/experiments/EXP-023/runs/vp001/artifacts/VP-001
      --out /home/neuroai/users/dhkim/aichampion/neurosync/experiments/EXP-024/runs/vp001_r2/out
    vp003: >
      cd apps/ai-server && uv run python -m src.continuous_test
      --f5-from-artifacts /home/neuroai/users/dhkim/aichampion/neurosync/experiments/EXP-023/runs/vp003/artifacts/VP-003
      --out /home/neuroai/users/dhkim/aichampion/neurosync/experiments/EXP-024/runs/vp003_r2/out

  results:
    vp001: exit 0; md 17,504B; pdf 460,208B; fhir 42,784B; generated_at 2026-07-14T15:34:31.349646
    vp003: exit 0; md 16,023B; pdf 350,513B; fhir 28,799B; generated_at 2026-07-14T15:34:32.730403

  r1_vs_r2_number_identity: >
    Directly diffed r1 vs r2 outputs (timestamp/UUID-normalized). md: the
    ONLY diff lines across both VPs are the `generated_at` header value,
    the disclaimer's "설문(A5/B1)"->"설문(A3/A5/B1)" scope wording, the 2
    (VP-001) / 3 (VP-003) new caveat-insertion lines, and (VP-003 only)
    the 1 new "질환 유사도 차트: 생성되지 않음" line — zero table cells,
    scores, LOINC codes, similarity_scores, CTRS values, dates, or any
    other numeric/factual content differs. FHIR: Patient/Observation/
    QuestionnaireResponse/ServiceRequest resources identical after
    UUID+timestamp normalization; Composition differs only in the same
    disclaimer wording; RiskAssessment differs only by the inserted
    caveat text in `.note` (VP-003 only, since VP-001's staleness pointer
    carries no scored number). Conclusion: every clinically-substantive
    number in r1 is reproduced identically in r2 — the engine's
    deterministic core is unchanged by the fix, exactly as expected since
    the fix touched only disclaimer/caveat/absent-marker text, not any
    score/candidate/trend computation.

# ============================================================================
# r3 (FINAL) — post ADR-038 fix wave (commit 50dd255)
# ============================================================================
# Reason: BUG-044 multi-renderer adjudication (qa, triggered by CVR-024
# Finding 1's divergence from REV-047's narrower 2-glyph finding) found that
# 2 of 3 tested renderer/font configs (poppler with no-CJK fontconfig;
# Ghostscript 9.55) reproduced CVR-024's "near-total Hangul dropout" —
# root cause: r1/r2's non-embedded, viewer-substituted CID fonts
# (`HYSMyeongJo-Medium`/`HYGothic-Medium`, ADR-037 Decision 7). ADR-038
# licensed a fix wave at 50dd255: (1) embed 2 subsetted Noto CJK KR TTFs
# (glyph outlines shipped inside the PDF, SHA256-pinned, fail-loud on
# missing/mismatched asset — no silent CID fallback); (2) 4 disclosure
# amendments — A7 VAL-016 validation-drop wording, A6 `reason_summary`
# surfacing when candidates empty and mode!=rag_live, exact-ceiling
# ISS-F2V-028 caveat co-located in A3/A5, FHIR A8-omission note. qa
# GATE:PASS 2026-07-14 (1817 passed/2 skipped; pdffonts emb=yes both fonts;
# byte-identical rasters under default poppler vs no-CJK-fontconfig
# poppler; Ghostscript 9.55 legible; mutation check on fail-loud font
# loading confirmed kill). ADR-038 Decision 5: r3 is the final adjudication
# basis for the PDF channel — no further round planned this mission.

r3:
  git:
    commit: 50dd255736e8db331921495c65514c7158897a99
    branch: feat/f5-handoff-report
    tree_status: "clean (git status --porcelain empty, verified before launch)"
    fix_commit_diff_reviewed: >
      `git diff 8717382..50dd255 --stat` per qa's BUG-044 GATE:PASS entry
      (error.md): 11 files — 2 new font binaries
      (apps/ai-server/assets/fonts/{NotoSansKR,NotoSerifKR}-Subset.ttf) +
      1 new build script (apps/ai-server/scripts/build_korean_fonts.py) +
      4 production files (src/services/f5_report.py — font registration
      replaced with embedded TTFont + SHA256 fail-loud check;
      src/f5.py — A7 VAL-016 wording, A6 reason_summary surfacing, ceiling
      caveat; src/schemas/handoff_report.py — disclosure text constants;
      src/continuous_test.py or FHIR export path — A8-omission note) +
      4 test files. qa's own delta-gate (error.md BUG-044 Resolution
      table) independently confirmed 0 red-line hits (disease-string/
      probability-framing/conformance-claim grep sweep) on this diff —
      cited, not independently re-run this dispatch (qa's charter, not
      duplicated here).

  font_asset_provenance: >
    Read `_BODY_FONT_SHA256`/`_HEADING_FONT_SHA256` module constants
    directly from apps/ai-server/src/services/f5_report.py (lines 493-499)
    and verified against `sha256sum` of the committed .ttf files this
    session (independent re-verification, not cited from qa alone):
      NotoSansKR-Subset.ttf  (_BODY_FONT="NotoSansKR-Subset")
        sha256: 4fc4ff8e7afe3170ecce9f947021abcafa17a0fd37723de66ce74503cdf41846
        module constant _BODY_FONT_SHA256: exact match
      NotoSerifKR-Subset.ttf (_HEADING_FONT="NotoSerifKR-Subset")
        sha256: 6afa567f91cd3879754d7f05e2c9d48a02b270d0f988a6b6a42bffc255979710
        module constant _HEADING_FONT_SHA256: exact match
    Both assets present at apps/ai-server/assets/fonts/, both fail-loud
    (RuntimeError) on missing file or hash mismatch per
    `_register_korean_fonts()` (no silent CID fallback).

  qa_gate_cited: >
    Not re-run this dispatch (qa's charter). Cited from error.md BUG-044
    Resolution / Multi-renderer adjudication (qa, 2026-07-14, commit
    50dd255): full suite 1817 passed/2 skipped; pdffonts emb=yes for both
    embedded fonts on fresh PDFs; poppler-default-fontconfig raster and
    poppler-no-CJK-fontconfig raster byte-identical (md5sum match);
    Ghostscript 9.55.0 raster fully legible (contrast with pre-fix
    near-total dropout on this exact renderer); mutation check on the
    fail-loud font-loading branch confirmed kill; 0 open-critical BUGs in
    the F5 code path (BUG-044 itself now resolved; BUG-001's legacy label
    is pre-F5/non-overlapping, same disposition as r1/r2).

  source_artifacts_reverify: >
    Re-hashed both source ledgers before launch — VP-001
    1b600a35b64d85fda270e326ae2249dec42b4b15525016d6a844a6d067445113,
    VP-003
    042b83bb2920059774aee7d83d8443f0ceadd79e4e46121e4795ed8a837a349a —
    both byte-identical to r1/r2's recorded hashes and to EXP-023's own
    record; unmodified, do_not_modify held.

  env_reverify:
    python: "3.12.13 (main, Mar 3 2026) [Clang 21.1.4] — identical to r1/r2"
    reportlab: "5.0.0 — identical to r1/r2"
    pypdf: "6.14.2 — identical to r1/r2"

  gate_hook: "same non-triggering command pattern as r1/r2 (`uv run python -m src.continuous_test --f5-from-artifacts ...` does not match EXP_LAUNCH); no GATE_OVERRIDE needed"

  fresh_out_dirs: "experiments/EXP-024/runs/{vp001_r3,vp003_r3}/out/ created and confirmed empty (`find`) immediately before launch — dedicated new dirs, not overwriting r1/r2's own out dirs"

  commands:
    vp001: >
      cd apps/ai-server && uv run python -m src.continuous_test
      --f5-from-artifacts /home/neuroai/users/dhkim/aichampion/neurosync/experiments/EXP-023/runs/vp001/artifacts/VP-001
      --out /home/neuroai/users/dhkim/aichampion/neurosync/experiments/EXP-024/runs/vp001_r3/out
    vp003: >
      cd apps/ai-server && uv run python -m src.continuous_test
      --f5-from-artifacts /home/neuroai/users/dhkim/aichampion/neurosync/experiments/EXP-023/runs/vp003/artifacts/VP-003
      --out /home/neuroai/users/dhkim/aichampion/neurosync/experiments/EXP-024/runs/vp003_r3/out
