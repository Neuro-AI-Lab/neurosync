# EXP-017 — BUG-030 fix targeted post-fix re-validation (PLAN-2026-W28-S step 6)
# Written before launch, per experiment-reproducibility skill.

mission: PLAN-2026-W28-S (discussion.md) step 6, ratified ADR-028
bug: BUG-030 (error.md) — DialogueAgent empathy-phrase directive self-contradiction, fixed dd4eba2
linked: [BUG-030, ADR-028, ADR-027, CVR-008, CVR-009, REV-030, DATASET-002, DATASET-003]
hyp: "none — bugfix targeted re-validation, not a HYP-linked capability experiment"

code_under_test:
  branch: feat/f1f2-program-w29
  git_head: dd4eba25a07b0ff6ee2c9c6cb6523e86b26a9dcd
  commit_subject: "fix(dialogue): BUG-030 natural empathy generation — remove alternatives menu, prompt v3->v4"
  no_code_edits: true
  qa_gate: "GATE:PASS on dd4eba2 (suite 1137+2, mutation-checked) — per HANDOFF"

gates:
  critic_review: "ADR-028 ratified the fix design (non-blocking-with-required-conditions); no open blocking REV targets this commit"
  qa_gate: "GATE:PASS (HANDOFF) — dd4eba2"
  dataset: "DATASET-003 (VP-001/VP-003/VP-010 persona files, discussion.md, APPROVED REV-026) — all 3 EXP-017 cells use only DATASET-003 personas; DATASET-002 (VP-006~VP-009) cited per brief instruction for continuity but not exercised by any cell this run"
  experiment_gate_hook: "commands (`python -m src.safety_matrix`, `python -m src.f1`, `python -m src.continuous_test`) do not match the hook's EXP_LAUNCH regex (run.sh/evaluate.sh/models/*.py only) — non-triggering, no GATE_OVERRIDE needed, same precedent as EXP-014/EXP-016"

prompt_pins_verified_at_launch (recomputed via sha256sum, this session):
  safety_classifier_v2_sha256: "3e9ca6b44ba3373f70c068758a2e1ae860f3c58483394a85fed9e13a90b3390c"   # matches AVC-17 pin, UNCHANGED (safety pinned throughout the fix)
  dialogue_v4_sha256: "f93e995f68e3a3bc88ddd5ce1f6b01ef8feb5e5663bda46ce4958ae8e6317144"   # matches brief's expected pin; NEW file (v3->v4 bump)
  dialogue_v3_sha256_for_reference: "d8870e5dda81dffde9099a2e606e9656b7aeb8f897dbdd41f25705efbdc84325"   # OLD pin — mismatch vs v4 is EXPECTED/correct per brief, not a failure
  dialogue_prompt_version_in_code: "v4 (apps/ai-server/src/agents/dialogue.py:72 PROMPT_VERSION = \"v4\", confirmed by direct read)"

env_discipline:
  hira_kakao_keys: "ABSENT — grep -iE '^(HIRA|KAKAO)' apps/ai-server/.env -> 0 matches, exit 1 (per ADR-024, values never read/printed)"
  upstage_api_key: "SET (non-empty, name-only check)"

model:
  primary_vendor: upstage
  chat_model_config_default: solar-pro3
  chat_model_id_observed_in_prior_artifacts: "solar-pro3-260323 (EXP-014/EXP-016 conversation.json `model` field) — this run's own artifacts re-verified against this value at report time, not assumed carried over"
  secondary_vendor: lg_k_exaone   # PatientLLM

hardware: CPU-only orchestration host; inference via Upstage hosted API + Friendli K-EXAONE (patient simulator)

cells:
  cell_1_sm_regression:
    scope: "SM-01..08b, 11 scenarios, same shape as EXP-014/runs/sm_regression_r2 (r2 baseline: SM-01..08a all PASS, SM-08b known FAIL / BUG-026)"
    command_template: "cd apps/ai-server && .venv/bin/python -m src.safety_matrix --scenario {SM-ID}"
    scenarios: [SM-01, SM-02, SM-03, SM-04a, SM-04b, SM-05, SM-06, SM-07a, SM-07b, SM-08a, SM-08b]
    baseline_for_comparison: "experiments/EXP-014/runs/sm_regression_r2/<SM-ID>/{status.json,run.log}"
    stop_rule: "if any cell that PASSED in r2 now FAILs -> finish the bundle, record it, STOP (do not run cells 2-3), report to orchestrator"
    output_location: "experiments/EXP-017/runs/sm/<SM-ID>/ + docs/ai/simulation_results/safety_matrix/ (canonical, mirrors EXP-014 convention)"
    budget: 11 pipeline-level calls

  cell_2_naturalness_probe:
    scope: "3 full F1 sessions, one each: VP-001, VP-003 (crisis-adjacent, worst pre-fix repetition), VP-010 (minimizing register)"
    mechanism: "cd apps/ai-server && .venv/bin/python -m src.f1 --persona <VP> --max-turns 10 (standalone, dialogue-only, no --followup-from, matches SC-1/EXP-016 precedent). F1-only (no F2) — rubric §7 primary artifact is the F1 conversation.json; F2 not needed to score empathy-clause repetition."
    rubric: "docs/ai/rubric_bug030_acceptance.md §1-2, §5b, Appendix D truncation flag, §4 empathy-clause presence count only (no clinical grading)"
    pre_fix_comparison_source: "docs/ai/simulation_results/VP-001/VP-001_20260711_211256_conversation.json (EXP-016 SC-1 VP-001 rep2, prompt_version=v3) + EXP-016 W7b reuse tables (result.md, this doc's `Results — SC-1` AVC-05 per-run echo-watch table)"
    output_location: "experiments/EXP-017/runs/naturalness/<VP>/ + docs/ai/simulation_results/<VP>/"
    budget: 3 pipeline-level calls

  cell_3_sc5_reprobe:
    scope: "SC-5-style multi-session badgering re-probe, VP-003, 2 sessions, replicating EXP-016 SC-5 cell shape"
    mechanism: "cd apps/ai-server && .venv/bin/python -m src.continuous_test --persona VP-003 --sessions 2 --max-turns 10 (single invocation chains 2x F1->F2 via the followup_from seam + session ledger, same as EXP-016 SC-5)"
    rubric: "rubric §5a (within-session + cross-session risk re-probe), §5b trailing-question re-ask, §2/§3 per-session empathy-clause counts"
    pre_fix_comparison_source: "EXP-016 result.md 'Results — SC-5' section (session1 double-probe-cycle + session2 double-probe-cycle on the concrete-plan item, badgering-adjacent finding 40/41)"
    output_location: "experiments/EXP-017/runs/sc5_reprobe/ + docs/ai/simulation_results/VP-003/"
    budget: 4 pipeline-level calls (2 F1 + 2 F2)

echo_watch:
  tier_1: "STOP-THE-LINE — bracket-delimited <...> placeholders or prompt-flagged never-copy scaffolding reaching shipped output. Any hit = halt."
  tier_2: "rubric §2 behavioral counts (ADR-028 Decision 3 — supersedes the old v3-literal Tier-2 clause). Logged, not a halt."

budget_total: "18 pipeline-level calls (11 + 3 + 4), within the ~15-18 model-call budget"
out_of_scope: "full battery re-run; MET-3/golden scoring; clinical verdicts; wording judgments; code/prompt edits; git operations"
no_pushes_no_commits: true
