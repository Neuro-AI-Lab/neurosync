# EXP-019 — Live F1->F2->F3 functional validation (PLAN-2026-W28-V step 8)
# Written before launch, per experiment-reproducibility skill.

mission: PLAN-2026-W28-V (discussion.md) step 8, gated by ADR-031/ADR-032/REV-036
linked: [PLAN-2026-W28-V, ADR-031, ADR-032, REV-036, VAL-014]
hyp: "none — F3 pipeline-functional validation (non-diagnostic, non-validated-content v0), not a HYP-linked capability experiment"

code_under_test:
  branch: feat/f1f2-program-w29
  git_head_at_launch: 1e4223ad3579e8d5e2d393c5618b82b10fd7d79f
  no_code_edits_this_run: true
  working_tree_state: >
    Uncommitted (not blocking per HANDOFF — "working tree may be uncommitted"):
    modified: apps/ai-server/src/continuous_test.py, apps/ai-server/tests/test_continuous_test.py,
    apps/ai-server/tests/test_multi_session_ledger.py, docs/ai/PRD_task1_v2.md,
    docs/ai/checklist_task1.md, docs/ai/workflow_checklist_f1f2.md;
    untracked: apps/ai-server/src/f3.py, apps/ai-server/src/schemas/survey_result.py,
    apps/ai-server/src/scoring/item_bank.py, apps/ai-server/tests/simulation/survey_answer_llm.py,
    apps/ai-server/tests/simulation/test_survey_answer_llm.py,
    apps/ai-server/tests/test_continuous_test_f3.py, apps/ai-server/tests/test_f3.py,
    apps/ai-server/tests/test_f3_hpi_isolation.py, apps/ai-server/tests/test_item_bank.py,
    docs/ai/f3_quick_dev_plan.md. `git stash list` empty. This is the exact file set the
    HANDOFF names as F3 implementation artifacts.
  qa_gate: "GATE:PASS per HANDOFF — suite 1342+2, mutation-checked, pins exact (safety v2 / dialogue v4)"

gates:
  critic_review: "REV-036 non-blocking-with-conditions (4 major, 3 minor) dispositioned by ADR-032 -- item 1 (v0 scope) ratified, item 4 (HPI-isolation tests) required before qa GATE:PASS (met per HANDOFF's tests/test_f3_hpi_isolation.py); no blocking REV open in discussion.md"
  qa_gate: "GATE:PASS (HANDOFF) -- suite 1342+2, mutation-checked, pins exact"
  dataset: "DATASET-003 (VP-001/VP-003/VP-012 golden-domain-label personas, APPROVED REV-026) -- all 3 cells use only DATASET-003 personas"
  experiment_gate_hook: "command (python -m src.continuous_test) does not match the hook's EXP_LAUNCH regex (run.sh/evaluate.sh/models/*.py only) -- non-triggering, no GATE_OVERRIDE needed, same precedent as EXP-014/016/017/018"

prompt_pins_verified_at_launch (recomputed via sha256sum, this session, before any launch):
  safety_classifier_v2_sha256: "3e9ca6b44ba3373f70c068758a2e1ae860f3c58483394a85fed9e13a90b3390c"   # matches AVC-17 pin / workflow_results_f1f2.md, UNCHANGED
  dialogue_v4_sha256: "f93e995f68e3a3bc88ddd5ce1f6b01ef8feb5e5663bda46ce4958ae8e6317144"   # matches AVC-17 pin / workflow_results_f1f2.md, UNCHANGED
  both_pins_match: true

env_discipline:
  lg_k_exaone_api_key: "PRESENT (name-only check, grep -c '^LG_K_EXAONE_API_KEY=' apps/ai-server/.env -> 1; value never read/printed)"
  lg_k_exaone_endpoint_id: "PRESENT (name-only check -> 1; value never read/printed)"
  upstage_api_key: "PRESENT (name-only check -> 1; value never read/printed)"
  db_preflight: "DB_OK reachable (postgresql+asyncpg://neurosync:***@223.194.33.26:28881/neurosync), verified via src.continuous_test._db_preflight() before launch"

model:
  primary_vendor: upstage
  chat_model_id_expected: "solar-pro3-260323 (dialogue v4/safety v2/clinical_slot/sentiment/domain_inference) -- re-verified per-artifact at report time"
  secondary_vendor: lg_k_exaone   # PatientLLM (F1) + SurveyAnswerLLM (F3), friendli.ai, temperature 0.7

hardware: CPU-only orchestration host; inference via Upstage hosted API + Friendli K-EXAONE

cells:
  - id: vp001_2session
    persona: VP-001
    cmd: ".venv/bin/python -m src.continuous_test --persona VP-001 --sessions 2 --answer-mode llm"
    purpose: "two-session chain, expect mood->PHQ-9; exercises session-to-session scale_scores chaining (session 2's F2 consumes session 1's F3 projection per plan SS6 item 4)"
  - id: vp003_1session
    persona: VP-003
    cmd: ".venv/bin/python -m src.continuous_test --persona VP-003 --answer-mode llm"
    purpose: "crisis-adjacent persona; record F2 recommendation + F3 behavior AS-GIVEN; recommended_questionnaire=None (no-questionnaire-indicated path) is a valid, not a failing, outcome"
  - id: vp012_1session
    persona: VP-012
    cmd: ".venv/bin/python -m src.continuous_test --persona VP-012 --answer-mode llm"
    purpose: "substance persona, expect substance->AUDIT-C"
  - id: gad7_cell
    status: "SKIPPED-awaiting-user-material — no run"
    reason: "GAD-7 item bank unpopulated by design (item_bank.py v0); no dedicated cell scheduled per plan SS8. Any organic GAD-7 surfacing in cell 1 (VP-001) is checked as an incidental item_bank_unpopulated-path proof, not separately budgeted."

wording_discipline_binding: >
  ADR-032 (2): outcomes described as "pipeline-functional evidence obtained on
  non-validated administration content (construct-label v0)" -- never as PHQ-9/AUDIT-C
  clinical results, never 인증/통과/certified. similarity_score never a probability.
  Persona-consistency judgment is clinical-validator's charter -- this entry tabulates
  simulator scores next to the persona's documented expected scores WITHOUT verdicting.
