# EXP-014 — W2 bundled SM-01..08b safety-matrix regression, post 5 f1.py-touching changes
# (BUG-021 fail-fast, BUG-011 turn-0 crisis fix, dialogue v3 autonomous turn-0 greeting,
# is_revisit field [atomic with v3], narrowed carry channel). Written before launch, per
# experiment-reproducibility skill. Required by PLAN-2026-W28-Q wave W2 / REV-023 ruling 4
# (Issue 7 bisection-on-failure protocol pre-registered in discussion.md PLAN-2026-W28-Q
# W1-complete status append, 2026-07-11).

mission: PLAN-2026-W28-Q (discussion.md) — wave W2, sequencing item 2
rev_binding_condition: "REV-023 ruling 4 / Issue 7 — bundled regression licensed for the PASS case only; on ANY non-pass cell, do not report the wave clean, execute the pre-registered bisection-on-failure protocol (fixed order: (1) BUG-011, (2) dialogue v3 greeting, (3) BUG-021, (4) is_revisit, (5) narrowed carry), contingency <=6 calls"

code_under_test:
  branch: feat/f1f2-program-w29
  git_head: d666a2c2a138cb48dd31b3f2197d346cf0585aeb
  no_code_edits: true

gates:
  hyp: "none — validation/measurement task, matches EXP-002/003/005..013 convention (no HYP-NNN)"
  critic_review: "REV-023 NON-BLOCKING for plan presentation (0 fully-blocking, 2 blocking-scoped [neither gates this run], 5 major incl. Issue 7 = this run's bisection protocol, pre-registered before launch per resolution item 7)"
  qa_gate: "W1 GATE:PASS (discussion.md PLAN-2026-W28-Q status append, 2026-07-11) — CI-mirror 833 passed/2 skipped; BUG-021 resolved (verified CLI-level, 11/11 regression tests); BUG-011 resolved (f1.py:1124, 3/3 repro); BUG-022 guard active; commit hygiene clean"
  dataset: "DATASET-003 (VP-001~VP-004, discussion.md) — safety-matrix scenarios are deterministic ScriptedPatient fixtures (tests/simulation/scenarios/SM-*.json), no persona files loaded (code-read confirmed: scripted_patient.py plays back JSON-embedded utterance scripts, zero calls into PERSONA_DIR/persona.md); DATASET-003 cited per EXP-002/003/013 convention for gate-checklist continuity, not because this run reads VP-file content"
  experiment_gate_hook: "command `python -m src.safety_matrix` does not match the hook's EXP_LAUNCH regex (run.sh/evaluate.sh/models/*.py only) — non-triggering, no GATE_OVERRIDE needed, same precedent as EXP-012/EXP-013"
  archive_gate_hook: "DISARMED (.claude/state/blind_active absent, confirmed) — Phase 1 (W0/W1/W2), expected; not yet armed until W6->W7"

prompt_pins_verified_at_launch:
  safety_classifier_v2_sha256: "3e9ca6b44ba3373f70c068758a2e1ae860f3c58483394a85fed9e13a90b3390c"   # PINNED, matches HANDOFF exactly, re-verified this launch
  dialogue_v3_sha256: "2440f29b63315d3613b8726b982d612479d97b7be8e4a796c0a5f80e947e1e0b"   # NEW W2 licensed change, matches HANDOFF exactly, re-verified this launch
  dialogue_v2_untouched: "git diff c85b3e1 HEAD -- docs/ai/prompts/dialogue/v2.system.md -> empty; only commit ever touching this file is abcdfb7 (pre-program) — confirmed untouched"
  clinical_slot_v3_sha256: "10458b2c0cf24249a5057bd65d8c10b20aee9dcb535167eb94b4c4436ab7c742"   # in effect, unchanged since EXP-002/003 baseline, recorded for completeness (not a pin under active W2 change)
  persona_file_hashes: "N/A — safety_matrix.py's 11 SM-* scenarios use ScriptedPatient (hardcoded JSON scripts), not PatientLLM+persona.md; zero PERSONA_DIR/persona-file reads in this harness (code-read: scripted_patient.py, safety_matrix.py both persona-file-free). VP-file hashing is not applicable to this regression; noted as a deviation from the brief's literal checklist item, reasoned above."

env_discipline:
  hira_kakao_keys: "ABSENT — name-only grep `grep -iE '^(HIRA|KAKAO)' apps/ai-server/.env` -> 0 matches, exit code 1 (confirmed this launch, per ADR-024, values never read/printed)"
  upstage_api_key: "SET (non-empty, name-only check, value never printed) — required by safety_matrix.py's own preflight"

bug021_check:
  method: "grep -c 'prompt not found' + grep -c 'using fallback' on every per-cell run.log; grep 'prompts_degraded.*true' on every *_result.json artifact"
  pass_criterion: "zero occurrences of either in every one of 11 cells; a nonzero is an immediate stop-and-report per the brief"

sm_matrix:
  command_template: "cd apps/ai-server && .venv/bin/python -m src.safety_matrix --scenario {SM-ID}"
  scenarios: [SM-01, SM-02, SM-03, SM-04a, SM-04b, SM-05, SM-06, SM-07a, SM-07b, SM-08a, SM-08b]   # 11, full matrix, source: tests/simulation/scenarios/SM-*.json glob
  n_per_scenario: 1   # deterministic scripted patient, matches EXP-002/003/013 methodology
  output_location: "docs/ai/simulation_results/safety_matrix/ (canonical) — mirrored into experiments/EXP-014/runs/sm_regression/<cell>/"
  pass_criteria_source: "each scenario's own encoded `expectations` list in tests/simulation/scenarios/SM-*.json, evaluated by safety_matrix.py's evaluate_expectations() — all_passed = all(checks passed)"
  baseline_for_reference: "EXP-002 (v2 baseline, 9/9 scenarios comparable) and EXP-013 (post PR#38-merge, 10/11 all_passed, SM-08b explained non-regression) — this run is a regression check after 5 f1.py-touching W2 changes, not a new baseline"
  budget: "11 of 159 total program calls (plan §5 budget table)"

evidence_source_type_scope: "EXCLUDED from this regression — F2-side, orthogonal (plan §5); safety_matrix.py exercises F1 only"

model:
  primary_vendor: upstage
  chat_model: solar-pro3
  base_url: https://api.upstage.ai/v1
  secondary_vendor: lg_k_exaone   # patient simulator not used by ScriptedPatient path; safety_matrix's real agents call Upstage only

hardware: CPU-only orchestration host; inference via Upstage hosted API

wording_discipline: "no 인증/certified/passed-beyond-this-matrix claims; safety_classifier prompt (v2) is pinned/unchanged — this regression does NOT re-validate safety_classifier itself; maximal claim licensed by a clean run is 'SM-01..08b regression pass'"

no_pushes_no_commits: true
no_code_edits_to_src: true
