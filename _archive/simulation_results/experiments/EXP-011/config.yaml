# EXP-011 — Clean populated-path re-verification on the BUG-019... no, VAL-011-fixed tree
# (REV-017 Finding 1 / f2.py:465 full-chunk risk-lexicon check). VP-001~004 RAG mode n=2 each
# (8 runs), k=3 (certified default). PLAN-2026-W28-K Task 7 follow-up per REV-017 Resolution #1.
# Genuinely new live LLM calls. Draws NO shippability/certification verdict — critic's REV-018.

git_head: f6cae4ea5d51e25a229699226ff92924e6bd68a4
git_branch: feat/f2-continuation-w28h
git_status_note: >
  Working tree carries uncommitted changes at launch (git status --porcelain): apps/ai-server/src/f2.py,
  apps/ai-server/src/rag/retrieval.py, apps/ai-server/src/schemas/ai_predicted_disease.py,
  apps/ai-server/tests/rag/test_retrieval.py, apps/ai-server/tests/test_ai_predicted_disease.py,
  apps/ai-server/tests/test_f2_grounding.py, apps/ai-server/tests/test_f2_pipeline.py,
  apps/ai-server/tests/test_hpi_isolation.py — identical file set to EXP-009's own working tree
  (same git_head f6cae4e), now carrying the REV-017 Finding 1 fix (f2.py:465: full chunk_text check
  added alongside the quote-only check). Confirmed by direct grep this launch:
  `_contains_any(chunk_text, _RISK_PHRASES) or _contains_any(quote, _RISK_PHRASES)` present at
  apps/ai-server/src/f2.py:465, matching qa's VAL-011 re-gate citation exactly.

suite_reverified: >
  cd apps/ai-server && .venv/bin/python -m pytest tests/ -q -> 812 passed, 0 failed
  (independently re-run by experiment-tracker before launch; exact match to qa's VAL-011 re-gate
  claim, error.md "QA re-gate (REV-017 Finding 1 fix)", 2026-07-09).

python: apps/ai-server/.venv (Python 3.12.13)
working_dir: apps/ai-server
prompts_base_dir_used: /home/neuroai/users/dhkim/aichampion/neurosync/docs/ai/prompts
prompts_base_dir_note: >
  BUG-013 operational workaround (documented, no .env/code edit): PROMPTS_BASE_DIR exported
  repo-root-absolute at invocation; conversation input paths passed absolute. Same workaround as
  EXP-004 through EXP-010.

prompt_version_pins:
  domain_inference: v2   # docs/ai/prompts/domain_inference/v2.system.md, zero diff at this HEAD
                          # (independently re-confirmed: git diff --stat -- '*v2.system.md' empty)

model:
  primary_vendor: upstage
  chat_model: solar-pro3
  base_url: https://api.upstage.ai/v1
  secondary_vendor: lg_k_exaone (K-EXAONE, strategy=benchmarked fallback, not observed to fire)
  agent: DomainInferenceAgent
  max_tokens: 4096   # unchanged since BUG-017 phase B
  k: 3   # --k flag default, certified value (ADR-018 certification batch's own default)

bug_and_adr_state_confirmed_at_head:
  ADR-018: "F2 RAG-arm certification LIFTED (full, VP-003 AND VP-001), standing conditions apply to
    every future RAG batch: (a) secondary taxonomy audit + retrieval_meta.queries audit incl.
    chief_complaint/HPI, (b) finish_reason/usage/raw_response/validation_errors verbatim, (c) VAL-010
    stays open (not closed by lift)."
  ADR-020: "Track B disease-source = path1 (chunk-derived, code-side), 4 binding conditions: (1)
    VAL-011 risk-lexicon filter on every candidate quote, (2) source_id+quote provenance fields,
    (3) two-hop-proxy caveat disclosed in reason_summary, (4) similarity_score clamped [0,1]."
  REV-017: "Authoritative evidence review of EXP-009/EXP-010 (Task 6 populated-path batch) — Issue #1
    (blocking): risk-lexicon filter checked only a +/-40-char extracted-quote window, not the full
    source chunk text; reproduced live twice (VP-003/run2, case_card:664 + case_card:563, both k=3
    and k=5). Ruled populated path NOT SHIPPABLE. VAL-011 reopened (blocking). Resolution #1: fix
    f2.py's check to cover the full chunk text, qa re-gates with a mutation check, experiment-tracker
    re-runs a clean VP-003(+VP-001... this brief: VP-001-004 all) populated-path n=2 batch, critic
    re-reviews (REV-018) before any 'shippable' wording is used. k=3 ratified (unaffected by Issue #1
    disposition). ADR-018 RAG-arm certification NOT invalidated by Track B code (confirmed structurally
    untouched)."
  VAL-011: "reopened by REV-017 (blocking) -> re-closed by qa re-gate 2026-07-09 (error.md 'QA re-gate
    (REV-017 Finding 1 fix)'). Fix located at f2.py:465: `_contains_any(chunk_text, _RISK_PHRASES) or
    _contains_any(quote, _RISK_PHRASES)`. qa independently reproduced closure via (a) a fresh synthetic
    long-chunk/distant-term fixture and (b) a real EXP-009 artifact replay of BOTH live leak instances
    (case_card:664, case_card:563) — both now drop under the fixed code. Mutation-checked load-bearing
    (reverting to quote-only reproduces the exact original leak). Confirmed non-over-blocking (benign
    long chunks with no risk phrase anywhere still ship). Suite green at 812/812. qa's verdict:
    'this does not itself license shippable/certified wording for Track B's populated path' — that is
    explicitly this EXP-011 + critic's REV-018 job, not qa's."

pre_run_checklist:
  plan: "PLAN-2026-W28-K Task 7 follow-up (discussion.md, REV-017 Resolution #1) — this EXP is the
    'clean re-verification populated-path batch' REV-017 requires before any shippable/certified
    wording can be reused."
  critic_review: "REV-017 (discussion.md) — Issue #1 was blocking (populated path NOT SHIPPABLE);
    Resolution #1's code-fix + qa-mutation-check preconditions are now satisfied (see VAL-011 re-gate
    above). This run is itself part of discharging Resolution #1's remaining step ('experiment-tracker
    re-runs a clean ... populated-path n=2 batch') — REV-017 is not yet re-reviewed post-fix (that is
    REV-018, critic's next step, out of this agent's scope), but no blocking issue remains open against
    the CODE under test — the code fix is what REV-017 demanded, and it is qa-verified in place."
  qa_gate: "GATE: PASS (error.md, 'QA re-gate (REV-017 Finding 1 fix)', 2026-07-09) — suite 812 green
    at that gate; independently re-run here, exact match (812 passed)."
  dataset: "DATASET-004 (F2 golden domain labels) APPROVED with amendments (REV-007) — supplementary
    top-1/top-3 cross-check only. DATASET-005 (RAG corpus) — unchanged since EXP-009/EXP-010, row
    counts re-verified 0 delta below."
  db_preflight: "PASS — see preflight.log. Corpus counts match EXP-005 through EXP-010 exactly
    (case_card 1248, qa 1789, symptom 40, disease 26, disease_symptom 169, session_insights 4), 0
    delta. No DATABASE_URL/password printed."
  gate_hook: "experiment_gate.py PreToolUse hook respected — python -m src.f2 does not match
    models/*.py or run.sh/evaluate.sh regex. No GATE_OVERRIDE used, none needed (no open critical BUG
    — VAL-011 is resolved per qa's re-gate; no open blocking REV against this build's code; DATASET
    entries present)."

db_preflight:
  method: "src.dependencies.get_settings()/get_sessionmaker() — project's own settings path, no CLI
    overrides, SELECT 1 + rag.{case_card,qa,symptom,disease,disease_symptom,session_insights} row
    counts"
  timeout_s: 10
  retries: 0
  log: experiments/EXP-011/preflight.log
  verdict: PASS

runs:
  clean_populated_path_reverification:
    command: ".venv/bin/python -m src.f2 --conversation <F1 conversation.json, absolute path> --out <run dir> (no --no-rag, k defaults to 3)"
    personas: [VP-001, VP-002, VP-003, VP-004]
    n_per_vp: 2
    input_selection: "byte-identical EXP-002-era F1 conversation artifacts, reused for reproducibility
      (SHA256-confirmed reuse below, byte-identical to EXP-004 through EXP-010's own recorded values)."
    inputs:
      VP-001:
        run1: docs/ai/simulation_results/VP-001/VP-001_20260707_152922_conversation.json  # sha256 c6af3fa9e5d6f2aa5e8db1108c59c283f38c625b8ef7e363fcb51f3242582816
        run2: docs/ai/simulation_results/VP-001/VP-001_20260707_153451_conversation.json  # sha256 5f5e0b5d6fa6c22e6145a09fa195867aadc8b758ce1a862b41ec9569d0a95a47
      VP-002:
        run1: docs/ai/simulation_results/VP-002/VP-002_20260707_154130_conversation.json  # sha256 6f0e190e859206c9d26e1972510bb543d900277e2b9e5d3240fe27b1b0d8f6c3
        run2: docs/ai/simulation_results/VP-002/VP-002_20260707_154407_conversation.json  # sha256 f292e5622a81688780c6b370d5ae641b276a319d3749d09e5291918b876baf09
      VP-003:
        run1: docs/ai/simulation_results/VP-003/VP-003_20260707_155149_conversation.json  # sha256 fbabd8202367322d338ea99eb154cf818f43de1094278b3e85340b5da02ccb96
        run2: docs/ai/simulation_results/VP-003/VP-003_20260707_155446_conversation.json  # sha256 126f5c012cf69e798ddfbe5fd10d2142432e44785f54a9607bf117203c4dabe9
      VP-004:
        run1: docs/ai/simulation_results/VP-004/VP-004_20260707_155542_conversation.json  # sha256 8ad65b5b25dfae9dde75a342ed3981126e68966b9e87d90ed99e9ba681e54901
        run2: docs/ai/simulation_results/VP-004/VP-004_20260707_155625_conversation.json  # sha256 7f34c9962e5aea3963dc5118b470d314e1c77070a5266a075d77bbbbe1b5fc71
    sha256_verification: "all 8 independently computed by this agent via sha256sum before launch;
      all 8 byte-identical to EXP-009's own recorded values (and EXP-004 through EXP-010's, where
      applicable) — same 8 F1 conversation files reused across the whole PLAN-2026-W28-K Task 6/7
      lineage."
    binding_requirement: "genuinely NEW live LLM calls (ADR-018 standing condition), NOT a replay of
      cached response bytes."
    verification: "every run's retrieval_meta.mode must == 'rag' (no silent llm_only downgrade,
      REV-006 condition 6). ai_predicted_disease.mode must == 'rag_live' (or a legitimate
      experimental_unpopulated fallback IF raw_chunks is empty — reported honestly if observed).
      finish_reason/usage/raw_response/validation_errors captured verbatim per run (ADR-018 standing
      condition b)."

audits_required_every_run:
  - "ADR-018(a): secondary manual broader-taxonomy audit over EVERY accepted post-cascade
    domain_candidates[].evidence[].quote AND EVERY ai_predicted_disease.candidates[].quote."
  - "ADR-018(a continued): retrieval_meta.queries audit incl. chief_complaint/HPI content, VP-003/VP-004."
  - "mode-honesty (both domain_candidates retrieval_meta.mode and ai_predicted_disease.mode)."
  - "finish_reason/usage/raw_response/validation_errors verbatim."
  - "DB preflight DSN masked, no secret printed."
  - "HPI red-line live check: ai_predicted_disease is a sibling top-level key; disease name/score/quote
    appear in no clinical slot / history_of_present_illness."
  - "NEW (REV-017 Issue #2, the load-bearing check this batch exists for): for EVERY shipped
    ai_predicted_disease.candidate, verify its source_id chunk's FULL text (from the run's own
    chunk_texts) is NOT risk-lexicon-flagged. Report as a chunk-level count. Specifically confirm
    VP-003's previously-leaking chunks (case_card:664, case_card:563) are now either not cited or their
    candidates dropped."

not_in_scope:
  - "Certification/pass/shippability verdict — critic's REV-018, not this agent."
  - "Code/prompt/test edits."
  - "k-sweep (EXP-010 stands, not re-run)."
