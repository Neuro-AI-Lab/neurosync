# EXP-007 — BUG-019 live diagnostic capture (PLAN-2026-W28-H Wave 5)
# Diagnostic capture only, NOT the full certification batch. Reproduces the VP-003 and VP-001
# RAG-mode schema-validation failures with the now-instrumented code (Wave-3, qa GATE:PASS,
# suite 758) so the failing field(s) + raw response become diagnosable.

git_head: f4804136acd1bdb6c74344800dc765ab1358d926
git_branch: feat/f2-continuation-w28h
git_status_note: >
  Working tree carries the uncommitted PLAN-2026-W28-H Wave 3 diff (BUG-019 instrumentation:
  domain_inference.py _parse/run now return+persist raw_response + field-level validation_errors;
  f2.py writes both to artifact JSON + report.md; plus Track A RAG-HTTP retirement (main.py,
  rag/auth.py, rag/route.py, rag_chat.py, tests/rag/test_route_auth.py) and Track B/C untracked
  files (continuous_test.py, schemas/ai_predicted_disease.py, test_hpi_isolation.py, etc.) —
  none of which this agent edits. No code/prompt changes made by this agent (run-only, per brief
  "you run, you don't fix").

suite_reverified: >
  cd apps/ai-server && .venv/bin/python -m pytest tests/ -q -> 758 passed, 0 failed
  (independently re-run by experiment-tracker before launch, exact match to qa's Wave-4
  GATE:PASS, 2026-07-09).

python: apps/ai-server/.venv (Python 3.12.13)
working_dir: apps/ai-server
prompts_base_dir_used: /home/neuroai/users/dhkim/aichampion/neurosync/docs/ai/prompts
prompts_base_dir_note: >
  BUG-013 operational workaround (documented, no .env/code edit): PROMPTS_BASE_DIR exported
  repo-root-absolute at invocation; conversation input paths passed absolute. Same workaround
  as EXP-004/EXP-005/EXP-006.

prompt_version_pins:
  domain_inference: v2   # docs/ai/prompts/domain_inference/v2.system.md, unchanged this wave

model:
  primary_vendor: upstage
  chat_model: solar-pro3
  base_url: https://api.upstage.ai/v1
  secondary_vendor: lg_k_exaone (K-EXAONE, strategy=benchmarked fallback, not observed to fire)
  agent: DomainInferenceAgent
  max_tokens: 4096   # domain_inference.py, unchanged since BUG-017 phase B (EXP-006)

pre_run_checklist:
  plan: "PLAN-2026-W28-H (discussion.md) Wave 5 (exp) — BUG-019 live diagnostic capture. Mission-scoped, not a HYP-NNN. Not the clean VP-003/VP-001 RAG n=2 re-verification (that is a later step, gated on a root-cause fix)."
  critic_review: "REV-013 — NON-BLOCKING (0 blocking / 2 major / 2 minor). Major issues target Track A (resolved by ADR-017 RETIRE) and Track B (isolation design gap, does not touch F2/BUG-019 diagnostic scope). No REV targets this diagnostic capture step specifically; REV-011's 5 pre-registered metric rules (sec 3 i-v) and the retrieval_meta.queries audit requirement (rule ii) remain binding and are applied below."
  qa_gate: "GATE: PASS (error.md BUG-019, Wave-3 instrumentation verification, 2026-07-09) — suite 758 green, independently re-run below (758 passed, 0 failed, exact match). Instrumentation (raw_response + validation_errors persistence on both parse- and schema-failure paths) confirmed non-vacuous by qa's own mutation checks."
  dataset: "DATASET-004 (F2 golden domain labels) APPROVED with amendments (REV-007); DATASET-005 (RAG corpus, rag.case_card+rag.qa) active, no VP-content leakage found (2026-07-09 data audit). Reused, not re-verified this run (unchanged corpus, row counts confirmed 0 delta below)."
  db_preflight: "PASS — see preflight.log. Corpus counts match EXP-006/EXP-005 exactly (case_card 1248, qa 1789, symptom 40, disease 26), 0 delta. No DATABASE_URL/password printed."
  gate_hook: "experiment_gate.py PreToolUse hook respected — no GATE_OVERRIDE used (no open critical BUG blocks this launch per the hook's own criteria: BUG-019 is major, not critical; no open blocking REV; DATASET-004/005 present)."

db_preflight:
  method: "src.dependencies.get_settings()/get_sessionmaker() — project's own settings path, no CLI overrides, SELECT 1 + rag.{case_card,qa,symptom,disease} row counts"
  timeout_s: 10
  retries: 0
  log: experiments/EXP-007/preflight.log
  verdict: PASS

runs:
  rag_arm_diagnostic_only:
    command: ".venv/bin/python -m src.f2 --conversation <F1 conversation.json, absolute path> (no --no-rag)"
    personas: [VP-003, VP-001]
    n_per_vp: 2
    input_selection: "byte-identical EXP-006-era F1 conversation artifacts (20260707/EXP-002-era), reused for reproducibility — input_sha256 verified to match EXP-006's own runs exactly before launch"
    inputs:
      VP-003:
        run1: docs/ai/simulation_results/VP-003/VP-003_20260707_155149_conversation.json  # sha256 fbabd8202367322d338ea99eb154cf818f43de1094278b3e85340b5da02ccb96
        run2: docs/ai/simulation_results/VP-003/VP-003_20260707_155446_conversation.json  # sha256 126f5c012cf69e798ddfbe5fd10d2142432e44785f54a9607bf117203c4dabe9
      VP-001:
        run1: docs/ai/simulation_results/VP-001/VP-001_20260707_152922_conversation.json  # sha256 c6af3fa9e5d6f2aa5e8db1108c59c283f38c625b8ef7e363fcb51f3242582816
        run2: docs/ai/simulation_results/VP-001/VP-001_20260707_153451_conversation.json  # sha256 (verified at launch, matches EXP-004/005/006's own VP-001/run2 input)
    verification: "every run's retrieval_meta.mode must == 'rag'; a silent llm_only downgrade = rollback trigger, stop and flag. finish_reason/usage and (on failure) raw_response/validation_errors must be captured per-run per REV-011 sec 3 rule iv (extended this run to the new instrumentation fields)."
    scope_note: "diagnostic capture ONLY — not the full certification batch (no llm_only arm, no VP-002/VP-004). If a run unexpectedly succeeds (parseable domain_candidates), report plainly per brief instruction."

not_in_scope:
  - "Fixing the failure (developer, Wave 6)"
  - "RAG-arm certification verdict (critic)"
  - "Clean n=2 re-verification (later step, once a fix exists)"
  - "llm_only runs"
  - "Track B/C/A"
