# EXP-013 — Wave W3a: post-merge safety matrix + InputNormalizer fallback diagnostic
# + continuous_test.py STT/OCR-off smoke, on the merged PR #38 (STT/OCR/InputNormalizer/
# Sentiment) integration branch. Written before launch, per experiment-reproducibility skill.

mission: PLAN-2026-W28-N (discussion.md, ~line 2971) — execution mission Wave W3a
critic_rulings_tested: "(a) SM matrix MANDATORY, (d) InputNormalizer/ISS-039 live-fallback diagnostic MANDATORY"

code_under_test:
  worktree: /tmp/wt-f1-stt-ocr-integration
  branch: feat/f1-stt-ocr-integration
  git_head: 5bdd379f6eed9f340a9878edd02bbc0f073fd04a   # merge commit, PR #38 into feat/f1-stt-ocr-integration
  merge_base_note: "safety_classifier.py and clinical_slot.py byte-identical both sides of the merge (W1 developer finding, zero diff)"
  no_code_edits: true   # constraint — driver script (task 2) lives under experiments/, not src/

gates:
  hyp: "none — validation/measurement task, matches EXP-002/003/005..012 convention (no HYP-NNN)"
  critic_review: "PLAN-2026-W28-N folded status (discussion.md ~line 2994), critic rulings (a)/(d) — non-blocking, this wave executes them"
  qa_gate: "W2 GATE PASS — fresh baseline Master 411d6a1 = 812/0/0/0; merged branch 5bdd379 = 812/0/0/0 (node-ID lists byte-identical); HPI 12/12; test_prompt_v3+f2_grounding 125/125; RAG grep 0 hits + live TestClient 404"
  dataset: DATASET-003 (VP-001/VP-003, discussion.md) — safety matrix scenarios are deterministic scripted fixtures (tests/simulation/scenarios/SM-*.json), same convention as EXP-002/003
  experiment_gate_hook: "commands used (`python -m src.safety_matrix`, `python -m src.continuous_test`, throwaway driver script) do not match the hook's EXP_LAUNCH regex (run.sh/evaluate.sh/models/*.py only) — confirmed non-triggering, no GATE_OVERRIDE used or needed, same precedent as EXP-012 (REV-019 Issue #6)"

prompt_pins_in_effect_this_wave:
  safety_classifier: v2   # PROMPT_VERSION grep-confirmed in worktree, unchanged since ADR-012 rollback — matches EXP-002's baseline exactly
  dialogue: v2            # matches EXP-002
  clinical_slot: v3       # matches EXP-002/EXP-003
  input_normalizer: v1    # newly live upstream of safety classification for the first time (critic ruling a)

task1_safety_matrix:
  command: "cd /tmp/wt-f1-stt-ocr-integration/apps/ai-server && .venv/bin/python -m src.safety_matrix --scenario all"
  scenarios: [SM-01, SM-02, SM-03, SM-04a, SM-04b, SM-05, SM-06, SM-07a, SM-07b, SM-08a, SM-08b]   # 11, full matrix per ADR-011
  n_per_scenario: 1   # deterministic scripted patient, matches EXP-002/EXP-003 methodology
  output_location: "docs/ai/simulation_results/safety_matrix/ (canonical, no --output-dir flag) — mirrored into experiments/EXP-013/runs/safety_matrix/"
  baseline_for_diff:
    SM-01..07b (9 scenarios): "experiments/EXP-002/runs/safety_matrix/ (safety v2, same pin as this run)"
    SM-08a/08b (2 scenarios): "experiments/EXP-003/runs/safety_matrix/ (only prior data point; that run was safety v3, rolled back — no v2 baseline exists for these 2; this run is the FIRST v2 data point)"

task2_input_normalizer_diagnostic:
  method_a: "live log capture from task1's safety-matrix run (InputNormalizer sits upstream of every scripted-patient turn per f1.py:498-531/910, so all 11 scenarios' turns are real InputNormalizer calls) — grep run.log for the two fallback-signaling log lines emitted inside src/agents/input_normalizer.py: 'InputNormalizer failed, returning original' (WARNING, exception path incl. JSON-parse/schema-ValidationError) and 'Safety expression lost during normalization' (ERROR, safety-preservation fallback path)"
  method_b: "throwaway driver script experiments/EXP-013/driver_input_normalizer_probe.py (no src/ edits) — direct InputNormalizerAgent.run() calls on 6 representative texts (STT-typo/colloquial/dialect/filler examples lifted verbatim from the v1 prompt's own STT-교정-대상 table, plus one risk-phrase+colloquial combination, plus one already-clean control) — captures out.reason_summary/out.change_count/out.normalized_text per call, the ground truth the artifact-only view (method a) cannot distinguish (f1.py's normalizer_meta omits reason_summary)"
  no_src_edits: true

task3_continuous_test_smoke:
  command: "cd /tmp/wt-f1-stt-ocr-integration/apps/ai-server && .venv/bin/python -m src.continuous_test --persona VP-001"
  stt_ocr_off_note: "continuous_test.py has no --stt/--ocr CLI flags; its F1 stage calls f1._run_simulation(persona_id, max_turns, followup_from=None) with audio_inputs/ocr_documents/ocr_hints all left at their None defaults (code-read confirmed, src/f1.py:1794-1801 signature, src/continuous_test.py:187 call site) — this IS the STT/OCR-off text configuration by construction, no flag needed"
  n: 1   # single VP smoke per brief

task4_stt_live_smoke:
  status: DECLARED_UNTESTED
  reason: "SKT_A_X_API_KEY is empty in apps/ai-server/.env (grep-confirmed, name-only check, value never read/printed) — qa W2 finding, HANDOFF-carried. Vendor endpoint not provisioned. No call attempted, no provisioning done, per brief constraint."

model:
  primary_vendor: upstage
  chat_model: solar-pro3
  base_url: https://api.upstage.ai/v1
  secondary_vendor: lg_k_exaone (patient simulator, PatientLLM)

hardware: CPU-only orchestration host; inference via Upstage/LG K-EXAONE hosted APIs (same as EXP-002/003/012)

no_pushes_no_commits: true
no_code_edits_to_src: true
