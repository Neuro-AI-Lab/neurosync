# orchestrator memory

Cross-session lessons for the orchestrator role. One fact per bullet, dated. Prune entries that turn out wrong.

- [Git-state volatility + gitignored root docs + worktree pipeline](git-state-volatility.md) — re-verify remote state per dispatch; root docs gitignored; /tmp worktree wave-pipeline; DR-append PR collisions; PROMPTS_BASE_DIR fallback trap (BUG-021); blind-validation protocol + surviving-doc transcription gate (ADR-023). (2026-07-10)
- [Gate verdicts: externalize before dependent dispatch](gate-verdicts-externalize-first.md) — append qa/critic/CVR verdicts to discussion.md the moment they arrive; specialists verify the doc, not your context; bit twice in W28-Q. (2026-07-11)
