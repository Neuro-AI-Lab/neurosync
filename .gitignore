# macOS
.DS_Store
**/.DS_Store

# Editors / IDEs
.vscode/
.idea/
*.swp
*.swo

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.tox/
htmlcov/
.coverage
.coverage.*

# Node / JS
node_modules/
.next/
dist/
build/
.expo/
.expo-shared/
*.log
.npm/
.pnpm-store/
.yarn/

# Mobile
ios/Pods/
android/.gradle/
android/build/
android/app/build/
*.keystore

# Env / Secrets — NEVER commit
.env
.env.*
!.env.example
*.pem
*.key

# Local data
*.sqlite
*.db
*.dump
audio/
uploads/
tmp/

# AI artifacts (local model caches, eval outputs)
.langsmith/
# Ignore ML model weight directories at repo root or under AI server,
# but NOT source package paths like `apps/api/src/models/`.
/models/
apps/ai-server/models/
*.bin
*.safetensors
*.gguf

# OCR/PDF working dirs
/tmp/pdf_pages/
ocr-output/

# CI cache
.github/cache/

# Turborepo cache
.turbo/

# Claude Code
.claude/
CLAUDE.md
discussion.md
error.md
result.md
version.md



# TS incremental build info
*.tsbuildinfo

# Source materials (large originals, stored separately)
references/

# Local datasets (AI-Hub 심리상담/QA 원본 — 대용량, 절대 커밋 금지)
data/

# Local-only runtime/working artifacts (Claude Code 세션 산출물)
tasks/
.active-agents
.active-agents.*
.failure-tracker.jsonl
**/.failure-tracker.jsonl

# Local compose override (LAN 노출용 — 공유 dev 기본값 변경 방지)
infra/deploy/docker-compose.lan.yml
