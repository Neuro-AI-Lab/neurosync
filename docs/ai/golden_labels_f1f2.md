# F1–F2 golden labels and reveal-partition spec

> **Source:** condensed from `DATASET-003`/`DATASET-004` (VP-001~004 golden domain labels, 2026-07-07/08) and `DATASET-003-ext`/`DATASET-004-ext` (VP-010/011/012 golden domain labels + the VP-001/VP-003 reveal-partition spec, 2026-07-11), all `data`-authored and `critic`-reviewed (`REV-007`, `REV-026`). This file is the verbatim-faithful, complete, standalone rendering of that content for W7/W8 scorers — it does not require reading any other document to be applied.
> **Created:** 2026-07-11 | **Author:** writer, on orchestrator dispatch (W6-end grand transcription pass).
> **Updated:** 2026-07-15 | **Author:** `data`, applying `CVR-029` Finding 5's VP-004 golden-label ruling (session-level `panic` addition, both-accepted not blanket-relabel) — see VP-004's card and per-session acceptance table below for the full session-by-session classification and its evidence.

## Access discipline (read first, binding)

**This file is raw validation data on the blind-state survival whitelist, referenced ONLY in scorer/audit briefs — never in system-under-test inputs, never in patient-simulator prompts, never readable by any clinical agent path.** The domain-set values, quotes, and SC-4/SC-5/SC-8 targets below are answer keys. Nothing in this file may be interpolated into any `DialogueAgent`/`ClinicalSlotAgent`/`SafetyClassifierAgent`/`DomainInferenceAgent`/Policy-B-judge prompt or input schema, and nothing in this file may be read by `tests/simulation/patient_llm.py` (the patient-simulator loader) or any persona-loading call site. A scorer/critic/data-agent brief may cite this file; a system-under-test-facing or patient-simulator-facing brief may not.

---

## Part A — golden domain labels (VP-001 through VP-012)

### Domain enum and mapping principle

8-value enum (source: `docs/ai/PRD_task1.md:474`): `anxiety | depression | alcohol | substance | trauma | sleep | psychosis | other`.

**Base rule (all 7 VPs).** A domain is included in a VP's golden set only if the persona's own **chief complaint (주호소) or stated diagnosis** names it — not merely because a secondary scale score (PHQ-9/GAD-7) crosses a severity-band label, and not because a risk-assessment or coping-behavior field mentions it. Applied uniformly so no VP gets an ad hoc inclusion/exclusion decision; every exclusion below is flagged explicitly, not silently decided.

**Extension for VP-010/011/012 (disclosed interpretive extension, `DATASET-003-ext`).** VP-010's minimization tiers and VP-011's somatic masking mean the SURFACE-level (Tier-1) chief complaint text is, by design, clinically uninformative or actively misleading (VP-010 Tier-1: "그냥 요즘 좀 피곤하고 스트레스 받아서 왔어요"; VP-011's surface 주호소: "두통, 소화불량, 만성피로" — zero psychiatric-domain words). Applying the base rule to the SURFACE layer for these two would license nothing for either persona. The license source for VP-010/011/012 is instead the **ground-truth** clinical-profile/expected-scale-score/expected-slot-value content (the persona's authored TRUE clinical picture, not the minimized/masked surface dialogue) — each file's own label-grounding note explicitly designs for this reading. The underlying chief-complaint-or-diagnosis discipline still governs which ground-truth content counts; only the *layer* read (ground truth vs. surface) differs for these three.

### Golden domain sets — all 7 personas

| VP | Golden domain set | License | Source (persona file) |
|:--|:--|:--|:--|
| VP-001 | `{anxiety, sleep}` | Chief complaint names both jointly and with equal weight | `VP-001_first_visit_mild.md` §2 주호소, line 28: "최근 3주간 지속되는 불안감과 수면 문제" |
| VP-002 | `{depression}` | Explicit revisit for a diagnosed mild depressive episode | `VP-002_revisit_mild.md` §2 주호소 line 29 / 이력 및 약물 line 48: "6주 전 경도 우울로 첫 방문 후 재진" / "경도 우울 에피소드 (의사 소견)" |
| VP-003 | `{depression}` (SI content is risk signal, not a domain — see rationale) | Chief complaint names depression; the co-located SI language is excluded as risk, not domain, per the 8-value enum having no suicide/risk member | `VP-003_first_visit_severe.md` §2 주호소, line 28: "극심한 우울, 자살 사고, 절망감" |
| VP-004 | Persona base `{depression, anxiety}`; **session-level `panic` addition** (both-accepted, not a blanket relabel) for panic-attack-specific sessions — see per-session table below | Chief complaint names an existing depressive episode plus newly-onset panic attacks; `panic` is now a valid enum value (`BUG-031` fix, `DomainName` 8→9 values) and is added as an accepted first-class label per session where the persona's own transcript shows an episodic-surge/acute-somatic panic presentation, additively alongside (not replacing) `anxiety` | `VP-004_revisit_severe.md` §2 주호소 line 28 / 이력 및 약물 line 58: "중등도 우울로 치료 시작... 공황 발작이 새로 발생" / "이전 진단: 주요우울장애 의심"; session-level ruling per `CVR-029` Finding 5, applied by `data` 2026-07-15 |
| VP-010 | `{depression, anxiety}` (comorbid) | Ground truth: PHQ-9~13 (moderate) + GAD-7~11 (moderate), both independently documented; ground-truth `chief_complaint` **slot value** (§4 — distinct from the minimized Tier-1 surface line) names both jointly | `VP-010_first_visit_minimizing.md:113`: "중등도 우울감과 불안, 흥미 저하" |
| VP-011 | `{depression}` only — `{sleep}` NOT added | Underlying-diagnosis anchor (§2, ground truth, EXEMPT from the reveal-partition gate — see Part B) | `VP-011_first_visit_somatic.md:40`: "중등도 주요우울장애 (Major Depressive Disorder, moderate)" |
| VP-012 | `{alcohol, depression}` (comorbid, diagnosis-level) | `alcohol`: surface and ground truth coincide (turn-1 presenting concern); `depression`: dedicated diagnosis-level comorbidity section + PHQ-9~10 | `VP-012_first_visit_alcohol.md:29-33` (주호소), `:50-58` (§2 comorbidity note), `:96` (§3 PHQ-9) |

### Per-VP rationale

**VP-001 = `{anxiety, sleep}`.** Chief complaint names both domains explicitly and with equal weight ("불안감과 수면 문제"), corroborated by GAD-7 ~8 "Mild anxiety" (§3). **Excluded: depression.** PHQ-9 ~7 carries the scale's own severity-band label "Mild depression" (§3), but the persona's symptom table states mood as "가끔 우울, 대체로 괜찮은 편" (line 38) — occasional and incidental, never named in the chief complaint or anywhere else as a presenting concern. Flagged as the closest judgment call among VP-001~004.

**VP-002 = `{depression}`.** Single domain: chief complaint is an explicit revisit for a diagnosed "경도 우울 에피소드" on Escitalopram, improving. **Excluded: anxiety.** GAD-7 ~5 "Mild anxiety" is present in the scale table but no diagnosis, chief complaint, or clinical-judgment line anywhere in the file names anxiety as a domain.

**VP-003 = `{depression}`.** Chief complaint: "극심한 우울, 자살 사고, 절망감." The suicidal-ideation content in this same phrase and the persona's 위험 평가 section ("자살 사고: 수동적 자살 사고 (매일)", §2) are **risk signal, not a domain value** — the 8-value enum has no suicide/risk member. Golden set for scoring: `{depression}` only. **Excluded, flagged as the most contestable judgment call in the full set: alcohol.** The persona documents daily heavy drinking ("거의 매일 소주 1병", up from "주 1-2회" pre-onset) as a coping behavior under 이력 및 약물/위험요인, not as a chief complaint or diagnosis — excluded under the same principle that excludes VP-001's incidental depression note.
**Binding wording condition (`REV-007`).** Any report must not characterize an F2 run that surfaces `alcohol` for VP-003 (alone or alongside `depression`) as a false positive, hallucination, or noise — it is a clinically well-grounded finding under this project's evidence-whitelist standard, excluded from the *scoring* golden set only by the chief-complaint/diagnosis-only convention, not by clinical implausibility. If any VP-003 run returns `{alcohol}` alone (no `depression`), that specific run must be called out by name in the descriptive report, not silently folded into an aggregate count.

**VP-004 = persona base `{depression, anxiety}`, session-level `panic` addition (updated 2026-07-15, `data`, per `CVR-029` Finding 5).** Chief complaint documents two co-occurring domains: an existing depressive episode ("이전 진단: 주요우울장애 의심"; "중등도 우울로 치료 시작") and newly-onset panic attacks with an ER visit one month prior ("공황 발작이 새로 발생하여 1개월 전 응급실 방문"). No `trauma` domain added despite the ER visit — the event is presented throughout as a panic-disorder episode ("공황 발작으로 진단"), not a discrete traumatic-stressor narrative (no re-experiencing/avoidance/hyperarousal language independent of the panic symptoms).

**Stale rationale corrected.** The persona-level card previously mapped panic → `anxiety` with the stated reason "no `panic` value in the enum." **That premise is no longer true**: `BUG-031`'s fix (`error.md`, qa-verified 2026-07-15) added `"panic"` to `DomainName` (8→9 values). `CVR-029` Finding 5 (clinical-validator, 2026-07-15) ruled the enum-gap workaround should not be preserved as a permanent construct-blur in the golden label now that the gap is closed: panic attacks and generalized anxiety are related but clinically distinguishable presentations (episodic/acute surge vs. persistent/diffuse worry), and collapsing them at the golden-label layer would make VP-004's dataset unable to validate whether the system correctly distinguishes the two once `DOMAIN_TO_SCALE`/`panic` is wired to a live call site. Ruling adopted **additively, session-by-session, not as a blanket persona-level relabel**: `panic` is added as an accepted first-class label for sessions whose own transcript shows a panic-attack-specific presentation, alongside (not instead of) the existing `anxiety` label; sessions without session-specific panic content keep the persona base `{depression, anxiety}` unchanged.

### Per-session acceptance table (VP-004, `EXP-025` baseline + `EXP-026` extension rerun)

**Classification method and blind-authorship order (disclosed).** Before this classification pass, `CVR-029`/`BUG-031`/`error.md` and `result.md`'s EXP-026 VP-004 per-session domain table were already read as part of ordinary mission context-gathering (they are this task's own cited linked entries — full blindness to their existence was not achievable). The classification itself was then performed independently: each of the 20 sessions' `docs/ai/simulation_results/VP-004/*_conversation.json` transcripts (full `turns[].patient_message` text, not just `final_slots`) was read and keyword/content-scanned for panic-attack-specific markers (episodic surge — "심장", "숨이"/"숨쉬", "죽을 것 같다"/"죽는 줄", "응급실", "발작", "헉헉", "벽에 기대") **before** re-opening any `domain_inference.json` artifact for that session. Only after all 20 sessions were independently classified from transcript content alone were the `domain_inference.json` `domain_candidates`/`validation_errors` fields re-opened, to fill the "F2 emitted panic literal" column below (a factual record of F2's raw output, not an input to the classification decision).

| Run | S | Transcript panic-marker hits (keyword count) | Content classification | Accepted golden set (this session) | F2 emitted `"panic"` literal (post-hoc cross-check) |
|:--|--:|:--|:--|:--|:--|
| EXP-025 | 1 | 심장×6, 숨이×6, 공황×6, 죽는줄×2 | panic-attack-specific (acute episode turn 0: "심장이 터질 것처럼 뛰고 숨이 안 쉬어져요. 진짜 죽는 줄 알았거든요") | `{depression, anxiety, panic}` | no |
| EXP-025 | 2 | 공황×2, 심장×1, 숨이×1 | panic-attack-specific (CC/HPI: 아침 기상 시 급성 심계항진·호흡곤란) | `{depression, anxiety, panic}` | no |
| EXP-025 | 3 | 심장×2, 숨이×2 | panic-attack-specific (아침마다 반복되는 급성 신체 증상) | `{depression, anxiety, panic}` | no |
| EXP-025 | 4 | 공황×2, 심장×11, 숨이×11 | panic-attack-specific (dense episodic-surge language) | `{depression, anxiety, panic}` | no |
| EXP-025 | 5 | 심장×4, 숨이×6, 응급실×3 | panic-attack-specific (ER-visit content directly present) | `{depression, anxiety, panic}` | **yes** |
| EXP-025 | 6 | 심장×2, 숨이×4, 응급실×2 | panic-attack-specific ("한 달 전에 응급실 갔던 날이랑 비슷한 느낌") | `{depression, anxiety, panic}` | **yes** |
| EXP-025 | 7 | 공황×4, 죽을것×5, 응급실×3, 발작×3, 헉헉×3, 벽에기대×3 | panic-attack-specific, most acute session (passive-SI co-occurring, CC: "공황 발작 시 심장이 터질 것 같고 숨이 안 쉬어져 벽에 기대 헉헉") | `{depression, anxiety, panic}` | **yes** |
| EXP-025 | 8 | 공황×2, 죽을것×2, 응급실×2, 헉헉×2, 벽에기대×2 | panic-attack-specific (same acute cluster as S7, carried) | `{depression, anxiety, panic}` | **yes** |
| EXP-025 | 9 | **0 hits** | **not diagnostic this session** — turns rotate through `medical_history`/`personal_social_history`/`family_history`/`past_psychiatric_history` probes (headache, hand tremor, mother-avoidance guilt, light wine use); no episodic-surge or generalized-diffuse-worry language elicited either. Conservative fallback: keep persona base, do not add `panic` on absent evidence | `{depression, anxiety}` (persona base, unchanged) | no |
| EXP-025 | 10 | 공황×2, 숨이×2, 죽을것×3, 응급실×2, 가슴쿵쾅×1 | panic-attack-specific (turn-9 acute episode: "밤이 되면 가슴이 쿵쾅거리고 숨이 안 쉬어지는 공포가 지속됨") | `{depression, anxiety, panic}` | **yes** |
| EXP-026 | 1 | 심장×1, 숨이×1, 죽을것×1 | panic-attack-specific (single turn, acute: "숨이 안 쉬어지고 심장이 터질 것처럼... 진짜 죽을 것 같아요") | `{depression, anxiety, panic}` | no |
| EXP-026 | 2 | 공황×2, 심장×9, 숨이×9, 죽는줄×9 | panic-attack-specific | `{depression, anxiety, panic}` | no |
| EXP-026 | 3 | 공황×11, 죽을것×11, 응급실×11 | panic-attack-specific | `{depression, anxiety, panic}` | no |
| EXP-026 | 4 | 공황×1, 심장×9, 숨이×9, 죽는줄×9 | panic-attack-specific | `{depression, anxiety, panic}` | no |
| EXP-026 | 5 | 공황×3 | panic-attack-specific, anticipatory form ("공황이 오면 또 어쩌지... 또 공황이 오면 어떡하지") — core anticipatory-panic-anxiety content, not generalized diffuse worry | `{depression, anxiety, panic}` | no |
| EXP-026 | 6 | 공황×3, 응급실×3, 심장×1, 숨이×3 | panic-attack-specific | `{depression, anxiety, panic}` | no |
| EXP-026 | 7 | 심장×2, 숨이×2, 죽을것×1, 죽는줄×1, 응급실×1 | panic-attack-specific ("갑자기 심장이 막 뛰고 숨이 안 쉬어져요... 한 달 전에도 응급실 갔었는데") | `{depression, anxiety, panic}` | no |
| EXP-026 | 8 | 공황×5, 심장×4, 숨이×7, 죽는줄×2, 응급실×1 | panic-attack-specific | `{depression, anxiety, panic}` | no |
| EXP-026 | 9 | 심장×2, 숨이×6, 죽는줄×4, 응급실×2, 발작×4, 벽에기대×2 | panic-attack-specific, dense acute cluster | `{depression, anxiety, panic}` | no |
| EXP-026 | 10 | 심장×1, 숨이×1, 죽을것×1 | panic-attack-specific (final turn acute episode: "숨이 안 쉬어져요. 심장이 터질 것처럼 뛰어요. 진짜 죽을 것 같아요") | `{depression, anxiety, panic}` | no |

**Summary:** 19/20 sessions across both runs are panic-attack-specific by transcript content and get `panic` added (additively) to the accepted golden set; 1/20 (EXP-025 session 9) is a thin, under-elicited session with no session-specific panic or generalized-anxiety content and keeps the unmodified persona base `{depression, anxiety}`. Zero sessions in this cohort are genuinely generalized-anxiety-presenting in a way that would license *excluding* `panic` while still being clinically informative — VP-004's arc (`fluctuating_panic_recurrence`) is designed as panic-specific throughout, so this outcome (19 panic-accepted, 1 abstained-on-thin-evidence, 0 generalized-anxiety-only) is expected given the arc design, not a surprising skew.

**Independent cross-check note.** The post-hoc `domain_inference.json` check (rightmost column) shows F2 itself emitted the literal `"panic"` string in exactly EXP-025 sessions 5, 6, 7, 8, 10 (matching `BUG-031`'s own citation) and in 0/10 EXP-026 sessions (matching `result.md`'s EXP-026 VP-004 sub-run finding — the panic-literal trigger did not stochastically recur that run). The transcript-based classification above is **broader** than F2's own panic-literal emission (19 sessions vs. 5) — this is expected and correct: the golden label reflects the persona's actual clinical presentation each session, not F2's own (stochastic, sometimes schema-violating) output. Session 9 is the one case where both the independent transcript read and F2's own output agree there is no panic content that session — a genuine convergence, not circular reasoning, since the transcript classification was completed first.

**VP-010 = `{depression, anxiety}` (comorbid).** Ground truth documents co-equal, comparably severe depression (PHQ-9~13, moderate band 10-14) and anxiety (GAD-7~11, moderate band 10-14), both independently documented with their own onset/trigger/duration, and the persona's own ground-truth `chief_complaint` **slot value** (§4 — distinct from the minimized Tier-1 surface line) names both jointly: "중등도 우울감과 불안, 흥미 저하." A jointly-named, co-equal-severity pair licenses a comorbid label, the same joint-naming reasoning underlying VP-004's persona-base `{depression, anxiety}` set above (VP-004's panic→anxiety enum-gap mapping has since been superseded by the session-level `panic`-addition ruling; see VP-004's updated card).

**VP-011 = `{depression}` only — `{sleep}` NOT added, judgment call.** Licensed via the underlying-diagnosis anchor (§2, ground truth) — EXEMPT from the reveal-partition gate (Part B). The anchor's own dedicated table row — "Underlying diagnosis: 중등도 주요우울장애" — names only depression; sleep appears only in the adjacent "근거" (evidentiary) row alongside three other symptom items ("뚜렷한 흥미/즐거움 감소, 수면장애, 식욕/체중 변화, 에너지 저하") — a symptom-cluster item supporting ONE diagnosis, structurally analogous to how PHQ-9's own sleep item is one of nine criteria comprising a single depression severity score, not an independently, separately-named presenting domain the way VP-001's chief complaint literally coordinates two grammatically co-equal nouns ("불안감과 수면 문제"). Including `sleep` on the strength of a diagnosis-supporting-symptom mention would break the same exclusion discipline that excluded VP-001's incidental depression note and VP-002's incidental GAD-7 elevation. Golden set: `{depression}` only.
**Reveal-partition status (verbatim, `VP-011_first_visit_somatic.md` §8, ratified `CVR-004`): EXEMPT.** "VP-011 runs only under SC-12 (dialogue-only, first-visit, no modality cross, no follow-up session scheduled anywhere in the program). The reveal-partition gate (which persona facts are gated to session 2+) governs facts intentionally withheld until a *later* session of a persona that *has* one. VP-011 has no session 2 to gate anything toward — applying the gate to her would make the underlying-diagnosis anchor (§2 '기저 진단') permanently unreachable. The anchor is therefore explicitly **exempt** and is licensed to surface within session 1."

**VP-012 = `{alcohol, depression}` (comorbid, diagnosis-level).** `{alcohol}`: licensed trivially — both surface and ground truth coincide (turn-1 stated presenting concern, §2/§5/§8 all confirm), no reveal-partition question applies (VP-012, like VP-011, has no scheduled follow-up session; alcohol content is present from turn 1). `{depression}`: the persona file carries a dedicated ground-truth section explicitly titled "동반 상태 (comorbidity note -- **진단 수준**의 우울 증상)" (`:50`) — the file's own author chose the words "진단 수준" (diagnosis-level) deliberately; §8 states: "The PHQ-9 ground truth (§3, ~10, item 9 = 0) is the primary basis for licensing `{depression}` as a diagnosis-level comorbidity." PHQ-9~10 sits at the lower edge of the moderate band (10-14).
**Binding overlap caveat, carried from VP-012 §8 verbatim.** The `sleep_quality` slot is jointly explained by alcohol use and low mood in this persona's design (§2 comorbidity note: "음주 자체와 기분 저하가 함께 작용하는 순환적 관계"). This is **not** treated as independent textual evidence for both `{alcohol}` and `{depression}` — `{depression}`'s primary basis is the PHQ-9 ground-truth score plus the dedicated diagnosis-level comorbidity-section text (mood/guilt content, §5), not the shared sleep slot.

### Scoring rule (pre-registered before any live run, unchanged across VP-001~004 and VP-010~012)

- **Top-1 hit:** the model's rank-1 domain (`domain_candidates[0].domain`) ∈ the VP's golden set. Binary.
- **Top-3 hit:** ANY element of the VP's golden set appears anywhere among the model's returned `domain_candidates` (capped at ≤3). Binary, existential match (OR across golden-set elements).
- **VP-004 cardinality note (added 2026-07-15, `data`).** VP-004's golden set is session-dependent following the per-session table above: `{depression, anxiety, panic}` (3-element) for 19/20 EXP-025+EXP-026 sessions, `{depression, anxiety}` (2-element, persona base) for the 1 thin/under-elicited exception (EXP-025 session 9). Any future scorer must read VP-004's golden set per-session-index from the table above, not use a single fixed set for all VP-004 rows — and any report comparing VP-004's hit rate against other VPs' must disclose this within-VP cardinality variation on top of the existing cross-VP cardinality-asymmetry caveat below.
- **No partial credit.** A 2-element golden set (VP-001, VP-004, VP-010, VP-012) scores the same top-1/top-3 hit as a 1-element set (VP-002, VP-003, VP-011) if any one element matches — no fractional credit, no penalty for surfacing only one of two valid domains.
- **0-candidate case (amendment, `REV-007`):** `domain_candidates=[]` scores as a miss (0) for both top-1 and top-3 for that VP/run — the agent's own designed degrade behavior (no silent fabrication) produces this as a live, expected outcome, not a hypothetical edge case.
- **Reporting format:** raw counts only, no percentage/rate claims (e.g. "top-1 X/N") until ≥12 cumulative observations exist.
- **Department scoring:** descriptive only, not scored, for all 7 personas. All are framed throughout as psychiatric pre-consultation intake (VP-001/003/010/011/012: first-visit, no prior prescriber; VP-002/004: explicit prior "정신건강의학과" prescriber).
- **Golden-set-cardinality asymmetry caveat (new finding, `REV-026`, binding wording discipline).** A 2-element golden set (VP-001, VP-004, VP-010, VP-012) has a structurally higher a-priori top-1/top-3 hit probability than a 1-element set (VP-002, VP-003, VP-011), independent of system quality, under the existential-OR scoring rule above. This is not a defect in the labeling — every comorbid call above is independently well-grounded in ground-truth text, and the direction is not self-serving (e.g. VP-011's single-domain call makes scoring *harder*, not easier). **Any W8 report comparing raw hit rates across 2-element-golden-set VPs vs. 1-element-golden-set VPs without disclosing this cardinality asymmetry risks an apples-to-oranges read.**

### Golden-label discipline — circularity caveat (binding on all 7 cards)

Restated verbatim, binding on any correctness claim drawn from any label in this file: "structural circularity between the golden label and F2's own input features... single-pass authorship; independent-source authorship is not available for this batch either." In full: the golden domain label and F2's eventual input text (F1's `final_slots`, produced by an interactive session between the real `DialogueAgent` and an LLM patient-simulator whose system prompt is drawn directly from the SAME persona file's clinical-profile content these labels are also derived from) are **not independently sourced** — both trace back to one paragraph in one file, authored in one pass, with no held-out or blinded generation step between them. **High fabrication-0/top-1/top-3 accuracy against any VP's golden label in this file cannot be read as evidence the pipeline generalizes beyond this closed, self-consistent persona set.** This caveat is binding on all 7 VPs alike (VP-001~004 per `DATASET-004`; VP-010~012 per `DATASET-003-ext`, re-incurred, not resolved by being a newer persona set).

### Hash pins (SHA256, `sha256sum`)

| Persona file | SHA256 |
|:--|:--|
| `docs/ai/personas/VP-001_first_visit_mild.md` | `1505137e405ca1c3d56299fcc8e9cd4fe2b2223edfe3673ec192490c198c8e4d` |
| `docs/ai/personas/VP-002_revisit_mild.md` | `7437d2df61e36cb279377137251caa58afda69201b59cd917f4603abc60102be` |
| `docs/ai/personas/VP-003_first_visit_severe.md` | `ceca9a55b767c8715175c6bd868e5b52b6abd09911b22af51bb9c3af3a461788` |
| `docs/ai/personas/VP-004_revisit_severe.md` | `c7e3daa2d54fac45f398bc4a4f8277ac2905db4c61008d9b8dc4ab6faea9901b` |
| `docs/ai/personas/VP-010_first_visit_minimizing.md` | `aa5a2a1bd891f2473cd1de3ee1576afd18a3140ce54c89b900879429f8c36ea4` |
| `docs/ai/personas/VP-011_first_visit_somatic.md` | `911d3f9a2a8707dbff0fd9f6dfe60223b9f1688d49ba81f451ce690861c16e2d` |
| `docs/ai/personas/VP-012_first_visit_alcohol.md` | `950ac1fcec1ed04eceeecaaccb29bc531511c71957da07a677ff7c41726dbb96` |

---

## Part B — golden reveal-partition spec (SC-4/SC-5/SC-8 chains)

Which persona facts are gated to session 2+ for the live multi-session `continuous_test.py` chains, authored blind, before any multi-session run.

### Chain-eligibility finding (binding on this spec's design)

Not all 4 of VP-001..004 are structurally suitable for `continuous_test.py`'s live `--persona VP-00X --sessions N` chaining mechanism, which reuses ONE persona's ONE static system prompt across every session in the chain. `f1.py:2197-2198` — the SYSTEM always treats `session_index=1` (no `--followup-from`) as `is_revisit=False`, regardless of the persona file's own `visit_type` metadata (independently confirmed structural, not a narrow edge case, by tracing `continuous_test.py:399-408`: `followup_from` is unset for every session-index=1 in any chain attempt). But VP-002's and VP-004's static Patient LLM simulation prompts hardcode revisit framing **unconditionally**: VP-002 — "오늘은 약 복용 후 경과를 확인받기 위한 재진 사전문진입니다" (`VP-002_revisit_mild.md:322`); VP-004 — "오늘은 재진 사전문진입니다" (`VP-004_revisit_severe.md:385`). Chaining VP-002 or VP-004 fresh (session 1, no `--followup-from`) would therefore produce an internally contradictory session: the SYSTEM's own greeting/dialogue framing treats it as first-visit, while the PATIENT simulator insists this is already a revisit. VP-001 and VP-003, by contrast, both state unconditional first-visit framing ("정신과를 방문한 적은 없습니다") with no revisit claim anywhere in their static prompts, and chain cleanly.

**This spec designates VP-001 and VP-003 as the SC-4/5/8 chain-relevant personas; VP-002/VP-004 are excluded from live `continuous_test.py` chaining by this structural finding** (RATIFIED, `REV-026` ruling 2 — independently re-verified against the same cited lines, confirming the conflict is structural for every possible VP-002/VP-004 chain attempt, not a narrow edge case).

**Persona-diversity scope-limit wording discipline (binding, `REV-026` ruling 2).** Any W7b/W8 report characterizing SC-4/SC-5/SC-8 multi-session results as validating induction/re-probe/revision behavior "across the persona set" overclaims — the multi-session battery exercises only VP-001 (mild, first-visit) and VP-003 (severe, first-visit), two of seven personas, both first-visit. **State this explicitly wherever SC-4/5/8 results are reported.**

### Reveal-partition design, per SC class

All three targets are drawn from the 8 canonical `QUESTIONABLE_SLOT_KEYS` (`grounding.py:43-52`): `chief_complaint`, `history_of_present_illness`, `past_psychiatric_history`, `medical_history`, `personal_social_history`, `family_history`, `substance_use_history`, `risk_assessment` — the same slot set `continuous_test.py`'s session ledger computes `missing_slots` against. No persona file is edited to manufacture these gaps — each target is chosen because the *existing*, unedited ground truth and dialogue-pattern design make it a plausible, real induction opportunity.

**SC-4 — VP-001, target = `family_history` (family_psychiatric_history).** VP-001's own §4 slot table gives `family_psychiatric_history` = "특이사항 없음" (negative, low clinical salience, unconnected to the presenting complaint). §5's worked example-utterance table gives concrete lines for chief complaint, sleep, work impact, mood, and SI — **no worked line exists for family history**. Given `max_turns=10` (default) and a genuine first-visit intake needing to cover all 8 canonical slots for VP-001 (no revisit shortcut available), it is plausible for a session-1 run to end before ever asking about family psychiatric history. **Test:** session 2 (given `missing_slots` containing `family_history` in the carried ledger entry) should induce a targeted family-history question; VP-001's persona has no scripted denial or evasion for this topic, so a direct negative answer ("없어요" family-consistent) is the expected, code-permitted completion — the patient LLM must generalize this from persona-consistent context, since no worked line exists.

**SC-5 — VP-003, target = `risk_assessment`.** VP-003's own system prompt (§6): "당신은 '자살 계획'은 없지만, '수동적 자살 사고'가 분명히 있습니다. 이것을 숨기지 마세요. 하지만 먼저 꺼내지는 않고, 기분에 대해 물어볼 때 자연스럽게 드러내세요" — combined with §5's documented avoidant/low-cooperation communication style ("협조도: 낮음~중간... 회피: 구체적 질문에 회피적, '몰라요', '그냥요' 빈번... 침묵: 어려운 질문 후 '...' 또는 긴 답변 지연"), a session-1 run that is turn-constrained or deflected could plausibly end without a full, accurate passive-SI capture.
**Disclosed structural complication (code-verified).** `f1.py:441-442` states `risk_assessment` is **never carried** across sessions by design — every session re-grounds risk from scratch via the Safety Probe/SI-screen — and `f1.py:1650-1656`'s termination gate additionally guarantees a session **cannot** end via the "slot-completion" early-exit path while `risk_assessment` is ungrounded (`if not other_missing and risk_grounded and turn >= 3: break`). This means SC-5 does **not** test whether a carry-channel bug improperly withholds risk information (the carry channel already, correctly, never carries it) — it tests whether session 2's own independent Safety Probe/SI-screen (which fires fresh, exactly as session 1's did) succeeds in fully grounding the passive-SI content when session 1, for a turn-constrained and avoidant persona, plausibly ended at `max_turns=10` with `risk_assessment` still ungrounded.
**"Session-1-DENIED" vs. this project's actual available personas — disclosed distinction.** SC-5's class name says "risk re-probe of a session-1-**denied** risk item." VP-003's own design is not a denial-then-reversal script — the persona never actively denies passive SI if properly asked; it simply does not volunteer it unprompted. No persona among VP-001..004 or VP-010..012 is designed with a genuine active-denial-then-later-reveal risk item. VP-003's under-elicitation-not-denial pattern is therefore the closest available analog in this project's current persona roster and is adopted as SC-5's test vehicle, with this distinction disclosed rather than glossed over.
**Pre-registered scoring fallback (required because of the above).** If session 1 already fully and accurately grounds `risk_assessment` (a legitimate, even desirable, outcome — this is what a well-functioning Safety Probe *should* do), the SC-5 cell must be flagged **inconclusive/excluded for the induction claim** (nothing to induce), not silently scored as a pass or reported as evidence of successful re-probing. **Mechanical trigger (adopted, `REV-026` ruling 2 recommendation):** cite `safety_matrix.py`'s `_risk_assessment_grounded(result)` function (`has_written and has_slot`, derived from `result.probe_events`/`result.final_slots`) as the fallback's concrete, mechanical trigger at scoring time — not a subjective prose judgment call.

**SC-8 — VP-001, target = `substance_use_history` — scope disclosed as overwrite-MECHANICS only, no content-revision authored.** This spec does **not** author a clinical-truth change for any VP-001..004 slot for session 2. VP-001's `substance_use_history` ("주 1-2회 소량 (맥주 1-2캔)") is a stable, already-answerable fact with no authored change over a short simulated interval. **Test, reframed honestly:** SC-8 with this target tests whether the system's slot-storage OVERWRITE mechanics correctly handle a same-truth, freshly-elicited session-2 re-answer for an already-filled canonical slot (replacing, not duplicating or ignoring, the session-1 carried value) — not whether a genuine content revision is detected, since none is authored here. A genuine content-revision test (e.g. "patient reports reduced drinking between sessions") would require either a new authored fact in a future persona revision or reuse of VP-002's/VP-004's own pre-authored improvement trajectory under a different, non-`continuous_test.py`-chained mechanism (since those two are chain-ineligible per the finding above) — open design question, not resolved by this spec.

### Circularity caveat (binding on every session-2+ correctness claim from SC-4/5/8)

Restated per the same discipline as Part A: the golden expectation for what session 2 *should* elicit and F1's actual session-2 input (the SAME persona file's ground truth, re-read by the SAME patient-simulator prompt) are **not independently sourced** — both trace back to one paragraph in one file, authored in one pass. A session-2 induction "success" cannot be read as evidence the system would perform equally well against an independently-authored persona whose designer did not also author the scoring target. **This restatement is binding on any report characterizing SC-4/SC-5/SC-8 results as validating multi-session induction/re-probe/revision behavior in general** — it validates behavior against this specific, single-pass-authored persona-and-target pairing only.

### Disjointness from canary content

None of this spec's three designated targets (`family_history`="특이사항 없음" for VP-001; `risk_assessment` passive-SI phrases — "사라지고 싶다"/"죽으면 편할 것 같다"/"살아서 뭐하나" — for VP-003; `substance_use_history` for VP-001) share any content, phrasing, or invented proper noun with the 21-canary infrastructure (customer-service/product/audit-code framing, entirely unrelated topics). The two audit mechanisms remain independently attributable.
