# F1–F2 continuous-scenario validation — workflow checklist

> **Purpose:** essence-only, at-a-glance status of the whole program — every plan-finalization step and every execution wave, one row each, with its owning gate(s) and status. Populated from `docs/ai/validation_plan_f1f2_continuous.md` v1.2.
> **Status symbols:** ☑ done (with evidence in the plan doc or `discussion.md`) · ☐ not done (pending or not yet started).
> **Companion docs:** the plan doc (full detail) and `docs/ai/workflow_results_f1f2.md` (per-workflow results log, appended as waves complete).
> **Last updated:** 2026-07-12c, **combined 3-fix cycle CODE-COMPLETE** (`PLAN-2026-W28-U`, post-battery, user-directed, scope reduced mid-mission to fixes-only) — Fix 1 (`BUG-036` empathy-repetition-guard dedup) + Fix 3 (`BUG-037` DialogueAgent output-isolation guard) landed atomically, then Fix 2 (`ADR-030` Option C retry-budget-exhaustion safe degrade, incl. the CF1 follow-up) on top (commit `5baefb9` code+tests, `e833937` design/review docs); qa **GATE:PASS ×2** (combined gate + CF1 micro-gate, suite 1236 passed + 2 skipped); `CVR-013`+`CVR-014` adequate-with-findings (Option C picked; CF1 found and fixed same cycle) ∥ `REV-034`+`REV-035` non-blocking-with-conditions (contamination-exclusion conditions satisfied; pre-battery prerequisites grow 3→4) — never merged. **Every status is code-complete, offline-gated, NOT live-verified.** Per the user's scope-change directive, the mission's own live re-validation battery (SM-01..08b + naturalness probes + an SC-5-style crisis-adjacent chain, ~18-22 calls) is CUT and DEFERRED into the upcoming F1–F5 total validation as absorbable cells, judged against the UNCHANGED pre-registered bars; `BUG-036`/`BUG-037` status: **fixed-pending-live-verification**, neither resolved nor closed. See the `combined-fixcycle` row below. Prior, same day: **BUG-030 iteration-2 + BUG-035 companion mission COMPLETE** (`PLAN-2026-W28-T`, post-battery, user-directed) — near-duplicate empathy-clause retry guard + crisis-adjacent presence check landed (`d68c8a2`), qa **GATE:PASS**; `EXP-018` re-validation: repetition bar (criterion A) **FAILS** as a pre-registered bar (guard-dedup defect `BUG-036` confirmed live), presence bar (criterion B) **PASSES** in the scored sample (not characterized as resolved); `CVR-012` BUG-035 adequate-with-findings / BUG-030 inadequate-blocking; critic `REV-033` **blocking** — mission stop-rule fired, no iteration-3 without user word. Two new bugs filed (`BUG-036`, `BUG-037`). Prior, same day: **BUG-030 fix iteration-1 COMPLETE** (`PLAN-2026-W28-S`) — see the `BUG-030-fix`/`BUG-030-iter2` rows below for full detail. Prior, 2026-07-11: **W7b main matrix + W8 adjudication COMPLETE** — SC-1 through SC-9 executed blind (SC-6/SC-10 remain `SKIPPED-awaiting-user-material`); qa **GATE:PASS** (`BUG-030`/`BUG-031` filed); `CVR-009` adequate-with-findings (MET-2 induction INADEQUATE, VP-010 MPD FAIL, SC-5 item V FAIL, RAG face-validity FLAGGED ≥12/14); critic `REV-030` **non-blocking-with-required-corrections** — RAG A/B adjudication rule applied, **Policy A adopted** (Criterion 1, zero-tolerance safety-dominant win — 0 vs. 3 risk-worded queries shipped in the paired set); MET-3 disclosure-gated composite **17/22 top-1, 18/22 top-3** (SC-1/SC-12/SC-3 reported as 3 separate sub-rates, corrected — the 2 Pydantic-`[]` SC-1 runs scored as MISSES, not excluded). 10 new findings filed (`ISS-F2V-013`..`ISS-F2V-022`, `BUG-030`..`BUG-034`, `VAL-014`/`VAL-015`) plus the clinical-validator weak-point register (8 items). No 인증/통과/certified/validated/deployment-ready wording licensed anywhere in this program (`docs/ai/workflow_results_f1f2.md` `w7b-main-matrix` + `w8-adjudication-disposition`, `docs/ai/workflow_discussion_f1f2.md` `ISS-F2V-013`..`022` + weak-point register). Prior: **W7a COMPLETE** — W6-end transcription-verification confirmed satisfied and the W6→W7 blind-state activation applied (commit `ec7c9bf`, `VER-002` version transition); W7a micro-batteries executed blind: SC-13 clean but first-visit-code-path-only (7/7 F1→F2 chains, 0/21 `AVC-01` canary hits), SC-14 clean (6/6 turn-0 greetings, diversity confined to disclaimer wording), SC-15 POSITIVE `AVC-05` finding (3/4 reps, contained by `grounding.py`, 0 hits shipped — an open containment-coverage gap, `ISS-F2V-012`). qa **GATE:PASS** (`BUG-029` filed); `CVR-007` adequate-with-findings (0 blocking / 2 major / 2 minor); critic `REV-028` ruled `AVC-05` **BLOCKING**, overridden by `ADR-026` — W7b proceeds under that override plus a per-run echo-watch (`docs/ai/workflow_results_f1f2.md` `w7a-micro-batteries`, `docs/ai/workflow_discussion_f1f2.md` `ISS-F2V-012`). Prior: **W6 COMPLETE, ALL W6 GATES GREEN** — personas VP-010/011/012 authored and `CVR-004`-cleared; `CVR-005` pre-registered the six clinical assessment instruments (transcribed, plan Appendix E); AUD ontology `alcohol-use-disorder` `CVR-006`-signed-off and DB-loaded (`1f47c54`, `rag.disease` 26→27, disclosed); golden labels (`DATASET-003-ext`) + reveal-partition spec (`DATASET-004-ext`) + 21 canaries with live zero-hit 63/63 (`DATASET-006`) all data-authored (`cceb8f6`) and `REV-026`-approved. W5 CLOSED — disease→questionnaire mapping shipped in two commits, both qa **GATE:PASS**: `9506650a` (10-classification table + `recommended_questionnaire` field, suite 1034+2) and `113acdf` (W5 addendum: `CLASSIFICATION_TO_CAVEAT` table + `recommendation_caveat` field + disclaimer/render fixes, suite 1063+2). `CVR-003` verdict **adequate-with-findings, 0 blocking** — "may ship into the battery"; Finding 1 (mood→PHQ-9 mania blind spot) and Finding 4 (disclaimer scope) fixed and gated that wave; Finding 5 (no minimum-score/margin gate) **DEFERRED as a disclosed limitation**, tracked as `ISS-F2V-010` (critic, owned at W8). `BUG-028` filed (major, open — live repetition-guard truncation found in the `EXP-015` cert run, new mechanism distinct from `BUG-025`); `ISS-F2V-009` investigation **COMPLETE** — disposition is a pre-registered W7 truncation-rate protocol, transcribed into the plan doc's Appendix D; W7b PROCEEDS under that protocol. (`discussion.md` `PLAN-2026-W28-Q`).

## Plan-finalization steps (`PLAN-2026-W28-P`)

| Step | Essence | Owning gate | Status |
|:--|:--|:--|:--|
| 1a | Static info-flow fact base across all clinical-agent input channels; found `BUG-022` | qa | ☑ |
| 1b | 21-class validity-violation audit-track design (`AVC-01..21`), canary design, greeting-autonomy + simulator-fidelity audit structure | brainstorm | ☑ |
| 1c | 3 new personas (VP-010/011/012) + AUD ontology design + canary data discipline + OCR fixture spec + reset protocol | data | ☑ |
| 2 | Scenario matrix + budget recompute (159 calls, up to 162) + run-reuse table | experiment-tracker | ☑ |
| 3 | Plan v1.2 + this checklist + the results-log scaffold | writer | ☑ |
| 4a | `CVR-002` — scenario coverage re-review with new personas folded; verdict: adequate-with-findings, 0 blocking/5 major/5 minor | clinical-validator | ☑ |
| 4b | `REV-023` — plan v1.2 review: audit-track ratification, reuse legitimacy, dialogue-v3 scope, blindness-manifest pre-registration; verdict: non-blocking, 0 fully-blocking/2 blocking-scoped/5 major/5 minor | critic | ☑ |
| 5 | STATE entry + `handoff.json` + user-facing report | orchestrator | ☐ in progress |

## Execution waves (`docs/ai/validation_plan_f1f2_continuous.md` v1.2, sections 3–7)

Execution is underway. The user directed full W0–W8 execution 2026-07-11 (`discussion.md` `PLAN-2026-W28-Q`); implementation is no longer on hold. W0, W1, W2, and W3 are complete — see their rows below for evidence pointers; W4 onward proceeds per the wave table.

| Wave | Essence | Owning gate(s) | Status |
|:--|:--|:--|:--|
| W0 | Program setup — **complete**: branch `feat/f1f2-program-w29` created off Master `c85b3e1`; `_archive/` scaffold + README manifest committed @`2dd56fd`; `archive_gate.py` hook built + registered + tested in both disarmed/armed states (DISARMED, `ADR-023` Phase 1); PR #42 crisis/safety-path overlap scan verdict — no safety-prompt/`safety_matrix.py`/SM-regression interaction, `f1.py`'s HIRA/Kakao crisis-guidance augmentation confirmed live-inert without provisioned keys (`ADR-024`); fresh CI-mirror suite baseline **812/812 pytest, 0 ruff violations** | filemanager (hook, archive, branch), qa (baseline, PR #42 scan) | ☑ |
| W1 | **Complete** — `BUG-021` fail-fast fix + `EvidenceSourceType` OCR-provenance value; `BUG-011` fix (turn-0 crisis early-return); fixture git mv to `tests/fixtures/` (answer #8a); **standing `retrieve_grounding()` schema-field guard active** (`AVC-01` check #2, forbids `class`/`phq9_score`/`gad7_score`/`flag_suicidal`, running W1 through W6, `REV-023` Issue 9). 4 commits: `dc98f24`, `eaca82b`, `f478719`, `247922e`. qa gate verdict **GATE:PASS** (10/10 items) — CI-mirror suite **833 passed / 2 skipped** (both named `test_bug_014.py` artifact-presence skips, expected); `BUG-011` resolved, `BUG-021` resolved, `BUG-022` guard standing-active W1→W6 (still open — dormant channel unchanged); 32 fixture assets at `apps/ai-server/tests/fixtures/`; 881 legacy files moved to `_archive/simulation_results/` | developer, qa | ☑ |
| W2 | **Complete, closed (r2 clean).** Multi-session narrowing (`final_slots`+`missing_slots` carry, `session_index`/`simulated_date`/`is_revisit`, file ledger) + dialogue v3 redesign landed atomically (`d3e524d`/`d666a2c`, qa **GATE:PASS**). Bundled `SM-01..08b` regression r1 (`EXP-014`): 9/11 pass — `SM-06` a genuine new dialogue-v3 regression, causally attributed by the pre-registered bisection protocol (`REV-023` Issue 7, plan Appendix B); `SM-08b` a pre-existing calibration gap (`BUG-026`), not a regression. Fix `4be9818` (new dialogue v3 pin `d8870e5d...4325`, repetition guard untouched) cleared its own qa **GATE:PASS**. `ADR-025`-authorized full r2 re-run (`EXP-014` r2): **CLEAN** — `SM-06` 12/12 turns, mandatory SI screen reached; `SM-08b` known-fail (`BUG-026`) disclosed, byte-identical to r1/`EXP-013` | developer, qa, experiment-tracker | ☑ |
| W3 | **Complete** — STT/OCR arbitrary-turn injection protocol (harness-only composer over `f1.py`'s public `patient_input_fn` seam) + `AVC-12` activation-rate instrumentation landed as pure-addition commit `cf6bc09` (5 files, +1525/-0): `src/injection_protocol.py` (composer + `ModalityProvenanceEvent` sidecar + `AVC-15` `ocr_texts` provenance helpers + zero-vendor `validate_schedule` dry-run) + `src/avc12_instrumentation.py` (activation-rate readers over existing surfaces: InputNormalizer `change_count`, dialogue retry-hint WARNING capture, greeting path, Policy-B placeholder) + 44 new deterministic tests. Live 2-vendor proof, zero model calls: SKT STT on `VP-001-001.mp3` → 92 chars/988ms; Upstage OCR on a VP-001 PDF → 1593 chars/3420ms. qa **GATE:PASS** (10/10 items — `AVC-03` independent grep 0 hits, pins unchanged). OCR fixtures for SC-6/SC-10 still absent (section 10) → both cells stay `SKIPPED-awaiting-user-material`; the composer itself is standalone-ready, zero code change needed once fixtures land | developer, qa | ☑ |
| W4 | **Complete — Policy B certified ELIGIBLE.** RAG trigger Policy A + Policy B implemented, config-switchable (default A), single-choke-point risk-lexicon filter over every composed query in both arms: code `91c04f5` (qa W4 gate **GATE:PASS**, 11/11 items, incl. a credential-containment check — see `error.md` `BUG-027`, self-disclosed security incident, containment confirmed clean) + fix batch `325daa4` (latency_ms persistence, `session_state` caller-scoped allowlist, stem-count correction — qa micro-gate **GATE:PASS**, 8/8 items). `REV-024`: `REV-022` Issues 9/10 **RATIFIED CLOSED at the design/code level** (ruling 1, binding W7/W8 wording discipline vs. the standing `BUG-014`/`VAL-009` lexicon-coverage gap); §6 allowlist-table widening **RATIFIED** (ruling 2, closes `ISS-F2V-002`); `session_state` inner-key mechanical allowlist **REQUIRED before W6-end** (ruling 3); Policy-B Gate 0 certification **pre-registered**, 5-criterion pass rule (ruling 4, plan Appendix C). **Honest halt-and-resume:** the tracker's first `EXP-015` dispatch refused to launch — the qa micro-gate verdict on `325daa4` existed only in the orchestrator's working context, not yet recorded in `discussion.md` — record was appended and `EXP-015` resumed (same ID) from its saved config, 0 cost from the halt. `EXP-015` certification batch (VP-003 + VP-001, 2 fresh F1→F2 chains, 4 model calls): all 5 Appendix C criteria **PASS** on both runs. `REV-025`: Gate-0 verdict **PASS** — **Policy B is ELIGIBLE for the A/B adjudication battery** (eligibility wording only — never certified/validated); `session_state` caller-scoped guard RATIFIED (standing condition on any future `routes/chat.py`-flow entry into scope); stem count corrected to **37 literal entries / 20 stem families**. Non-blocking follow-up carried to qa: `ISS-F2V-009`, a live VP-003 repetition-guard trip found during the cert run, must resolve before W7b. | developer, data, critic, qa | ☑ |
| W5 | Disease→questionnaire static mapping (ontology classification → `ScaleName` ⊆ `SUPPORTED_SCALES`) + `recommended_questionnaire` + caveat fields; CVR review of the mapping *(per `PLAN-2026-W28-Q` execution order — swapped in from the earlier plan-P wording, which put questionnaire-linkage here and disease mapping at W3)*. **Complete, closed.** Code shipped in two commits, both qa **GATE:PASS**: `9506650a` (`rag/questionnaire_mapping.py` 10-classification static table + `recommended_questionnaire` field + HPI-isolation test extension, 5th checked leak surface; suite 1034 passed + 2 known skips; structural offline check — every disease resolves into `SUPPORTED_SCALES` ∪ {None} — runs in default CI) and `113acdf` (W5 addendum: `CLASSIFICATION_TO_CAVEAT` table + `recommendation_caveat` field + disclaimer/report-render fixes for `CVR-003` Findings 1+4; suite 1063 passed + 2 known skips). `CVR-003` (clinical-validator): **adequate-with-findings, 0 blocking / 2 major / 3 minor — "may ship into the battery"**; Finding 1 (mood→PHQ-9 mania blind spot) and Finding 4 (disclaimer scope) fixed in `113acdf` and qa-gated; Finding 5 (no minimum-score/margin gate on the top-1-derived recommendation) **DEFERRED as a disclosed limitation** — pending W7's MET-4 near-tied-candidate data, W8 may recommend a gate empirically (`ISS-F2V-010`). | developer, clinical-validator, qa | ☑ |
| W6 | New-persona authoring (VP-010/011/012) + golden labels + AUD ontology landing; canary-copy provenance tagging (`DATASET-006` addendum); `THRESHOLD_1b`/N outputs recorded into a surviving plan appendix before archival. **Complete.** **Personas** (brainstorm): `VP-010_first_visit_minimizing.md`/`VP-011_first_visit_somatic.md`/`VP-012_first_visit_alcohol.md` authored; `CVR-004` (persona content gate) verdict **adequate-with-findings, 0 blocking each (overall 0 blocking/1 major/6 minor)** — cleared for golden-label authoring on all three, no revision required. **`CVR-005`** authored the six pre-registered clinical assessment instruments (MET-2 L1/L2/L3 rubric + trajectory sub-check, SC-6 confrontation checklist, SC-5 safety bar, protective-factors/medication-adherence slot-home, VP-010 Minimization-Probing Depth) — transcribed into plan Appendix E this fold. **AUD ontology** (`alcohol-use-disorder`, F10.2): `CVR-006` **sign-off, no conditions** (KO name finalized "알코올 사용장애(의존)"); DB load EXECUTED and disclosed — commit `1f47c54`, `rag.disease` **26→27** / `rag.disease_symptom` **169→171** (+craving, +perceived_loss_of_control), idempotency re-verified by a second run, canary zero-hit re-run post-load clean, suite 1092+2. **Golden labels + reveal-partition spec + canaries**: `DATASET-003-ext` (VP-010/011/012 golden domain labels), `DATASET-004-ext` (SC-4/5/8 reveal-partition spec, VP-001/VP-003; VP-002/VP-004 chain-ineligibility disclosed), `DATASET-006` (21 canaries, 7 personas × 3 categories; live zero-hit **63/63** across `rag.case_card`/`rag.qa`/`rag.session_insights`, run twice) — all three data-authored, commit `cceb8f6`. **`REV-026` (critic): all three DATASETs APPROVED** (independently re-verified), transcription-checklist items 1-7 RATIFIED as the complete gating set, `archive_gate.py` ruled ADEQUATE; its pre-arming condition (qa symlink audit) is now **PASS**, per `discussion.md` `PLAN-2026-W28-Q` status block "qa pre-arming symlink audit: PASS — formal record; REV-026 ruling 5 condition SATISFIED" (2026-07-11) — zero symlinks in `_archive/`/personas/docs/fixtures/experiments/root, repo-wide reverse check toward `_archive` → 0, limitation classified THEORETICAL; resolves `REV-027` blocker (2) (blocker (1), remedy-i, resolved via plan Appendix E item I). | data, clinical-validator, critic | ☑ |
| W6-end | **Transcription-verification checklist (orchestrator+critic)** — confirms CVR-002's six clinical remedies, REV-023's own pre-registered content (allowlist table, canary storage amendment, adopted AVC amendments), EXP-012 comparison excerpts, and data's threshold N/`THRESHOLD_1b` each have a surviving-doc location — **REQUIRED before `archive_gate.py` arming + `VER-00X`** (`REV-023` Issue 5, blocking-scoped). **Satisfied** — the precondition pass (plan Appendix E, `docs/ai/golden_labels_f1f2.md`, Appendix C cross-reference, the W6 checklist fold, `workflow_results_f1f2.md` entry, `ISS-F2V-011`) landed, and this gate was confirmed satisfied at the W6→W7 blind-state activation (below). | orchestrator, critic | ☑ |
| W6→W7 | **Applied** (commit `ec7c9bf`) — **Blind-state activation** (`ADR-023` Phase 2): `archive_gate.py` ARMED for all agents on `_archive/**` (observed live-armed during `EXP-016`); `VER-002` version transition executed (`result.md` reset to its VER-002 template, dated 2026-07-11, per `EXP-016`'s own gate-state observation); agent-memory quarantine and blind-brief protocol in force | orchestrator (activation), filemanager (`VER-002`), critic (blindness manifest already pre-registered and ratified in `REV-023`; W6-end row above is now ☑) | ☑ |
| W7a | **Complete.** Micro-batteries (blind execution): SC-13 canary — 7/7 F1→F2 chains, exit 0, 0/21 `AVC-01` canary hits (first-visit code path only — no evidence for the revisit/continuity path); SC-14 greeting diversity — 6/6 lightweight calls, exit 0, 0 repetition-guard trips, diversity confined to disclaimer wording (chief-complaint prompt byte-identical ×3; revisit-awareness exercised only in the minimal-reference case); SC-15 prompt-echo — 4/4 lightweight calls, exit 0, POSITIVE `AVC-05` finding on 3/4 reps (contained by `grounding.py`, 0 hits in any persisted artifact — an open containment-coverage gap, not "prompt-echo clean", `ISS-F2V-012`). qa **GATE:PASS** (`BUG-029` filed); `CVR-007` adequate-with-findings (0 blocking / 2 major / 2 minor); critic `REV-028` ruled `AVC-05` **BLOCKING**, overridden by `ADR-026` — W7b proceeds under that override plus a per-run echo-watch. | qa (mechanical), clinical-validator (canary/greeting sign-off), critic (verdict) | ☑ |
| W7b | **Complete.** Main matrix (blind execution): SC-1 (8/8, `AVC-05` empathy-reuse POSITIVE 6/8) → SC-12 (6/6, Tier-2 empathy reuse 6/6 logged) → SC-2/SC-3/SC-3b (46/46 calls, MET-9 12/16 agree, 3 English-paraphrase risk queries reached live retrieval) → SC-4/SC-5/SC-8 (VP-001/VP-003 only, 12/12 calls — SC-4 induction+6x-repeat truncation, SC-5 badgering-flag, SC-8 overwrite) → SC-7/SC-11/SC-9 (14/14 calls, 6/7 real STT/OCR vendor calls fired). **SC-6/SC-10 remain ☐ `SKIPPED-awaiting-user-material`** (0 of 2 required fixture genres exist on disk — not a gap in this wave's execution). qa **GATE:PASS** (`BUG-030`/`BUG-031` filed); `CVR-009` adequate-with-findings; critic `REV-030` non-blocking-with-required-corrections. Evidence: `docs/ai/workflow_results_f1f2.md` `w7b-main-matrix`; `docs/ai/workflow_discussion_f1f2.md` `ISS-F2V-013`..`ISS-F2V-022` + weak-point register. | qa, clinical-validator, critic (triple-gate, invariant 3) | ☑ |
| W8 | **Complete.** RAG A/B adjudication rule applied (Criterion 1, safety-dominant): **Policy A adopted** — 0 risk-worded queries shipped in the paired SC-2/SC-3/SC-3b set vs. Policy B's 3, a zero-tolerance asymmetric win, not a default fallback; MET-9 12/16 (75%, thin base); Gate 0.5 judge stability PASSED (unanimous raw `retrieve` both VPs, query-language instability noted separately, `ISS-F2V-019`/`VAL-015`). Final validity-gate verdict: **non-blocking-with-required-corrections** (`REV-030`) — MET-3 correction gated (17/22 top-1, 18/22 top-3, corrected). `AVC-12` contingency check: not triggered (no positive-path-never-fired condition observed this battery). No 인증/통과/certified/validated/deployment-ready wording licensed for any part of this program. Evidence: `docs/ai/workflow_results_f1f2.md` `w8-adjudication-disposition`. | critic (adjudication + disclosure), clinical-validator (per-run RAG face-validity check + `AVC-07`/`08`/`09` clinical shares), writer (`workflow_results_f1f2.md` entries) | ☑ |
| BUG-030-fix | **Complete** (`PLAN-2026-W28-S`, user-directed, post-battery). Iteration-1: `alternatives` re-recommendation menu deleted, dialogue prompt superseded v3→v4 (`dd4eba2`), qa **GATE:PASS** (suite 1137+2, mutation-checked). `EXP-017` targeted re-validation: SM-01..08b regression-free at the assertion level (`SM-08b` known-fail `BUG-026` unchanged); user hypothesis (prompt example-overcontrol) CONFIRMED, code/prompt channels ELIMINATED; repetition magnitude dropped on 2/3 naturalness personas but the rubric's §2 no-repetition bar FAILED all 3 sessions (VP-003 flat 9/10, NED=0.20 near-duplicate of a deleted v3 phrase). Triple gate: qa GATE:PASS / `CVR-010` **INADEQUATE** (2 blocking — empathy-presence collapse under probe load `BUG-035`; systematic SI item-V re-probe) / `REV-031` evidence-sound-with-corrections. **`BUG-030` stays open — NOT resolved to the pre-registered bar; mission stop-rule fired on `CVR-010`'s blocking finding, no iteration-2 dispatched autonomously.** Evidence: `docs/ai/workflow_results_f1f2.md` `bug030-fix-revalidation`; `docs/ai/workflow_discussion_f1f2.md` `ISS-F2V-013` (status update), `ISS-F2V-023` (new). | developer, qa, clinical-validator, critic | ☑ |
| BUG-030-iter2 | **Complete** (`PLAN-2026-W28-T`, user-directed — "문서 관리와 함께 버그 픽스 진행하라," ratifying the queued iteration-2 recommendation). Design (`docs/ai/fix_design_bug030_iter2.md`) reviewed pre-implementation: `REV-032` non-blocking-with-conditions (4 major, criteria A-E pre-registered) ∥ `CVR-011` adequate-with-conditions (rubric §10 addendum); reconciled in `ADR-029`. Implementation: near-duplicate empathy-clause retry guard + crisis-adjacent empathy-presence check (BUG-035 companion) + `[:30]` cap removed + marker-set correction, landed `d68c8a2` (code) / `266eea1` (design/review docs, 0 code diff), qa **GATE:PASS** (suite 1161+2, mutation-checked). `EXP-018` post-implementation re-validation (21 pipeline calls, 1 over the 20-call cap, disclosed) scored against `REV-032`'s pre-registered criteria: **A (repetition) FAIL** — VP-003 improved-but-over-bar in one cell, auto-FAILs in another with zero guard detection (guard-dedup defect, `BUG-036`); VP-001/VP-010 nominal PASS ruled instrument-limited by `REV-033`. **B (presence) PASS** both scored SC-5 sessions (0.909, zero two-consecutive misses) — `CVR-012`: "presence held in this sample, not guaranteed." **C (SM regression) PASS-with-conditions** — `SM-06` NEW failure, bisected stochastic-flake-likely, not cleared. **D (telemetry) PASS** (0/137 invariant violations). **E (marker spot-check) PASS-with-conditions** (bare-겠 eliminated; new soft-false-positives found in `-군요`). Triple gate: qa GATE:PASS / `CVR-012` (BUG-035 adequate-with-findings, BUG-030 inadequate-blocking, 2 blocking findings incl. a new interface-integrity finding `BUG-037`) / `REV-033` **blocking**. **`BUG-030`/`BUG-035` stay open — repetition bar (criterion A) FAILS as a pre-registered bar; mission stop-rule fired, no iteration-3 dispatched autonomously.** A distinct guard-dedup defect (`BUG-036`) and a distinct clinical-note-leak defect (`BUG-037`) were filed standalone this mission. Evidence: `docs/ai/workflow_results_f1f2.md` `bug030-iter2-revalidation`; `docs/ai/workflow_discussion_f1f2.md` `ISS-F2V-013`/`ISS-F2V-023` (status updates), `ISS-F2V-024`/`ISS-F2V-025` (new). | developer, qa, clinical-validator, critic | ☑ |
| combined-fixcycle | **Code-complete** (`PLAN-2026-W28-U`, user-directed — "빠르게 진행할 수 있는 순서로 진행해," scope reduced mid-mission to fixes-only by later user word: "버그 픽스만 하고 통합 재검증은 나중에 할거다"). Three ranked next-diagnosis items from the iteration-2 stop-rule addressed as one combined cycle: Fix 1 (`BUG-036` empathy-repetition-guard dedup — occurrence-preserving clause extraction restores both the session-cap and back-to-back sub-checks) + Fix 3 (`BUG-037` DialogueAgent output-isolation guard — top-priority check for same-turn/prior-turn slot-value echo and verbatim patient-echo, never falls through on exhaustion) landed atomically, then Fix 2 (`ADR-030` Option C — retry-budget-exhaustion safe degrade of the leading empathy clause only, question/clinical content byte-identical by construction) implemented on top, including the post-implementation CF1 follow-up (a real splice-boundary risk found by `CVR-014`, fixed same cycle via an `_is_empathy_clause` gate on the replace branch). Commit `5baefb9` (code+tests) + `e833937` (design/review docs). Design/pre-implementation review: `CVR-013` (Option C picked, 3 binding conditions, F1 scope finding) ∥ `REV-034` (non-blocking-with-conditions, marker-contamination requirement, splice condition), reconciled in `ADR-030`. Post-implementation review: `CVR-014` (adequate-with-findings; condition 3 NOT-as-claimed → CF1, dispositioned same cycle) ∥ `REV-035` (non-blocking-with-conditions; `REV-034` conditions (a)(b)(c) all SATISFIED; NEW criterion-B scorer contamination finding, pre-battery prerequisites grow 3→4). qa **GATE:PASS ×2** (combined implementation gate, mutation-checked all detectors/paths + both-shape A-B-A/A-A-A artifact replay; CF1 micro-gate, suite 1236 passed + 2 skipped, +5 tests, zero regressions). **Every status in this row is code-complete, offline-gated, NOT live-verified.** Per the user's scope-change directive ("일단 진행중인 사안부터 마무리하자. 나중에 F1-F5 총 검증할거기 때문에 총검증은 보류하자"), this mission's own live re-validation battery (SM-01..08b [11] + naturalness probes VP-001/003/010 [3] + an SC-5-style crisis-adjacent cell [~4] + telemetry review, ~18-22 calls) is CUT from this mission and DEFERRED into the upcoming F1–F5 total validation as absorbable cells, to be judged against the UNCHANGED pre-registered bars (repetition family ≤2/session + zero back-to-back; presence ≥0.90 + zero 2-consecutive misses; zero DETECTED-violation ship-throughs; zero clinical-note-register text patient-facing; SM assertion-identical with SM-08b known-fail `BUG-026` unchanged and SM-06 inconclusive per `REV-033` §C; telemetry sanity) plus 4 pre-battery prerequisites (criterion-A scorer flag-exclusion; criterion-B scorer flag-exclusion + `b1_telemetry` fix; criterion-D invariant extension; criterion-A stem-level clustering backport). **`BUG-036`/`BUG-037` status: fixed-pending-live-verification — neither resolved nor closed; `BUG-030`'s own repetition bar stays open, unmeasured against the new code.** Evidence: `docs/ai/workflow_results_f1f2.md` `bug036-exhaustion-bug037-fixcycle`; `docs/ai/workflow_discussion_f1f2.md` `ISS-F2V-013`/`ISS-F2V-024`/`ISS-F2V-025` (status updates), weak-point register items 12/13/15 (annotated). | developer, qa, clinical-validator, critic | ☑ |

## F3 quick development (`PLAN-2026-W28-V`)

> Separate, orthogonal mission thread — F2-driven questionnaire administration, per the user's redefinition (`ADR-031`). Not a wave of the F1-F2 continuous-scenario validation program above: the blind gate was lifted specifically for this development phase and is scheduled to re-activate for the future F1–F5 total validation (`ADR-031`); the deferred F1–F5 live-verification cells and 4 pre-battery prerequisites listed in the program above are untouched, bars unchanged. Full plan: `discussion.md` `PLAN-2026-W28-V`. Design doc: `docs/ai/f3_quick_dev_plan.md`.
>
> **Status (2026-07-12, mission COMPLETE through doc fold):** all 7 stages below now ☑. `REV-036` pre-implementation review non-blocking-with-conditions, dispositioned `ADR-032`; implementation + qa `GATE:PASS` (suite 1342+2); `EXP-019` live functional validation (3 of 4 cells run, GAD-7 `SKIPPED-awaiting-user-material`); `CVR-015` adequate-with-findings ∥ `REV-037` evidence-sound-with-corrections (both entries reported side by side, never merged). Coverage boundary, stated plainly: AUDIT-C was never exercised live this battery (VP-012 resolved to PHQ-9 instead, a live `VAL-014` instance); the `safety_referral`/critical-item-positive path was never exercised (VP-003 fell to `no_questionnaire_indicated` via an F2-side `VAL-015` lexicon-drop); GAD-7/PHQ-4/WHO-5 remain item-bank-unpopulated by design. Open user question, carried from `f3_quick_dev_plan.md` §2.3: whether a v1 item bank (validated PHQ-9/GAD-7/PHQ-4/WHO-5/AUDIT-C item text + response anchors) should be team-authored (routed through clinical-validator content review) or user-supplied with recorded licensing terms — unresolved. Full detail: `docs/ai/workflow_results_f1f2.md` `f3-quick-dev`.

| Stage | Essence | Owning agent(s) | Status |
|:--|:--|:--|:--|
| Plan doc | Architecture, schemas, F2→F3 trigger flow, item bank v0, F5-consumption ledger record design, simulator score-selection design, validation design (`docs/ai/f3_quick_dev_plan.md`) | developer | ☑ |
| Critic pre-impl REV | Pre-implementation review of the plan doc + this PRD/checklist doc fold — gates implementation | critic | ☑ **`REV-036`** (2026-07-12) — non-blocking-with-conditions (4 major, 3 minor, 8 positive findings); dispositioned by `ADR-032` (v0 scope ratified, MAY/MUST-NOT table ordered for step 9, `VAL-014` cross-ref ordered, HPI-isolation adversarial-test gate on step 6) |
| Implementation | `src/f3.py` production module + item bank (`src/scoring/item_bank.py`) + ledger extension + `continuous_test.py` F3 stage (production); simulator score-selection mode + tests (harness, `tests/simulation/`) | developer | ☑ (2026-07-12) — landed on `feat/f2-continuation-w28h` at HEAD `1e4223a` (restoration commit, `PLAN-2026-W28-V` step 0); confirmed by `EXP-019`'s own pre-run checklist (untracked `src/f3.py`, `src/schemas/survey_result.py`, `src/scoring/item_bank.py` + harness files) and the qa gate record below |
| qa gate | CI-mirror ruff+pytest, mutation-checks on scoring logic, no-harness-deps-in-production check | qa | ☑ |
| qa gate record (2026-07-12) | **GATE:PASS.** CI-mirror: ruff clean, suite 1342 passed + 2 skipped (baseline 1236+2, +106 = 18+31+25+17+15 across the 5 new test files, verified by `--collect-only`). Mutation-checks (all reverted, working tree clean after): (a) item-bank PHQ-9 range bound corrupted (3→5) → `test_phq9_populated_nine_items_range_0_3` failed as expected; (b) `_clamp_response` disabled → 3/4 `TestClampResponse` tests failed as expected; (c) `_scale_scores.json` total misreported (+1) in `save_f3_result` → `test_scale_scores_projection_round_trips_into_f2_load_scale_scores` failed as expected; (d) `_build_user_content` mutated to surface `AgentInput.extra` (independent channel, not the slot-data live-container channel `test_f3_hpi_isolation.py` already exercises) → 2/3 `TestHPIIsolationChannelExtra` tests failed as expected. No-harness-deps: `src/f3.py` — 0 `tests/` imports, 0 `session_ledger` references, 0 LLM/vendor call sites (grep + inspection); `continuous_test.py`'s `tests/simulation` imports are the documented harness-only seam, not a violation. Item-bank content: PHQ-9/AUDIT-C `text_ko` labels diffed byte-for-byte against `VP-001`/`VP-003`/`VP-012` persona tables — exact match, no invented text; GAD-7/PHQ-4/WHO-5 unpopulated, administration structurally impossible (`administer_survey` raises `ValueError`, answer_fn never called — test-proven); `response_anchors=None` everywhere. Pins: safety v2 SHA256 `3e9ca6b4...b3390c` and dialogue v4 SHA256 `f93e995f...7144` recomputed exact-match against `workflow_results_f1f2.md`; `git diff` empty for all `docs/ai/prompts/**`, `orchestrator.py`, `chat.py`, `f1.py`, `f2.py`. Harness-gap fixes: `TestSingleSessionLedgerGapFix::test_main_writes_single_session_ledger_entry` (single-session `run_chain`/`_main` writes a ledger entry with `"f3"` populated) and `TestRunMultiSessionChainF3Integration::test_session2_f2_receives_session1_f3_scale_scores_path` (session N's `_scale_scores.json` feeds session N+1's F2 call) both pass. Leakage lens: persona "예상 점수" (expected-score) tables are read only by `ExpectedAnswerFn`/`_extract_expected_scores_table` (harness-side, `tests/simulation/`); `SurveyAnswerLLM`'s prompt splices only `item.text_ko` + Section 6/5 persona text — Section 3 (expected scores) independently verified structurally disjoint, never spliced in; production `survey.json` never stores the persona's answer-key table. Full evidence: qa RESULT this session. | qa | ☑ |
| EXP functional validation | `EXP-019` — live F1→F2→F3 functional validation, 3 of 4 pre-registered cells run (4 session-level cells: VP-001 ×2 + VP-003 ×1 + VP-012 ×1), all exit 0; GAD-7 cell `SKIPPED-awaiting-user-material` | experiment-tracker | ☑ (2026-07-12) — `result.md` `EXP-019` |
| CVR + REV verdicts | Clinical-adequacy review (persona consistency, band adequacy) and evidence-validity review of `EXP-019` — separate verdicts, never merged | clinical-validator, critic | ☑ (2026-07-12) — `CVR-015` adequate-with-findings (0 blocking for dev-scope; 5 major, 1 minor, 3 findings gate any patient-facing reuse) / `REV-037` evidence-sound-with-corrections (1 major — disclosure-completeness, not a data-validity defect); MAY/MUST-NOT wording table (`REV-037`(c)) binds this fold |
| Doc folds | Monitoring-doc folds (this checklist, `checklist_task1.md`, `PRD_task1_v2.md`) + `development_report.md` DR entries (this mission + 2 parked — actual count 3, see `DR-015` preamble) | writer | ☑ (2026-07-12) — this pass (`PLAN-2026-W28-V` step 10); `workflow_results_f1f2.md` `f3-quick-dev` entry, `workflow_discussion_f1f2.md` `ISS-F2V-026`/`ISS-F2V-027`, `development_report.md` `DR-015`..`DR-018` |

Source: `discussion.md` `PLAN-2026-W28-V`, `ADR-031`; `docs/ai/f3_quick_dev_plan.md`.

## Reading this checklist

- A ☑ plan-finalization row means the step's RESULT exists and is folded into plan v1.2 — it does not mean the step's content is beyond challenge; `CVR-002`/`REV-023` (now ☑, verdicts recorded in plan section 8) were exactly that challenge mechanism, applied against this revision.
- An execution-wave row starts ☐ and flips to ☑ once its deliverable is evidenced (RESULT + doc update, per its "Done when" column) — W0 (2026-07-11), W1 (2026-07-11, qa GATE:PASS), W2 (2026-07-11, qa GATE:PASS ×2, `ADR-025` r2 clean), W3 (2026-07-11, qa GATE:PASS 10/10 items, commit `cf6bc09`), W4 (2026-07-11, code `91c04f5` + fix batch `325daa4` both qa GATE:PASS, `REV-025` Gate-0 PASS — Policy B ELIGIBLE), W5 (2026-07-11, code `9506650a` + addendum `113acdf` both qa GATE:PASS, `CVR-003` adequate-with-findings — may ship into the battery), and W6 (2026-07-11, personas `CVR-004`-cleared, `CVR-005` instruments authored, AUD ontology `CVR-006`-signed-off and loaded `1f47c54`, golden labels/reveal-partition/canaries `cceb8f6` and `REV-026`-approved), W6-end (2026-07-11, transcription-verification confirmed satisfied), W6→W7 (2026-07-11, blind-state activation applied, commit `ec7c9bf`, `VER-002`), W7a (2026-07-11, qa **GATE:PASS**, `CVR-007` adequate-with-findings, critic `REV-028` `AVC-05` BLOCKING overridden by `ADR-026`), W7b (2026-07-11, qa **GATE:PASS**, `CVR-009` adequate-with-findings, `REV-030` non-blocking-with-required-corrections, SC-6/SC-10 remain skipped), and W8 (2026-07-11, RAG A/B adjudication CLOSED — Policy A adopted per Criterion 1, `REV-030` final validity-gate verdict non-blocking-with-required-corrections) are the program's completed waves so far; see their rows for evidence pointers.
- Plan-finalization step 5 (STATE entry + `handoff.json` + user-facing report, orchestrator) remains **☐ in progress** as of this doc-fold pass — this writer pass covers the monitoring-doc side of the record only (`workflow_results_f1f2.md`, `workflow_discussion_f1f2.md`, this checklist); the STATE entry and `handoff.json` update are orchestrator's own remaining action, unchanged by this pass. W6-end and W6→W7 were each a separate, one-time confirmation/transition step, not folded into W6's own row.
- The W3/W5 row wording was realigned this update to the binding execution order recorded in `discussion.md` `PLAN-2026-W28-Q`'s wave table (W3 = STT/OCR injection + `AVC-12`; W5 = disease→questionnaire mapping) — an earlier plan-P-era draft of this checklist had the two swapped; no wave has actually executed under the old ordering, so this is a wording correction, not a re-scope.
- W6→W7 is listed as its own row, not folded into W6 or W7a, because it is a one-time state transition (`ADR-023`) rather than a batch of runs.
- `BUG-030-fix`, `BUG-030-iter2`, and `combined-fixcycle` are three further rows appended below W8, not folded into it — all are user-directed, post-W8 remediation missions (`PLAN-2026-W28-S`/`PLAN-2026-W28-T`/`PLAN-2026-W28-U`) outside the original W0-W8 plan. Neither `BUG-030-fix` nor `BUG-030-iter2` implies `BUG-030`/`BUG-035` are closed — see `docs/ai/workflow_discussion_f1f2.md` `ISS-F2V-013`/`ISS-F2V-023` for the current open status, and `error.md` for the authoritative bug state. `combined-fixcycle` differs from the two rows above it in kind, not just content: it carries **no `EXP-NNN` run** (the mission's own live re-validation battery was cut by user word and deferred into the upcoming F1–F5 total validation) — its ☑ marks the mission's own implementation scope (code + offline gates + design/post-implementation review) as complete, not any bug's live behavior against real generation. `BUG-036`/`BUG-037` status in `error.md` is fixed-pending-live-verification, not any other status, until that deferred battery runs.
