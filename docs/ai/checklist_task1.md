# Task 1 개발/검증 체크리스트 (v2)

> AI 기반 사전문진 Handoff Report 생성 (기능 1-1 ~ 1-5)
> **Version**: v2.0 | **Date**: 2026-07-06
> **기준 문서**: `PRD_task1_v2.md` | **작업 보고**: `development_report.md` (감사 근거: DR-001)
> 이전 버전 체크리스트/이슈/리포트는 `docs/ai/backups/` 참조.

## ID 형식 및 상태

`T1-Fn-{TYPE}-{SEQ}` — ID는 v1에서 승계하며 전역적으로 증가한다 (재사용 금지).

| 상태 | 의미 |
|---|---|
| `[ ]` | TODO |
| `[~]` | IN_PROGRESS / 부분 완료 |
| `[x]` | DONE (증거 유효) |
| `[!]` | BLOCKED / **REGRESSED** (완료였으나 회귀·증거 무효화) |

## v1 → v2 상태 조정 내역 (2026-07-06 통합 감사 반영)

2026-07-06 3-agent 통합 감사(QA 코드 감사, Critic 검증 타당성 감사, 아키텍처 갭 분석 — `development_report.md` DR-001)에 따라 v1의 `[x]` 중 다수가 조정되었다. 핵심 사유:

1. **서버 부팅 불가** (ISS-024): `orchestrator.py:41`이 삭제된 `trend_plotter`를 import → `src.main` import 실패, 전체 라우트 다운.
2. **머지 회귀** (ISS-025/026): ISS-020 fix(scale_scores/risk_events 인계)가 후속 머지에서 소실, ISS-019 fail-closed가 부분 소실(LLM 전면 장애 시 fail-open 실증됨).
3. **Slot 날조** (ISS-027): clinical_slot 프롬프트 예시가 slot 값으로 verbatim 유입 — 07-03 F1 "pass" 근거 무효화.
4. **격리 테스트** (ISS-033): keyword recall, survey scoring, orchestrator, 12-section 테스트가 `_backup/tests_old/`로 이동되어 활성 스위트에 부재 — 다수 `[x]`의 회귀 방어선 상실.

---

## Phase 1 — 상태 (감사 반영)

### F0: 공통/Orchestrator (T1-F0-*)

| ID | Type | 항목 | 상태 | 근거/비고 |
|---|---|---|---|---|
| T1-F0-DEV-001 | DEV | Orchestrator state machine 구현 (11-state) | [x] | Stage 0 복구로 부팅 정상화 (ISS-024 해결), 23 unit test 활성 green |
| T1-F0-DEV-002 | DEV | CTRS <-> RiskLevel 매핑 enum 구현 | [x] | RPT-001 |
| T1-F0-DEV-003 | DEV | agent_model_registry.yaml 전체 agent 등록 확인 | [x] | 13 agent 키 확인 |
| T1-F0-DEV-004 | DEV | PromptLoader 경로 설정 및 검증 | [x] | RPT-008 |
| T1-F0-CFG-001 | CFG | Docker Compose AI server 개발 환경 확인 | [x] | **갱신(2026-07-08):** `PLAN-2026-W28-D` 산출물(`docs/dev-environment.md`, PR #36 Master 병합)로 충족 — qa 사실검증 게이트가 서비스명·포트·env 키·경로를 실제 compose/Makefile 대비 검증(1차 FAIL → BUG-013 발견·수정 후 재게이트 PASS, suite 627). redis 관련 compose 블록 제거(PR #35)도 동일 게이트에서 grep 0건으로 검증됨. 근거: `discussion.md` PLAN-2026-W28-D, `error.md` BUG-013 |
| T1-F0-CFG-002 | CFG | shared-contracts 패키지 동기화 | [ ] | - |
| T1-F0-DOC-001 | DOC | PRD_task1 작성 | [x] | v2: `PRD_task1_v2.md` |
| T1-F0-DOC-002 | DOC | checklist_task1 작성 | [x] | 본 문서 (v2) |
| T1-F0-DOC-003 | DOC | 가상 환자 Persona VP-001~VP-004 작성 | [x] | - |
| T1-F0-DOC-004 | DOC | Patient LLM 시뮬레이션 사양서 작성 | [x] | spec ↔ f1.py 불일치 다수 (drift 목록 DR-001) — 갱신 필요 |
| T1-F0-VER-001 | VER | Orchestrator 전체 파이프라인 e2e 테스트 | [~] | Stage 0 후 mocked-level e2e 테스트 복원 green. 실데이터 재검증은 T1-F0-VER-003 |
| T1-F0-VER-002 | VER | 전체 VP 4명 통합 시뮬레이션 | [!] | 07-03 런 evidence 무효화 (ISS-027 slot 날조) — G-F 이후 재실행 |

### F1: 자율 대화 기반 문진 (T1-F1-*)

| ID | Type | 항목 | 상태 | 근거/비고 |
|---|---|---|---|---|
| T1-F1-DEV-001 | DEV | SafetyClassifierAgent CTRS 5단계 매핑 | [x] | RPT-002 |
| T1-F1-DEV-002 | DEV | InputNormalizerAgent 구현 | [~] | **갱신(2026-07-10, critic ruling (d)/REV-021):** PR #38 통합(`feat/f1-stt-ocr-integration`@`5bdd379`)으로 dead code가 아니게 됐다 — `f1.py`가 모든 환자 입력을 이 agent에 통과시킨다(§2.10 `PRD_task1_v2.md`). 그러나 프롬프트↔스키마 drift(ISS-039)가 이제 **라이브에서 실측 확인**됐다: 교정 시도 14/14(100%)가 스키마 검증 실패 → `_safe_fallback()`(`error.md` BUG-020, open, major; `result.md` EXP-013). Fail-safe는 검증됨(원문·위험 표현 verbatim 보존). PR #38 병합의 merge-gating 조건은 아니다(수정 미적용, near-term follow-up 권고). "인증"/"통과"/"검증됨" 표현 미사용 |
| T1-F1-DEV-003 | DEV | STT Agent + SKT A.X adapter 구현 | [!] | 벤더 계약 대기 (ISS-011) — **2026-07-10 갱신 없음, 신규 as-built 상태는 T1-F1-DEV-030 참조** (본 ID는 과거 스코프 기록으로 보존, 재사용 금지) |
| T1-F1-DEV-004 | DEV | OCR Agent + Upstage Document Parse adapter 구현 | [ ] | ISS-011 — **2026-07-10 갱신 없음, 신규 as-built 상태는 T1-F1-DEV-031 참조** (본 ID는 과거 스코프 기록으로 보존, 재사용 금지) |
| T1-F1-DEV-005 | DEV | DialogueAgent slot coverage tracking | [x] | 역할 분리 후 f1.py가 coverage 전담 |
| T1-F1-DEV-006 | DEV | /ai/chat/respond Orchestrator 연동 리팩토링 | [~] | 부팅 정상화(Stage 0). slot 누적 불능(ISS-029)은 T1-F0-DEV-006에서 해결 |
| T1-F1-DEV-007 | DEV | POST /ai/stt/transcribe 라우트 | [!] | T1-F1-DEV-003 선행 — **2026-07-10 갱신 없음, 신규 as-built 상태는 T1-F1-DEV-030 참조** |
| T1-F1-DEV-008 | DEV | POST /ai/ocr/parse 라우트 | [ ] | T1-F1-DEV-004 선행 — **2026-07-10 갱신 없음, 신규 as-built 상태는 T1-F1-DEV-031 참조** |
| T1-F1-DEV-009 | DEV | safety_classifier prompt 고도화 | [x] | 07-03 v1 58줄. 간접표현 하한표 방향 모호성은 T1-F1-DEV-017에서 함께 수정 |
| T1-F1-DEV-010 | DEV | dialogue prompt 고도화 | [x] | 07-03 v1 54줄 |
| T1-F1-DEV-011 | DEV | SentimentAnalyzerAgent 구현 (Mode A/B) | [x] | RPT-011. 단 f1.py 미연동 (T1-F1-DEV-014) |
| T1-F1-DEV-012 | DEV | SentimentAnalyzer schema 정의 | [x] | RPT-011 |
| T1-F1-DEV-013 | DEV | SentimentAnalyzer prompt v1 | [x] | RPT-005 |
| T1-F1-VER-001 | VER | VP-001 자율 대화 시뮬레이션 (초진 경증) | [!] | 07-03 evidence 무효화 (slot 날조, CTRS 4 vs spec 5) — G-F 후 재실행 |
| T1-F1-VER-002 | VER | VP-003 자율 대화 시뮬레이션 (crisis) | [x] | 위기 감지+109/119+즉시종료는 2회 재현으로 유효. slot 값은 무효 (재검증은 VER-014) |
| T1-F1-VER-003 | VER | Safety gate CTRS 1-2 crisis bypass 통합 테스트 | [x] | VP-003 pipeline 레벨 재현 |
| T1-F1-VER-004 | VER | STT->InputNormalizer 정규화 정확도 | [!] | T1-F1-DEV-003 선행 |
| T1-F1-VER-005 | VER | Slot coverage convergence (15턴 내 0.7) | [!] | 07-03 "3턴 내 0.8"은 날조 fill의 산물 (ISS-027/034). coverage 재정의(DEV-019) 후 재측정 |
| T1-F1-VER-006 | VER | 위기 키워드 recall >= 95% 회귀 테스트 | [x] | Stage 0에서 활성 복원, 40 tests green (ISS-038 칼로리 오탐 케이스 포함) |
| T1-F1-VER-007 | VER | SentimentAnalyzer per-utterance 정확도 | [x] | RPT-018 |
| T1-F1-VER-008 | VER | SentimentAnalyzer session-level 정합성 | [x] | RPT-018 |

### F2: RAG 기반 정신건강 영역/진료과 후보 추론 — DomainInferenceAgent (T1-F2-*) — 구현·llm_only 라이브 배치 완료, risk≠domain 규칙 위반으로 비인증 처분 (설계 확정 2026-07-08, 구현 미션 완료 2026-07-08)

> 사양 근거: `discussion.md` PLAN-2026-W28-C(Step 2 C-1~C-5, Step 3 REV-006, 구현 미션 REV-007/REV-008), ADR-013/ADR-014, 2026-07-08 사용자 승인. PRD: `PRD_task1_v2.md` §3(v2.2). 아래 5개 DEV 항목은 2026-07-08 구현 미션(`development_report.md` DR-006)에서 실제 구현 완료 여부에 따라 상태를 갱신했다 — 상세는 각 행의 근거 인용 참조.

| ID | Type | 항목 | 상태 | 비고 |
|---|---|---|---|---|
| T1-F2-DEV-001 | DEV | **DomainInferenceAgent** 구현 (Stage 2, LLM 1회 호출, strategy=benchmarked: Solar Pro3 1차/K-EXAONE 2차) + I/O 스키마 정의 + 프롬프트 신규 작성(v3 12원칙 적용, placeholder-only) | [x] | PRD §3.1/§3.4/§3.5. **구 TemporalRetrieverAgent 정의는 F4(종단 검색)로 보류 — superseded-for-F2, 삭제 아님**(구 명칭·`/ai/temporal/retrieve`·composite scoring·8-쿼리 플래너는 F4 이관 대상). 구현 완료(2026-07-08): `agents/domain_inference.py`, `schemas/domain_inference.py`, `docs/ai/prompts/domain_inference/v1.system.md`(2382자/74줄, echo-risk audit clean — REV-007). qa GATE:PASS. 근거: DR-006 §1 |
| T1-F2-DEV-002 | DEV | 근거-화이트리스트(evidence whitelist) 런타임 검사 모듈 신규 구현 — `evidence.source_id ∈ 실제 반환 청크 ∪ F1 발화`; rag_chunk evidence는 `chunk_ids` 멤버십뿐 아니라 quote↔청크 본문 어휘 대조 포함(`grounding.py`의 F2판, REV-006 조건 1) | [x] | 구 "pgvector embedding 저장/검색 모듈"은 이미 PR #31로 `rag/retrieval.py`에 구현·병합됨(Master) — 본 항목은 대체 범위(F2 fabrication=0 하드 게이트의 전제 조건). PRD §3.2. 구현 완료: `src/eval/f2_grounding.py` — REV-006 조건 1 genuinely closed(REV-007 Part B #1: rag_chunk evidence 어휘 대조 code-verified). 근거: DR-006 §1 |
| T1-F2-DEV-003 | DEV | `POST /ai/domain/infer` 라우트 신규(registry key `domain_inference`) — 프로덕션 통합(orchestrator.py 연동)은 G-D-F2 게이트로 보류 | [x] | 구 `POST /ai/temporal/retrieve`는 F4 보류(superseded-for-F2). 인증은 이 신규 라우트가 아니라 기존 rag 라우터 대상(T1-F2-SEC-001 참조). PRD §3.6, §9.2. **정정(2026-07-08):** 직전 판정("미착수")은 증거 부족에 따른 오류였다. 직접 확인 결과 `apps/ai-server/src/routes/domain.py`가 존재하며(`POST /ai/domain/infer` 구현, standalone route), `apps/ai-server/src/main.py:24`가 이를 import(`from src.routes.domain import router as domain_router`)하고 `main.py:51`이 `app.include_router(domain_router)`로 등록해 서버 부팅 시 실제로 마운트된다(`main.py:11` 문서화: "POST /ai/domain/infer (F2, standalone — not wired into orchestrator.py)"). 항목 텍스트 자체가 명시하듯 orchestrator.py 11-state machine 프로덕션 연동은 별도 범위(G-D-F2 게이트 보류)이며 본 항목(라우트 신규 구현·등록)의 완료 여부와 무관 — 라우트는 완료. qa GATE:PASS(구현 전체 게이트, 라우트 파일 포함 검증 범위). 근거: `src/routes/domain.py`, `src/main.py:24,51`, `docs/ai/development_report.md` DR-006 §1 |
| T1-F2-DEV-004 | DEV | 오프라인 테스트 5종 구현: 출력 스키마(evidence≥1/후보), 근거-추적성 유닛(fixture accept/reject), 프롬프트 단정문(placeholder/절대규칙/예산/경계지시), `llm_only` 폴백 경로, department-derives-from-domain | [x] | 구 "RAG scoring formula"는 composite scoring/8-쿼리 플래너와 함께 F4 보류. PRD §3.7. 구현 완료: `tests/test_domain_inference.py`, `tests/test_f2_grounding.py`, `tests/test_f2_pipeline.py`, `tests/test_prompt_domain_inference.py`(5종 커버). qa GATE:PASS. 근거: DR-006 §1, REV-007 Part B |
| T1-F2-CFG-001 | CFG | pgvector extension (placeholder) | [ ] | - |
| T1-F2-VER-001 | VER | VP-002 domain/department 후보 라이브 검증 (재진) | [ ] | 골든 라벨(DATASET, multi-label, critic 승인) 대비 n≥2 세션(권장 n≥3), 서술적 보고(비율 주장 아님). 선행: T1-F2-VER-005. PLAN-2026-W28-C C-4, REV-006 조건 2 |
| T1-F2-VER-002 | VER | VP-004 domain/department 후보 라이브 검증 (중증 재진) | [ ] | 상동 + 위험≠도메인 근거 사용 금지 부정 fixture 결과 포함(REV-006 조건 3) |
| T1-F2-VER-003 | VER | VP-001 domain/department 후보 라이브 검증 (초진, 최소 정보) | [ ] | 상동. DB 프리플라이트 실패 시 `llm_only` 런으로 별도 라벨(REV-006 조건 6, ADR-013 (4)) |
| T1-F2-VER-004 | VER | Retrieval ranking 품질 (top-1/top-3 정량 검증) | [ ] | **유예 목표**: "사전 정의 10개 임상 시나리오"가 저장소에 실존하지 않음(grep 0건, REV-006 Summary 긍정 소견 (a)) — 70%/90%는 임상 검토 시나리오셋 확보 또는 관측 ≥12건 누적 전까지 보류. PLAN-2026-W28-C C-4 |

### F3: 구조화된 사전문진 설문 (T1-F3-*)

| ID | Type | 항목 | 상태 | 근거/비고 |
|---|---|---|---|---|
| T1-F3-DEV-001 | DEV | ClinicalSlotAgent 구현 (12 Standard Slots) | [~] | 구현 존재하나 **프롬프트 예시 echo 날조 결함** (ISS-027) — DEV-017로 수정 |
| T1-F3-DEV-002 | DEV | POST /ai/slots/extract 라우트 | [x] | 단 safety gate 없이 원문 수용 (DR-001 참고) |
| T1-F3-DEV-003 | DEV | Rule-based scoring engine (5개 척도) | [x] | 39 boundary test — 감사에서 견고성 확인 (positive) |
| T1-F3-DEV-004 | DEV | POST /ai/survey/score 라우트 | [x] | PHQ-9 Q9 → safety_referral 동작 확인 |
| T1-F3-DEV-005 | DEV | Survey Planner 로직 Orchestrator 통합 | [!] | `plan_surveys`/`score_and_check_safety` **호출자 0 — dead code** (ISS-029 연관) |
| T1-F3-VER-001 | VER | PHQ-9 scoring unit test | [x] | Stage 0에서 활성 복원 green |
| T1-F3-VER-002 | VER | GAD-7 scoring unit test | [x] | 상동 |
| T1-F3-VER-003 | VER | VP별 slot extraction 정확도 비교 | [!] | RPT-018 실제 내용은 키 멤버십/산술 검증 — **정확도 측정 아님** (항목명 과대). VER-013으로 대체 수행 |
| T1-F3-VER-004 | VER | 자살 문항 양성 시 Safety 연동 테스트 | [~] | 로직 단위검증은 됐으나 소비자가 dead code — 통합 레벨 재검증 필요 |

### F4: 종단적 상태 추론 (T1-F4-*)

> **F4 신규 항목은 `docs/ai/f4_checklist.md`에서 관리** (`PLAN-2026-W29-D`, 2026-07-13, 사용자 지시:
> "Checklist는 f4_checklist 문서로 정리하라") — 아래 표는 레거시 항목(`T1-F4-DEV-001~005`/
> `VER-001~006`, TemporalSummaryAgent pairwise 유산)만 보존한다. 신규 세부 항목을 이 표에 중복
> 기재하지 않는다. ID 시퀀스(`DEV-006`부터, `VER-007`부터)는 `f4_checklist.md`가 승계한다.

| ID | Type | 항목 | 상태 | 근거/비고 |
|---|---|---|---|---|
| T1-F4-DEV-001 | DEV | TemporalSummaryAgent 구현 | [x] | rule-engine (LLM 아님 — 프롬프트 문서와 drift, DR-001). ISS-014/015/016 수정 반영 |
| T1-F4-DEV-002 | DEV | POST /ai/temporal/summarize 라우트 | [x] | 단 prior 데이터 공급자가 시스템에 없음 (F2 미구현) |
| T1-F4-DEV-003 | DEV | Direction classification (majority vote) | [x] | RPT-014/020 |
| T1-F4-DEV-004 | DEV | Plot-ready time-series 생성 | [x] | Stage 0에서 trend_plotter 복원, 테스트 green |
| T1-F4-VER-001 | VER | VP-002 종단 비교 (호전) | [~] | 합성 수치 unit test만. 실데이터 재검증 = VER-005 |
| T1-F4-VER-002 | VER | VP-004 종단 비교 (악화) | [~] | 상동. "새 증상 감지"는 현 rule 엔진으로 불가 (RPT-021 자인) |
| T1-F4-VER-003 | VER | 초진 "unknown" 반환 | [x] | RPT-014 |
| T1-F4-VER-004 | VER | 모순 감지 테스트 | [x] | RPT-016. 테스트 격리 — 복원 필요 |

### F5: Handoff Report 생성 (T1-F5-*)

> **F5 신규 항목은 `docs/ai/f5_checklist.md`에서 관리** (오케스트레이터 dispatch, 2026-07-14) —
> 아래 표는 레거시 항목(`T1-F5-DEV-001~007`/`VER-001~008`)만 보존한다. 신규 세부 항목을 이 표에
> 중복 기재하지 않는다. ID 시퀀스(`DEV-008`부터, `VER-009`부터, `DOC-001`부터)는
> `f5_checklist.md`가 승계한다.

| ID | Type | 항목 | 상태 | 근거/비고 |
|---|---|---|---|---|
| T1-F5-DEV-001 | DEV | 12-section 템플릿 강화 | [x] | RPT-009 |
| T1-F5-DEV-002 | DEV | Evidence registry 완전성 강화 | [x] | RPT-009 |
| T1-F5-DEV-003 | DEV | CTRS-action 일관성 검증 | [!] | 구현됐으나 **호출자가 ctrs_level을 전달하지 않아 dead code** (ISS-030) |
| T1-F5-DEV-004 | DEV | PDF/JSON 듀얼 출력 | [!] | `report_renderer.py` 삭제(58db676) — **producer 부재, 회귀** |
| T1-F5-DEV-005 | DEV | /ai/handoff/generate 전체 파이프라인 연동 | [~] | 라우트 유효. 단 orchestrator 경로는 ISS-025(scale/risk 소실)+ISS-028(slot 9종 drop) |
| T1-F5-VER-001 | VER | VP-001 handoff 생성 + 검증 | [~] | 합성 slot 입력. 실데이터 재검증 = VER-007/008 |
| T1-F5-VER-002 | VER | VP-002 handoff (종단 포함) | [~] | 상동 |
| T1-F5-VER-003 | VER | VP-003 handoff (고위험) | [~] | 상동 |
| T1-F5-VER-004 | VER | VP-004 handoff (악화) | [~] | 상동 |
| T1-F5-VER-005 | VER | 12-section 완전성 자동 검증 | [x] | Stage 0에서 test_handoff_sections 복원 green |
| T1-F5-VER-006 | VER | Evidence citation 100% coverage | [x] | Stage 0에서 test_evidence_coverage 복원 green |

---

## Phase 2 — Stage 0 긴급 복구 (Gate G-0) — **완료 (2026-07-06, QA gate APPROVE-WITH-NOTES, DR-002)**

| ID | Type | 항목 | 상태 | 해결 이슈 |
|---|---|---|---|---|
| T1-F0-DEV-008 | DEV | 서버 부팅 복구: `trend_plotter` 복원 | [x] | ISS-024. `import src.main` OK, test_trend_plotter green |
| T1-F0-DEV-009 | DEV | ISS-020 fix 재적용: `_build_handoff_input`에 scale_scores/risk_events 인계 | [x] | ISS-025. 회귀 테스트 2/2 green, fix 브랜치와 line-identical |
| T1-F1-DEV-016 | DEV | Safety fail-closed 재수정 (`_fail_closed_result`, pre-adapter try/except, `_max_risk` ordinal 병합) + 회귀 테스트 6건 + ISS-038 칼로 키워드 anchoring | [x] | ISS-026/038. QA 독립 probe로 outage fail-closed 실증 |
| T1-F0-DEV-010 | DEV | 격리 테스트 복원: 22/23 파일 복원(12-slot 스키마 적응), pytest-timeout 설치+활성화 | [x] | ISS-033. 전체 354 passed. test_report_renderer만 잔류(대상 미복원, 소비자 0) |
| T1-F3-DEV-007 | DEV | ISS-042 수정: `routes/slots.py` dangling `safety_flag` 참조 제거 + 라우트 회귀 테스트 (QA gate 발견, 기존 결함 — 성공 응답이 500) | [x] | ISS-042. red-then-green 검증, 전체 356 passed |

## Phase 2 — F1 grounding 재검증 (Gate G-F)

| ID | Type | 항목 | 상태 | 해결 이슈 |
|---|---|---|---|---|
| T1-F1-DEV-017 | DEV | clinical_slot 프롬프트 v2 (예시 → placeholder, null 강제, risk 추론 금지) + placeholder-echo 거부 | [x] | ISS-027. `prompts/clinical_slot/v2.system.md`, PROMPT_VERSION=v2, test_prompt_v2 9건 |
| T1-F1-DEV-018 | DEV | **런타임 grounding filter** (`src/grounding.py` + f1.py merge 적용): 추출 slot은 근거 판정 통과 시에만 병합, risk_assessment는 추출기 경로 차단, ungrounded risk 상태 종료 금지 + 필수 SI screen | [x] | ISS-027. QA gate 우회 시도 7건 중 2건 발견 → ISS-044/045로 즉시 수정, 회귀 16건 추가. 445 tests green |
| T1-F1-DEV-019 | DEV | Coverage 재정의: grounded_coverage(질문가능 8-slot) 병행 보고, 0턴=실측 | [x] | ISS-034 |
| T1-F1-VER-014 | VER | Slot grounding 자동 감사 (`src.eval.grounding_audit`) + 07-03 런 4건 소급 판정 | [x] | 소급 확정: 4런 전부 날조 7건, 재계산 cov 0.25. 신규 8런 날조 0. `retro_audit_20260703/` (DR-003) |
| T1-F1-VER-010 | VER | 재현성: VP당 n>=3 반복 런, coverage/CTRS/turns/날조율 분산 보고 | [~] | n=2 완료 (8런, 날조 0). n>=3 확장 + 분산 보고 잔여 |
| T1-F1-VER-011 | VER | 장기 세션: 12턴 런, 반복 루프·피로도 검증 | [~] | 12턴 런 6건에서 반복 <=1 충족. SM-06 r1 종반 반복 루프 1건 (ISS-047) |

## Phase 2 — 프롬프트 아키텍처 v3 (Gate G-F 확장, `docs/ai/prompt_redesign_v3.md` v3.1) — 신규 2026-07-07

> 사양 근거: `discussion.md` PLAN-2026-W28(B1), REV-002(2026-07-07 non-blocking 종결), ADR-006, ADR-007. PRD 요약: `PRD_task1_v2.md` §11.

| ID | Type | 항목 | 상태 | 선행 조건 |
|---|---|---|---|---|
| T1-F1-DEV-024 | DEV | safety_classifier v2 프롬프트: 절대 규칙 5개 압축(ISS-046 관용구 규칙 신규 포함) + v1 핵심 규칙 7개 중 5개 verbatim 보존/2개 표 통합(삭제 0개) + ISS-048/050 신규 앵커 | [x] | `prompt_redesign_v3.md` §2.1. 구현 완료 — BUG-007(캘리브레이션 앵커 무언 삭제) 발견 후 수정, qa GATE:PASS(510 tests). 근거: DR-004 §2/§4, `error.md` BUG-007 |
| T1-F1-DEV-025 | DEV | dialogue v2 프롬프트: 절대 금지 8→6개 통합 + Safety 참고 섹션 5→1줄 축소(P12, 런타임 주입과 중복 제거) | [x] | `prompt_redesign_v3.md` §2.2. 구현 완료, qa GATE:PASS(510 tests). 근거: DR-004 §2 |
| T1-F1-DEV-026 | DEV | clinical_slot v3 프롬프트: 최우선 원칙 5개 hold(내용 불변) + risk_assessment 상호참조 노트는 프롬프트에 포함하지 않음(REV-002 #3 — 오케스트레이션 레벨로 이관, 코드 변경은 본 항목 범위 밖) | [x] | `prompt_redesign_v3.md` §2.3. 구현 완료(0개 규칙 삭제 확인), qa GATE:PASS(510 tests). 근거: DR-004 §2 |
| T1-F1-DEV-027 | DEV | handoff_generator v2 프롬프트: ctrs_level 출력 지시 삭제(ADR-007 옵션A) + P7 placeholder화 7곳(§6/§8/§9/§12) + evidence citation 단일 선언(§3/§5/§7 3회→1회) | [x] | `prompt_redesign_v3.md` §2.4, ADR-007. 구현 완료, qa GATE:PASS(510 tests). 근거: DR-004 §2 |
| T1-F1-DEV-028 | DEV | sentiment_analyzer v2 프롬프트: session 모드 섹션 전체 삭제(LLM 미호출 dead code 확인) + turn_index 예시 필드 제거 + evidence_phrase placeholder화 | [x] | `prompt_redesign_v3.md` §2.5. 구현 완료, qa GATE:PASS(510 tests). 근거: DR-004 §2 |
| T1-F1-VER-015 | VER | 오프라인 프롬프트 검증 테스트: placeholder-only/스키마 키/절대규칙 존재/char 예산/session모드 부재/ISS-050 문구/v1 7개 규칙 존치/citation 단일 선언 8개 단정문 자동 검사 | [x] | T1-F1-DEV-024~028. `tests/test_prompt_v3.py` 8개 단정문 구현·green, qa GATE:PASS. 근거: DR-004 §4, `error.md` BUG-007 |
| T1-F1-VER-016 | VER | 라이브 A/B 검증: Safety Matrix SM-01~06+SM-07a/SM-07b(8개 전건 필수) + VP-001~004 n>=2(n>=3 권장) A/B vs DR-003 베이스라인, 롤백 기준(날조>0/crisis miss/probe miss) 적용 | [~] | T1-F1-VER-015. EXP-002 실행 완료 — SM 매트릭스 9/9, VP-001~004 n=2/VP, §4.4 형식 기준 롤백 미발동(REV-003 non-blocking). 단 VP-003 crisis 재현 1/2은 critic이 **inconclusive**로 판정(VAL-001 harness 결함 개입 가능성 — `f1.py` 절 분리 결함) — "무회귀" 문구 사용 불가. Stage-2에서 VAL-001 수정 후 재실행 시 완결 예정. 근거: DR-004 §3/§4, `result.md` EXP-002, `discussion.md` REV-003 |
| T1-F1-DEV-029 | DEV | safety v3 프롬프트 ISS-049 정책 반영(ADR-010): 수동적 자살사고를 CTRS 3(Safety Probe 경유)에서 high/CTRS 2 즉시 위기 대응으로 상향(ADR-006 대체) — 위험 수준 표 + 간접 표현 CTRS 하한표 2개 행 갱신, 규칙 5 재작성, 부정 문맥(SM-02)/공황 관용구(SM-07a)/ISS-050 규칙 verbatim 보존, 자해-충동-단독(SM-04류)은 CTRS3→probe 유지. pin v2→v3, `_MEDIUM_KEYWORDS` 정합성 검토(테이블 변경 불필요로 판정, 근거는 코드 주석 명시), SM-03 판정(변경 없음), spec/board/checklist 정합 | [!] | **롤백됨 (ADR-012, EXP-003)** — 라이브 재검증(T1-F1-VER-016 계열)에서 SM-04a/SM-04b 프로브 회귀 발견: 자해-충동-단독 발화가 Safety Probe를 우회하고 turn 0에서 즉시 crisis로 상향(ADR-010 scope limit (c) 위반). PROMPT_VERSION pin은 v2로 되돌림; `v3.system.md`는 실패한 후보로 디스크에 보존, v4 재시도 대기. ADR-010 정책 자체는 유효, 구현만 롤백. 근거: `result.md` EXP-003, `discussion.md` ADR-012 |

## Phase 2 — Safety 심층 + 경로 통일 (Gate G-B, G-C)

| ID | Type | 항목 | 상태 | 해결 이슈 |
|---|---|---|---|---|
| T1-F1-DEV-022 | DEV | **Safety Probe 모드** (ISS-035 L1-L2): CTRS 3 + SI/self-harm category → 탐문 강제 주입 (빈도→계획→수단→보호요인), risk_floor latch | [x] | f1.py probe state machine, probe_events/risk_floor/session_ctrs 기록. LLM-free 테스트 12건 |
| T1-F1-DEV-023 | DEV | Safety Probe 승급/유지 (L2-L3): 재분류+lexical 이중 승급, 부인 시 grounded risk 기록 후 지속, cooldown(재발동 상한 2회) | [x] | ISS-043 clause-local 부정 수정 포함 (QA gate 발견 즉시 수정). 파이프라인 레벨 검증은 SM-04a/b 실행 배치 |
| T1-F1-VER-012 | VER | Safety 시나리오 매트릭스 (파이프라인 레벨, 스크립트 환자): 중간 턴 위기 전환, 부정 문맥, 간접·masked 표현, **CTRS3+자해사고 → probe 발동·승급·비승급 3분기**, 복약 순응 오탐 0 | [~] | 2026-07-06 실행: 최종 7/7 통과. SM-06 1회차 flake(ISS-047), 신규 오탐 클래스 ISS-046 발견 — 수정 후 재실행 시 [x] |
| T1-F0-DOC-005 | DOC | ISS-035 정책 문서화: `_simulation_spec.md` §4.4 조건부 위기 대응을 Safety Probe 프로토콜로 재정의 + safety prompt 간접표현 CTRS 하한표 방향 모호성 수정("N단계 이하(고위험 방향)") | [ ] | ISS-035, ISS-041 일부. **정정(2026-07-09, DR-008):** ISS-041의 `docs/ai/agents/` 차원(agent 문서 04/01 구버전 등)은 `PLAN-2026-W28-F`(T1-F0-DOC-006)로 resolved — 본 항목이 다루는 `_simulation_spec.md` §4.4 서브 항목은 별개이며 여전히 `[ ]` 미결 |
| T1-F1-DEV-020 | DEV | 위기 핫라인 상수 통일 (109/119/112): orchestrator.py·f1.py·verifier·테스트·문서 | [ ] | ISS-032 |
| T1-F0-DEV-005 | DEV | `SlotData`(handoff)를 canonical 12-slot으로 정합화 (S1) | [ ] | ISS-028 |
| T1-F0-DEV-006 | DEV | orchestrator.py slot 키 canonical 통일 + dialogue loop 내 ClinicalSlot 매 턴 호출 (S2) | [ ] | ISS-028/029 |
| T1-F0-DEV-007 | DEV | f1.py ↔ orchestrator.py coverage·종료조건·안전 실패 처리 단일화 (S4) — 단일 프로덕션 경로 결정 | [ ] | G8 |
| T1-F5-DEV-007 | DEV | EvidenceVerifier에 ctrs_level 전달 + 발화-보고서 일치(Check 8) 구현 + **CTRS 3 보고서에 "24-48시간 내 정신건강의학과 평가 권고" 문구 필수 검증** (ISS-035 L4) — slot 날조 탐지선 | [ ] | ISS-030, ISS-027/035 방어 |
| T1-F1-DEV-021 | DEV | followup handoff 생성기 수정: min-CTRS fallback 제거(위기 세션 CTRS 5 오기록), 12-section 표준 형식, 산출물 디스크 저장 | [ ] | ISS-036 |

## Phase 2 — 종단 프로토콜 + 기능별 파이프라인 (Gate G-A, G-D)

| ID | Type | 항목 | 상태 | 선행 조건 |
|---|---|---|---|---|
| T1-F1-DEV-014 | DEV | SentimentAnalyzer f1.py 루프 연동 (턴별 Mode A + 세션 Mode B, sentiment.json 저장) | [ ] | G-F |
| T1-F1-DEV-015 | DEV | 종단 세션 러너: S1→Q1→S2→Q2→S3 자동 실행, VP별 종단 데이터셋 축적 | [ ] | T1-F1-DEV-014, DEV-021 |
| T1-F1-VER-009 | VER | Follow-up 시뮬레이션: 4VP S2/S3 — prior 참조 정확성, 약물 조정 문답, delta 중심 질문 검증 | [ ] | T1-F1-DEV-015 |
| T1-F1-VER-013 | VER | Slot faithfulness: persona ground truth 대비 정량 평가 (필수 slot 정확도 >= 0.8) | [ ] | T1-F1-VER-009 |
| T1-F3-DEV-006 | DEV | `f3.py` 파이프라인: F1 산출물 → 척도 선택 rule → PatientLLM 문항 응답(또는 --mode expected) → 채점 → Q9 양성 시 Safety 재평가. survey.json 저장 | [ ] | G-0. **Superseded-by-redefinition (2026-07-12, `PLAN-2026-W28-V`/`ADR-031`)** — this row's scope (F1-artifact-driven scale selection + Q9→Safety-re-evaluation wiring) is not what `PLAN-2026-W28-V`'s F3 builds. The new, user-redefined F3 (F2-driven questionnaire administration, trigger = `ai_predicted_disease.recommended_questionnaire`, Q9 recorded via `safety_referral` only — never wires a safety re-evaluation call) is tracked under fresh IDs `T1-F3-DEV-008`~`013`/`T1-F3-VER-007`~`013` below. This row is kept as a historical scope record, not deleted or repurposed — see `docs/ai/PRD_task1_v2.md` §4.1 and `docs/ai/f3_quick_dev_plan.md` §0 for the full coexistence rationale. |
| T1-F3-VER-005 | VER | 실 F1 세션 기반 설문 선택 적합성 + Q9 연동 E2E | [ ] | T1-F3-DEV-006. **Superseded-scope note (2026-07-13):** the new F3 v2.1 path's own PHQ-9 item-9 deterministic safety-pathway (`ADR-033`, `PLAN-2026-W29-A` step 3) has been invoked in `EXP-018`/`EXP-019`/`EXP-020` but never triggered (no documented-positive item 9 in any battery to date) — this row's own Q9-E2E scope (the old, still-dead-code Survey Planner path) remains untouched and stays unchecked. See `T1-F3-VER-016` for the new path's safety-pathway-wiring status and `T1-F3-VER-015` for the item-8/whole-instrument recheck disposition. |
| T1-F3-VER-006 | VER | PHQ-4 subscale / WHO-5 변환 / AUDIT-C cut-off boundary 테스트 활성 스위트 확인 | [ ] | T1-F0-DEV-010 |
| T1-F4-DEV-005 | DEV | `f4.py` 파이프라인: 종단 데이터셋(t1..tN) → sentiment 집계 → TemporalSummary → temporal.json | [x] | **갱신(2026-07-13, `PLAN-2026-W29-D`):** 재정의·구현 완료 — 신규 `src/f4.py`(N-세션 종단 분석 엔진, zero-LLM, commit `75472ee`)가 SentimentAnalyzer는 **재사용**(세션별 재호출이 아니라 F1이 이미 저장한 Mode A/B 산출물을 소비)하고 `agents/temporal_summary.py`의 pairwise comparator를 N-1회 반복 재사용하는 방식으로 구현됨 — 원 항목명의 "sentiment 집계 → TemporalSummary" 순서보다 넓은 범위로, F1(slot/CTRS/crisis/probe/sentiment)+F2(domain/disease-similarity trend)+F3(item/total/band) 세션 데이터 전체를 통합한다(`f4_quick_dev_plan.md` §1.1). qa **GATE:PASS**(suite 1669 passed/2 skipped; `BUG-041`/`BUG-042` mutation-gap 발견·동일 세션 해결). 상세: `docs/ai/f4_checklist.md`. 근거: `discussion.md` ADR-036 item 1, `error.md` BUG-041/BUG-042(resolved) |
| T1-F4-VER-005 | VER | 실 종단 데이터 direction: VP-002 improved / VP-004 worsened / 초진 unknown | [x] | **갱신(2026-07-13, `EXP-023`):** 원 항목의 VP-002/VP-004 대신 VP-001/VP-003(`f4_quick_dev_plan.md` §2.2 justified pick)로 실행 — VP-001 `overall_direction=improved`(PHQ-9 22→17, severe→moderately_severe), VP-003 `overall_direction=worsened`(`session_ctrs`-carried, `phq9_total=unknown` n=1). 초진(session 1) "unknown" 반환 자체는 이번 N-세션 배터리 범위 밖(레거시 pairwise 케이스, `TemporalSummaryAgent`가 무변경으로 계속 담당). qa step-8 독립 재계산 정확 일치. `REV-045`(critic) evidence-sound-with-conditions — 사전등록 Criteria 0/0b/1 전건 PASS. **필수 병기**(`REV-045` wording-table row 2): VP-001의 `improved`는 `overall_direction`에 sentiment를 포함하는 결합규칙에 outcome-determinative하게 의존 — sentiment 제외 시 `unchanged`로 뒤집힘(원칙적·outcome-blind 선택이나 공시 필수). **금지:** "임상 상태 변화 탐지/검증" 서술, 민감도/특이도 주장(비-각본 비교군 없음, `REV-044` circularity ruling). 상세: `docs/ai/f4_checklist.md`. 근거: `result.md` EXP-023, `discussion.md` REV-045 |
| T1-F4-VER-006 | VER | plot_data 실데이터 무결성 (점수 출처 추적) | [x] | **갱신(2026-07-13, `EXP-023`):** qa Check 5 — VP-001/S1 disease/domain candidate가 raw `domain_inference.json`과 exact match; VP-003의 disease-similarity PNG 결측은 정직한 결과(`min_sessions=2` recurrence filter — disease-bearing 세션이 1개뿐이라 재현 0건, 결함 아님, `REV-045`가 코드 직접 확인으로 독립 재검증). **범위 caveat**(`REV-045` Issue 3, minor): "모든 plotted point" 전수 검증이 아니라 표본 검증 + 결측 정직성 확인 수준. 상세: `docs/ai/f4_checklist.md`. 근거: `result.md` EXP-023, `discussion.md` REV-045 |
| T1-F5-DEV-006 | DEV | `f5.py` 파이프라인: 12-slot→SlotData 매핑, risk_events 추출(CTRS<=3 턴), verifier 루프, handoff.md/json 저장. f1 --followup-from이 이 산출물을 우선 소비 | [ ] | T1-F0-DEV-005 |
| T1-F5-VER-007 | VER | 실데이터 handoff: Section 9 종단 변화 = F4 출력 일치 | [ ] | T1-F5-DEV-006, T1-F4-VER-005 |
| T1-F5-VER-008 | VER | Evidence 원문 추적성: Section 12 registry ↔ 대화 원문 자동 대조 | [ ] | T1-F5-DEV-006 |
| T1-F2-DEV-005 | DEV | `f2.py` 검증 파이프라인 구현(f1.py 패턴): 입력 로드 → Stage1 코드검색(실패 시 `mode=llm_only` 강등) → Stage2 DomainInferenceAgent LLM → 근거-화이트리스트 검사(T1-F2-DEV-002) → 산출물(`{VP}_{ts}_domain_inference.json`+report.md) → 콘솔 요약 | [x] | T1-F2-DEV-001, T1-F2-DEV-002, T1-F2-CFG-002. PRD §3.2. 구현 완료: `src/f2.py`. EXP-004 라이브 배치로 실행 확인(8/8 런, llm_only arm). 근거: DR-006 §2, `result.md` EXP-004 |

## Phase 2 — F2 도메인 추론(DomainInferenceAgent) 구현 (PLAN-2026-W28-C 승인, ADR-013) — 구현·llm_only 라이브 배치 완료, risk≠domain 위반으로 비인증 처분(ADR-014) — 2026-07-08

> 사양 근거: `discussion.md` PLAN-2026-W28-C(Step 2 C-1~C-5, Step 3 REV-006, 2026-07-08 승인 상태 업데이트, 구현 미션 REV-007/REV-008), ADR-013/ADR-014, REV-006(구속 조건 1/2/3/4/5/6). PRD: `PRD_task1_v2.md` §3.7, §3.8. 구현 미션 완료 보고: `development_report.md` DR-006.

| ID | Type | 항목 | 상태 | 선행 조건 |
|---|---|---|---|---|
| T1-F2-CFG-002 | CFG | `DATABASE_URL`/`ENCRYPTION_KEY` env 배선 + DB 연결성 프리플라이트(SELECT 1/스모크, 쓰기 없음) | [~] | ADR-013 (4). PLAN-2026-W28-C C-1 — DB 프로브 FAIL 확인됨(`.env`에 `DATABASE_URL` 부재), 구현 착수 선행조건. **부분(2026-07-08):** 프리플라이트 스모크 메커니즘 자체는 구현·검증됨(`SELECT 1`, 10s timeout, 결과 정직 보고 — `experiments/EXP-004/preflight.log`). `DATABASE_URL`/`ENCRYPTION_KEY` env 배선은 여전히 미완(`.env`에 `DATABASE_URL` 키 부재, grep 0건) — RAG arm 차단의 직접 원인. 근거: DR-006 §2 |
| T1-F2-SEC-001 | SEC | rag 라우터(`POST /ai/rag/grounding` 등) env 기반 bearer/API-key 인증 적용 + `rag_chat.py` 하드코딩 공인 IP/환자 UUID → env화 | [x] | VAL-005 옵션(a) 채택, ADR-013 (1). 구현 완료·검증됨: `src/rag/auth.py`(fail-closed 503) + `rag_chat.py` 하드코딩 IP/UUID 제거. `tests/rag/test_route_auth.py` 6 cases, qa GATE:PASS. git 이력 정리(옵션 (b))는 별도 미결(ADR-013(3)). 근거: DR-006 §4 |
| T1-F2-SEC-002 | SEC | `retrieval.py` `_dec()` 복호화 갭 수정: crypto.py decrypt 구현 + `situation_encrypted` 실복호화 적용, `ENCRYPTION_KEY` 부재 시 해당 필드 제외 + 명시 로깅 | [x] | VAL-005 cross-ref (S2), ADR-013 (2). 구현 완료·검증됨: `crypto.py::decrypt_str`(AES-GCM), `retrieval.py::_dec()` 실패 시 필드 제외. `tests/rag/test_crypto.py` 7 cases. 부수 발견: `load_simulations.py`의 사전 존재 결함(BUG-012, situation 필드 평문 기록)은 본 항목 범위 밖(retrieval.py 측은 정상 동작). 근거: DR-006 §4 |
| T1-F2-SEC-003 | SEC | `apps/ai-server/src/rag/` 모킹 기반 유닛 테스트 커버리지 신규 | [x] | VAL-005 cross-ref (S3), ADR-013 (2). 구현 완료: `tests/rag/` 신규 모킹 테스트 26건(이전 0건). 근거: DR-006 §4 |
| T1-F2-VER-005 | VER | 골든 라벨 DATASET 저작(VP-001~004, 다중-라벨 집합 — 예: VP-001={anxiety,sleep}) — persona 전문에서 blind 도출, `rag_chat.py` 비공식 태그 비의존 명시 + critic 승인 | [x] | REV-006 조건(2), PLAN-2026-W28-C C-4 — 라이브 실행(T1-F2-VER-001~003) 선행조건. 완료: DATASET-004 **APPROVED with amendments**(REV-007 Part A, 2026-07-08). 근거: `discussion.md` DATASET-004/REV-007 |
| T1-F2-VER-006 | VER | 라이브 n≥2/VP 배치 실행, `llm_only`/RAG arm 구분 라벨 — DB 프리플라이트 실패 시 RAG-arm 검증 런 중단·보고(silent `llm_only` 강등 금지) + `latency_ms` 캡처·p95 서술 보고(REV-006 조건 5) | [~] | REV-006 조건(6), ADR-013 (4). 선행: T1-F2-CFG-002, T1-F2-VER-005. **부분(2026-07-08, EXP-004):** llm_only arm 완료(VP-001~004 n=2, 8런). RAG arm은 당시 DB 프리플라이트 FAIL(`DATABASE_URL` 미설정)로 BLOCKED-awaiting-DB(ADR-013(4)) — 미실행, silent 강등 아님. **갱신(2026-07-08, `PLAN-2026-W28-E` 착수):** 본 개발 워크스테이션에서 외부 포트 개설 완료로 DB 연결이 라이브로 검증됨(conductor 확인 — `SELECT 1` OK, pgvector 설치, corpus 행수 `rag.case_card` 1,248/`rag.qa` 1,789/`rag.symptom` 40/`rag.disease` 26) — **BLOCKED-awaiting-DB 해제**, RAG arm은 `EXP-005`에서 실행 예정(T1-F2-VER-009). 상태는 `[~]` 유지(RAG arm 자체는 v2 remediation 게이트 통과 후 실행 — 아직 미실행). 근거: `result.md` EXP-004, DR-006 §2, `discussion.md` PLAN-2026-W28-E |
| T1-F2-VER-007 | VER | 근거 요약 grounding 감사: utterance→`has_lexical_evidence()` 재사용, rag_chunk→`chunk_ids` 멤버십 + quote↔청크 본문 어휘 대조 — fabrication=0 하드 게이트 | [x] | REV-006 조건(1), PLAN-2026-W28-C C-3/C-4 — 하드 게이트, 위반 시 프롬프트 버전 롤백. **완료(하드 게이트 자체는 충족, 2026-07-08 EXP-004):** fabrication 0/32 evidence entries(정정치, VAL-008), whitelist reject 0건 — 본 항목 고유 범위(evidence 근거성 검사)는 위반 없음. **단, 별도 규칙인 risk≠domain 절대 규칙(prompt rule 2)이 VP-003 2/2 런에서 라이브 위반 확인**(VAL-006/REV-008) — 롤백 트리거 발동, `domain_inference` v1 **비인증** 처분(ADR-014). 근거: `result.md` EXP-004, `error.md` VAL-006/VAL-008, `discussion.md` REV-008/ADR-014 |

## Phase 2 — F2 v2 remediation → RAG-arm 검증 (`PLAN-2026-W28-E`) — 완료(llm_only arm 비인증 해제 조건부; RAG arm EXPERIMENTAL 잔류), 2026-07-08

> 사양 근거: `discussion.md` PLAN-2026-W28-E, ADR-014, REV-008, VAL-006. PRD: `PRD_task1_v2.md` §3(v2.3 갱신). 목표: VAL-006/REV-008 해소(코드 강제 risk-lexicon evidence filter + prompt v2) 후 `EXP-005` 양 arm(llm_only + RAG) 재검증, ADR-014 비인증 해제 여부는 critic이 판정한다(본 문서는 판정을 선취하지 않는다). **갱신(2026-07-08, DR-007):** 판정 완료 — critic REV-010 채택, `ADR-015`: llm_only arm 비인증 해제(조건부, 상시 수동 taxonomy 감사), RAG arm은 EXPERIMENTAL 잔류(결함 2건 + VAL-010 상시화 미비, 아래 각 행 참조). RAG arm에 대해 "인증"/"통과" 문구는 사용하지 않는다.

| ID | Type | 항목 | 상태 | 선행 조건 |
|---|---|---|---|---|
| T1-F2-DEV-006 | DEV | `f2_grounding.py` 위험-어휘 필터(risk-lexicon evidence filter) 코드 강제 구현 — evidence quote가 프로젝트의 passive-SI/자해/burdensomeness 어휘(BUG-007/ADR-010 taxonomy)를 포함하면 해당 evidence 수용 거부(reject), 수용 거부→후보 탈락 동작 명시 테스트 | [x] | ADR-014 (2), REV-008. PLAN-2026-W28-E Step 1. **완료(2026-07-08):** `src/eval/f2_grounding.py`에 필터+cascade(수용거부→후보탈락) 구현, 3회 게이트 반복(BUG-014 어휘 커버리지 부족, BUG-015 과대차단 발견·수정) 끝에 qa 최종 GATE:PASS(스위트 687) — EXP-004 실제 아티팩트 재생 시 13/13 광의 taxonomy 인용문 전량 차단 확인. 근거: `discussion.md` PLAN-2026-W28-E, `error.md` BUG-014/BUG-015(resolved), `development_report.md` DR-007 §2 |
| T1-F2-DEV-007 | DEV | domain_inference 프롬프트 v2 — v1 보존, rule 2(위험≠도메인) 문구 강화, **ISS-046 공황 관용구 예외 유지**, VP-003 실패 fixture 추가 | [x] | ADR-014 (2), PLAN-2026-W28-E Step 1. **완료(2026-07-08):** `docs/ai/prompts/domain_inference/v2.system.md` 작성, v1 보존, `EXP-005`에서 pin으로 사용 확인(84줄; 자수는 문서 간 불일치 있음 — DR-007 §2 참조). 근거: `result.md` EXP-005 Setup, `development_report.md` DR-007 §2 |
| T1-F2-VER-008 | VER | **EXP-005** llm_only arm 재검증 — VP-001~004 n≥2, REV-008 위반(위험 표현의 domain evidence 인용) 재발 0 확인 | [x] | T1-F2-DEV-006/007, qa GATE:PASS + critic 사전리뷰(1g). PLAN-2026-W28-E Step 1g/2. **완료(2026-07-08, EXP-005):** llm_only arm 8런(4VP×n=2) — REV-008 위반 재발 0/42(accepted 인용문 전수 수동감사, critic REV-010). top-1/top-3 7/8(VP-003/run2는 100% 위험-근거로 정당 탈락 — intended-cost, 회귀 아님). critic REV-010 채택 → `ADR-015` (1) llm_only 비인증 해제(상시 수동감사 조건부). 근거: `result.md` EXP-005, `discussion.md` REV-010/ADR-015, `development_report.md` DR-007 §3/§4 |
| T1-F2-VER-009 | VER | **EXP-005** RAG arm 검증 — DB 프리플라이트→n≥2, `mode=rag` 확인, fabrication=0, DATASET-004 채점, latency 캡처 | [~] | T1-F2-VER-008, DB 연결 라이브 검증 완료(§3.8 갱신 참조). PLAN-2026-W28-E Step 2. **실행·측정 완료, 인증 아님(2026-07-08, EXP-005):** RAG arm 8런, DB 프리플라이트 PASS(corpus 델타 0), `mode=rag` 8/8, fabrication 0/35(명목 10건은 BUG-016 source_id 포맷 결함으로 판명 — content fabrication 아님), top-1 5/8·top-3 6/8, latency p50 11666ms/p95 19218ms(서술 보고만). **RAG arm은 `ADR-015` (2)에 따라 EXPERIMENTAL로 잔류한다** — 잔류 사유: BUG-016(`chunk_id=` 포맷 과잉거부 ~29%), BUG-017(VP-003 RAG 2/2 LLM 출력 실패), VAL-010(risk_assessment-as-query 채널 상시 완화 미비). 이 3항목 해소가 EXPERIMENTAL 해제 경로다. 근거: `result.md` EXP-005, `discussion.md` REV-010/ADR-015, `error.md` BUG-016/BUG-017/VAL-010, `development_report.md` DR-007 §3/§4/§5 |

## Phase 2 — F2 RAG-arm remediation & certification (`PLAN-2026-W28-G`) — BUG-016 resolved; BUG-017 diagnosed truncation mechanism resolved (practical symptom persists as new BUG-019); RAG-arm certification NOT LIFTED (ADR-016) — 2026-07-09

> Source: `discussion.md` PLAN-2026-W28-G, REV-011 (pre-run gate), REV-012 (evidence review), ADR-016. PRD: `PRD_task1_v2.md` §3 (v2.5). Target: close the three RAG-arm defects `ADR-015` (2) named as the certification path (`BUG-016`, `BUG-017`, `VAL-010`) and re-verify VP-003 RAG cleanly via `EXP-006`. Outcome: 2/3 conditions met (`BUG-016` resolved, `BUG-017`'s diagnosed truncation mechanism resolved), 1/3 not met (VP-003 RAG clean n=2 re-verification) — critic `REV-012` ruled the RAG arm's `ADR-015` EXPERIMENTAL/UNCERTIFIED status is **not lifted**; orchestrator adopted this ruling as `ADR-016`. No "인증"/"통과"/"검증됨" wording is used for the RAG arm anywhere in this section. The `llm_only` arm's existing certification (`ADR-015` (1)) is unaffected.

| ID | Type | Item | Status | Preconditions/notes |
|---|---|---|---|---|
| T1-F2-DEV-008 | DEV | `BUG-016` fix: `check_evidence` normalizes a `chunk_id=` source_id prefix before the `chunk_texts` lookup (deterministic, code-level) + prompt-wording defense-in-depth | [x] | `ADR-015` (2) condition 1. Completed 2026-07-09: `src/eval/f2_grounding.py::_normalize_rag_chunk_source_id()`, `rag_chunk`-scoped only, mutation-checked non-vacuous by qa. Confirmed live at n=32 (0/32 `rejected_unknown_source` this batch vs `EXP-005`'s 10/35 ~29%). Source: `error.md` BUG-016 (resolved), `result.md` EXP-006 |
| T1-F2-DEV-009 | DEV | `BUG-017` phase A (`finish_reason`/`usage` capture+logging+schema/artifact persistence) + phase B (`max_tokens` 1536→4096, single-variable) | [x] | `ADR-015` (2) condition 2. Completed 2026-07-09: qa GATE:PASS both phases (suite 707). W4a diagnostic confirmed the truncation hypothesis (`finish_reason="length"`, `completion_tokens==1536` exactly at pre-fix ceiling); post-fix, 0/16 certification-batch runs show `finish_reason="length"`. **Diagnosed mechanism resolved — the BUG as a whole stays open** (see T1-F2-VER-011: practical symptom persists via `BUG-019`, a distinct, undiagnosed failure mode). Source: `error.md` BUG-017 (open), `result.md` EXP-006 |
| T1-F2-DEV-010 | DEV | `VAL-010` code mitigation: `_STAGE1_QUERY_SLOTS` narrowed to `(chief_complaint, history_of_present_illness)` — `risk_assessment` excluded | [x] | `ADR-015` (2) condition 3. Completed 2026-07-09, critic `REV-011` §2: correctly and deterministically implemented, zero legitimate clinical signal lost. **Does not resolve the underlying concern for VP-003** — `chief_complaint`/HPI are themselves risk-worded by persona design for this persona and are unfiltered; reproduces live for a 2nd consecutive batch (`EXP-005`, `EXP-006`). Standing `retrieval_meta.queries` audit continues unconditionally (`REV-011` §3 rule ii, `REV-012` §5). Source: `error.md` VAL-010 (open), `discussion.md` REV-011 §2/§5, REV-012 §5 |
| T1-F2-VER-010 | VER | `BUG-017` diagnostic run (W4a): single VP-003 RAG call under pre-fix `max_tokens=1536`, reading `finish_reason`/`usage` to confirm/falsify truncation before spending the full certification batch budget | [x] | Sequenced before T1-F2-VER-011 per `REV-011` §1's positive finding (sound experimental design). Completed 2026-07-09: `finish_reason="length"`, `completion_tokens=1536` exactly at ceiling — truncation confirmed, licensing phase B (T1-F2-DEV-009). Source: `result.md` EXP-006 "W4a — BUG-017 diagnostic" |
| T1-F2-VER-011 | VER | `EXP-006` certification batch: llm_only + RAG arm, VP-001~004 n=2 each (16 runs), mode verified, secondary taxonomy audit, DATASET-004 scoring, latency — RAG-arm `ADR-015` certification disposition | [~] | T1-F2-DEV-008/009/010, qa GATE:PASS + critic `REV-011`. **Completed and measured, certification NOT LIFTED at the time of this batch (2026-07-09, `EXP-006`/`REV-012`/`ADR-016`):** rollback trigger does not fire (0/49 accepted quotes match the broader taxonomy, 14 primary-counter interceptions); `BUG-016` confirmed resolved live (0/32); llm_only arm replay unchanged (top-1/top-3 7/8, intended-cost, same `VP-003/run2` miss as `EXP-005`) — this arm's own `ADR-015` certification is unaffected. **RAG arm: VP-003 RAG remains 0/2 interpretable** (new schema-validation-failure mode, distinct from `BUG-017`'s diagnosed truncation — tracked as `BUG-019`), and a previously-clean persona (`VP-001/run1`) newly failed with the identical signature — RAG-arm interpretability is **5/8 clean this batch (3/8 LLM-output failures), down from `EXP-005`'s 6/8 clean/2/8 failed**. RAG top-1/top-3 = 5/8 (raw counts; all 3 misses are 0-candidate LLM-output-failure outcomes, not accuracy misses). critic `REV-012`: "RAG 잔류 EXPERIMENTAL — 2/3 인증조건 충족(BUG-016 해소, 진단된 절단 메커니즘 해소), 1/3 미충족(VP-003 RAG 클린 재검증)" — a partial/per-persona certification is explicitly not licensed. Orchestrator adopted this ruling as `ADR-016`. Source: `result.md` EXP-006, `discussion.md` REV-012/ADR-016, `error.md` BUG-016 (resolved)/BUG-017 (open)/BUG-019 (open, new)/VAL-010 (open). **Superseding status note (2026-07-09, `T1-F2-VER-012` below): the "NOT LIFTED" disposition recorded here was this batch's own accurate result and stands as history, but it is superseded going forward — a later batch (`EXP-008`) and critic's authoritative evidence review (`REV-015` Part A) LIFTED the RAG-arm certification in full (VP-003 and VP-001); orchestrator adopted this as `ADR-018`, superseding `ADR-016`'s EXPERIMENTAL disposition.** Do not read this row's "NOT LIFTED" text as the current disposition — see `T1-F2-VER-012` for the current, LIFTED state. Per `REV-015` §A.7: the lift does not close `VAL-010` (stays open in `error.md`) and does not retire the secondary taxonomy + `retrieval_meta.queries` audits, which remain mandatory standing conditions on every future RAG-mode batch. |
| T1-F2-VER-012 | VER | RAG-arm certification precondition (successor mission): diagnose `BUG-019` (persist raw LLM response / `ValidationError.errors()` on schema-validation failure) → root-cause → fix → clean VP-003 (and VP-001) RAG n=2 re-verification passing the secondary taxonomy audit | [x] | `ADR-016`. **Complete (2026-07-09, `EXP-008`/`REV-015`/`ADR-018`):** the diagnose-and-fix sub-scope was already complete and code-verified — `T1-F2-DEV-012` (Wave-3 instrumentation), `T1-F2-VER-013` (`EXP-007` live diagnostic), `T1-F2-DEV-013` (root-cause fix `_normalize_source_type_collision`). **The remaining blocker — a live, clean VP-003 (and VP-001) RAG n=2 re-verification passing the secondary taxonomy audit — is now DONE:** `EXP-008` (4 genuinely-new live RAG calls, VP-003 n=2 + VP-001 n=2) produced 4/4 interpretable outcomes per critic's pre-registered decision rule (`REV-014`) — 3 non-empty grounded (VP-003/run1, VP-001/run1, VP-001/run2) + 1 legitimate 0-candidate cascade outcome (VP-003/run2, correctly distinguished from a failure), 0 JSON-parse or schema-validation failures. Secondary taxonomy audit: 0/42 accepted quotes match the broader risk taxonomy — rollback trigger does not fire. Critic's authoritative evidence review (`REV-015` Part A) independently re-derived every number and ruled **LIFT, in full** (VP-003 and VP-001, not partial/per-persona) — orchestrator adopted this as `ADR-018`. `BUG-016`/`BUG-017`/`BUG-019` all disposed "may CLOSE" per `REV-015` §A.5 (qa to perform the formal tracker updates citing `EXP-008`/`REV-015`). **Standing conditions that survive the lift (not one-time gates):** the secondary taxonomy audit and `retrieval_meta.queries` audit remain mandatory on every future RAG-mode batch; `VAL-010` stays OPEN in `error.md` — the lift does not close it, only its standing-audit discipline was ever a certification precondition (`REV-015` §A.7). Source: `error.md` BUG-019 (→resolved), `result.md` EXP-008, `discussion.md` REV-014/REV-015/ADR-018, `development_report.md` DR-011 §1 |

## Phase 2 — Agent spec 문서 총검증·정비 (`PLAN-2026-W28-F`) — 완료, 2026-07-09

> 사양 근거: `discussion.md` PLAN-2026-W28-F, `docs/ai/development_report.md` DR-008, `error.md` BUG-018. 사용자 지시(원문 인용): "그럼 docs/ai/agents에 agent spec 추가해야지 않니? issue 총검증 하라". 목표: `docs/ai/agents/` 스펙 14종 ↔ 구현(src/agents + f1/f2) ↔ 프롬프트(현재 pin) 3-way 전수 감사, 신규 `14_domain_inference.md` 저작, 드리프트 스펙 정정, qa 사실검증 게이트. 코드/프롬프트/`agent_model_registry.yaml` 무변경(docs-only 미션).

| ID | Type | 항목 | 상태 | 근거/비고 |
|---|---|---|---|---|
| T1-F0-DOC-006 | DOC | `docs/ai/agents/` 스펙 3-way 감사(스펙↔코드↔프롬프트, file:line) + 신규 `14_domain_inference.md`(as-built, placeholder-only) 저작 + 기존 11개 스펙 as-built 정정(00/01/02/04/05/06/07/08/09/11/13) + qa 사실검증 게이트 | [x] | PLAN-2026-W28-F. Stage 1 developer 3-way 감사(file:line) → Stage 2 writer 신규 저작+정정(12 files) → Stage 3 qa **GATE: PASS-WITH-NOTES**(registry drift 신규 발견 → BUG-018) → Stage-2 미접촉 기존 오류 3건(02/11/01) bounce 정정 → qa 재게이트 **GATE: PASS**. ISS-041의 docs/ai/agents/ 차원은 resolved(잔존 3항목은 code/타 문서 scope, out of scope). 근거: `development_report.md` DR-008, `error.md` BUG-018 |

## Phase 2 — PLAN-2026-W28-H: Track A RAG-HTTP retirement, Track B AI-predicted-disease entity, Track C continuous_test.py, Track D DB handoff, BUG-019 diagnosis+fix — 2026-07-09

> Source: `discussion.md` PLAN-2026-W28-H, ADR-016, ADR-017, REV-012 §6, REV-013; `result.md` EXP-007; `error.md` BUG-019 (Wave-3/Wave-6 qa subsections), BUG-017, VAL-005; `development_report.md` DR-010; `PRD_task1_v2.md` §3.8/§3.9 (v2.6). No "인증"/"통과"/"검증됨" wording is used for the RAG arm anywhere in this section — `BUG-019` stays open pending a live clean VP-003/VP-001 RAG n=2 re-verification (`ADR-016` condition 2); the `llm_only` arm's certification (`ADR-015` (1)) is unaffected.

| ID | Type | Item | Status | Preconditions/notes |
|---|---|---|---|---|
| T1-F2-SEC-004 | SEC | Track A: RAG HTTP API permanent descope — **full deletion** (user-directed, supersedes the earlier RETIRE/unmount disposition): `src/rag/route.py`, `src/rag/auth.py`, `tests/rag/test_route_auth.py` deleted from the filesystem (not merely unmounted/retired-in-tree), `main.py` carries no `rag_router` reference, `rag_chat.py` refactored to call `retrieve_grounding()` in-process, `NS_RAG_API_KEY`/`NS_RAG_DEV_MODE`/`NS_RAG_URL` removed from `.env.example`+`.env` | [x] | User decision (permanent, categorical — no HTTP-served RAG now or at deployment; user verbatim: "auth.py 파일을 안쓸거면 삭제하면되는거아니냐? RAG API 관련 기능 모두 없애라"). qa live-verified 2026-07-09 (post-deletion): `POST /ai/rag/grounding` → 404 (route absent — no code exists to remount), `/health` → 200, zero rag-prefixed mounted paths, suite 760 passed (768 minus the 8 deleted route-auth tests). `VAL-005` structurally resolved (route object itself no longer exists — strictly stronger than the prior RETIRE-unmounted disposition, critic `REV-015` Part B independently confirmed by static code/filesystem evidence). In-process RAG core (`retrieval.py`/`embed.py`/`crypto.py`/`ontology.py`) and `UPSTAGE_API_KEY` confirmed untouched/disjoint. Rollback safeguard strengthened: any future RAG-HTTP reversal requires a fresh ADR + re-implementing bearer auth from scratch (no code to reactivate) + a fresh VAL-005-class review, before any route is mounted. Source: `error.md` VAL-005, `discussion.md` ADR-017/ADR-019/REV-013 §2/REV-015 Part B, `development_report.md` DR-010 §2/DR-011 §2 |
| T1-F2-DEV-011 | DEV | Track B: AI-predicted-disease container schema (`AIPredictedDiseaseCandidate`/`AIPredictedDiseaseOutput`) + `f2.py` sibling-key wiring (`mode="experimental_unpopulated"`) — `similarity_score` labeling, `is_diagnostic: Literal[False]`, no softmax across top-5 | [x] | `REV-013` §4 binding labeling ruling; §3 binding isolation conditions. Container built now; live auto-population gated on RAG-arm certification (`ADR-016`, unchanged EXPERIMENTAL/UNCERTIFIED). Source: `discussion.md` PLAN-2026-W28-H Track B, REV-013 §3/§4, `development_report.md` DR-010 §3 |
| T1-F2-VER-014 | VER | Track B: qa adversarial HPI-isolation test suite (`tests/test_hpi_isolation.py`, 9 tests: `AgentInput.extra` channel ×3, `conversation_history` call-graph ×4, live-behavior container ×2) + labeling assertions (`Literal[False]` type, no `probability`/`confidence`/softmax) | [x] | `REV-013` §3 condition 1 (binding precondition for "structurally isolated" wording) — MET. 9/9 pass, mutation-checked non-vacuous (2 in-memory regressions simulated, both correctly caught, reverted). Open non-blocking follow-up: `extra="forbid"` type-layer invariant not implemented this wave (does not weaken this claim — 9 tests pass without it). Source: `error.md` BUG-019 "Track B" subsection, `discussion.md` REV-013 §3/§4, `development_report.md` DR-010 §3 |
| T1-F0-DEV-011 | DEV | Track C: `continuous_test.py` — F1→F2→…→F6 chaining harness, extensible stage registry, explicit no-op stubs for unimplemented F3–F6 (never silent skip), live local DB in-process (`.env`-driven), read-only, safe to re-run | [x] | Not wired into `orchestrator.py`'s 11-state production machine (verification harness only, `f1.py`/`f2.py` pattern). qa verified `_mask_dsn()` and confirmed no password-leak path (live connection test, password absent from raised exception `str()`). Source: `error.md` BUG-019 "Track C" subsection, `discussion.md` PLAN-2026-W28-H Track C, `development_report.md` DR-010 §4 |
| T1-F0-DOC-007 | DOC | Track D: DB engineer handoff notes appended to `development_report.md` (DR-010 §6/§6b) — connection facts (password never stated), schema/migration recommendations (conditional on AI-disease persistence), `rag.*` row-count/embedding state, pgvector conventions, `continuous_test.py` DB needs, live-fill gating | [x] | Grounded in `data`'s DATASET-005 provenance review + developer's DB-connection code — no fabricated DB facts. Source: `development_report.md` DR-010 §6/§6b |
| T1-F2-DEV-012 | DEV | `BUG-019` Wave-3 instrumentation: `DomainInferenceAgent._parse` returns `(parsed, reason, validation_errors)`; raw response + field-level `ValidationError.errors()` persisted through `DomainInferenceOutput` into the artifact JSON and a new `report.md` diagnostics section (additive, no change to the clean-success case) | [x] | Precedent: `BUG-017` phase A's `finish_reason`/`usage` persistence. qa GATE:PASS (instrumentation dimension), suite 758. Source: `error.md` BUG-019 "Wave-3 instrumentation" subsection, `development_report.md` DR-010 §5 |
| T1-F2-VER-013 | VER | `EXP-007` — BUG-019 live diagnostic capture: VP-003/VP-001 RAG, n=4, raw response + field-level `ValidationError` detail captured; root-cause hypothesis grounded (`RetrievedChunk.source_type` DB-table-origin value colliding with `DomainEvidence.source_type`'s schema enum via the prompt's chunk-listing format) | [x] | 2/4 reproduced the symptom, 2/4 did not (1 legitimate cascade-elimination, 1 unexpected success) — non-determinism confirmed on byte-identical EXP-006 inputs. Diagnostic capture only, explicitly not a certification batch (no `llm_only` arm, no VP-002/VP-004, no clean-n=2 claim). Source: `result.md` EXP-007, `discussion.md` PLAN-2026-W28-H §順序(a), `development_report.md` DR-010 §5 |
| T1-F2-DEV-013 | DEV | `BUG-019` root-cause fix: `_normalize_source_type_collision` (regex-coerces a `case_card`/`qa` table-origin value colliding with `DomainEvidence.source_type` to `"rag_chunk"`, strictly pre-Pydantic-validation) | [x] | Wave 6, evidence-gated on `T1-F2-VER-013`. qa GATE:PASS: diff-scope confirmed to exactly 2 files, mutation-checked (6/10 new tests failed under a no-op mutation, confirming load-bearing), narrowness confirmed (does not mask `BUG-016`-shaped or genuinely-malformed values), suite 768. **`BUG-019` itself stays open** — code-verified only, no live re-verification performed this item. Source: `error.md` BUG-019 "Wave 6" subsection, `development_report.md` DR-010 §5 |

**Status update on `T1-F2-VER-012` (unchanged ID, status revised — not a new entry):** `[ ]` → `[~]`. The diagnose-and-fix half of this item's scope is now complete and code-verified (`T1-F2-DEV-012`/`013`, `T1-F2-VER-013` above); the item's remaining scope — a live, clean VP-003 (and VP-001) RAG n=2 re-verification passing the secondary taxonomy audit — has not been executed and remains this item's sole open blocker (`ADR-016` (4)). Source: `error.md` BUG-019 "Wave 6" subsection, `development_report.md` DR-010 §5.

**Further status update on `T1-F2-VER-012` (2026-07-09, `PLAN-2026-W28-I` TASK 2 — supersedes the note directly above):** `[~]` → `[x]`. The sole remaining blocker noted above — a live, clean VP-003 (and VP-001) RAG n=2 re-verification passing the secondary taxonomy audit — was executed as `EXP-008` (4/4 interpretable, genuinely-new live calls) and adjudicated by critic's authoritative evidence review (`REV-015` Part A): **RAG-arm certification LIFTED, in full.** Orchestrator adopted this as `ADR-018`, superseding `ADR-016`'s EXPERIMENTAL/UNCERTIFIED disposition — see the row above (`T1-F2-VER-011`'s superseding status note) and `T1-F2-VER-012`'s own updated cell for the full evidence trail. `BUG-016`/`BUG-017`/`BUG-019` all disposed "may CLOSE" per `REV-015` §A.5. **`VAL-010` stays open** in `error.md` — the lift does not close it; the secondary taxonomy audit and `retrieval_meta.queries` audit remain mandatory, standing conditions on every future RAG-mode batch, not one-time gates that expire on lift (`REV-015` §A.7). Source: `result.md` EXP-008, `discussion.md` REV-014/REV-015/ADR-018, `error.md` BUG-019, `development_report.md` DR-011 §1.

---

## Phase 2 — PLAN-2026-W28-K: F2 completion — Track B live disease-population SHIPPABLE (`ADR-020`/`REV-018`, path1), k-sweep (enhancement #1), evidence-source-type reporting (enhancement #4 code-side) — 2026-07-09

> Source: `discussion.md` PLAN-2026-W28-K, RES-001, REV-016, ADR-020, ADR-021, REV-017, REV-018; `result.md` EXP-009/EXP-010/EXP-011; `error.md` VAL-011 (closed, final)/VAL-010 (open); `development_report.md` DR-012. PRD: `PRD_task1_v2.md` §3.9 (v2.8). Wording bound by `REV-018` §2: the populated path is **shippable** (licensed, with 5 mandatory caveats), never **certified** — the RAG arm's own certification (`ADR-018`) already exists and is unaffected by Track B; population is a shippable feature built on that certified arm, not a second certification event. `VAL-010` stays open — nothing in this section closes it.

| ID | Type | Item | Status | Preconditions/notes |
|---|---|---|---|---|
| T1-F2-DEV-014 | DEV | Track B live-population implementation (path1, `ADR-020`): disease candidates derived code-side from already-retrieved `rag.case_card`/`rag.qa` chunks via the existing `_detect_symptoms`→`disease_symptom` ontology join (widened per-chunk); `mode` `experimental_unpopulated`→`rag_live`; top-5, MAX-not-sum aggregation, never padded; `source_id`+`quote` provenance added to `AIPredictedDiseaseCandidate`; `similarity_score` clamped to `[0,1]`; two-hop-proxy caveat disclosed verbatim in `reason_summary`; risk-lexicon filter on the extracted quote window (initial implementation) | [x] | `ADR-020` conditions 2/3/4. The initial risk-lexicon filter checked only the ±40-char extracted quote window, not the full source chunk — this specific gap is the subject of `T1-F2-VER-015`/`T1-F2-DEV-015` below (found live, then fixed). Source: `discussion.md` ADR-020, RES-001 §2, `development_report.md` DR-012 §2 |
| T1-F2-DEV-015 | DEV | REV-017 Finding 1 fix: risk-lexicon check widened to the full source-chunk text (`_contains_any(chunk_text, _RISK_PHRASES) or _contains_any(quote, _RISK_PHRASES)`, `f2.py:465`) — closes the quote-window blind spot `T1-F2-DEV-014`'s initial implementation shipped with | [x] | `ADR-020` condition 1 (blocking-scoped, `VAL-011` reopened by `REV-017`). qa re-gate independently reproduced the fix closing both live leak instances (synthetic fixture + real `EXP-009` artifact replay of `case_card:664`/`case_card:563`), mutation-checked load-bearing (reverting to quote-only reproduces the exact leak), confirmed non-over-blocking on a benign long-chunk fixture. Suite 812 passed. Source: `error.md` VAL-011 "QA re-gate (REV-017 Finding 1 fix)", `development_report.md` DR-012 §3 |
| T1-F2-DEV-016 | DEV | Enhancement #4 code-side evidence-source-type reporting — `_build_artifact`/`_build_report` surface `rag_chunk` (`case_card`/`qa`) vs `utterance` provenance from data already persisted in the artifact (`DomainEvidence.source_type`); zero prompt edit, no re-certification triggered | [x] | `REV-016`(a)/`ADR-021`. RAG-aware-prompt half of #4 **deferred** — `docs/ai/prompts/domain_inference/v2.system.md` not edited under this PLAN; a prompt change would require a fresh VP-003+VP-001 RAG n=2 re-validation against `ADR-018`'s standing audits before "certified" wording could attach to a new prompt version; no such re-validation was run this mission, no license granted. Source: `discussion.md` ADR-021, `development_report.md` DR-012 §6 |
| T1-F2-VER-015 | VER | `EXP-009` — first live populated-path validation batch (VP-001~004 n=2, RAG mode, `k=3`): mode honest 8/8 (`rag_live`), secondary taxonomy audit 0/102 (quote-text level), risk-lexicon filter actively engaged (8 candidate votes dropped, VP-003/VP-004), HPI isolation confirmed live at the artifact level — **but** found the REV-017 Finding 1 leak (see `T1-F2-VER-017` below) | [x] | `ADR-018` standing audits; `VAL-011` qa code gate (pre-existing, quote-window-only). Critic `REV-017` ruled the batch's disease-evidence-shipping deliverable **NOT SHIPPABLE (blocking)** — `case_card:664`/`case_card:563` shipped as disease-candidate evidence while the same run's own `domain_candidates` pipeline independently flagged the identical chunks `rejected_risk_lexicon`. Source: `result.md` EXP-009, `discussion.md` REV-017 §1, `error.md` VAL-011 (reopened by REV-017) |
| T1-F2-VER-016 | VER | `EXP-010` — k-sweep diagnostic (`k∈{2,3,5}`, VP-001~004 n=2/k, 24 runs): no `k` dominates top-1/top-3 (k=2/k=3 identical 6/8·7/8; k=5 marginally higher 7/8·8/8 on a single flipped run — sampling variance, not attributable to `k`); latency and accepted-evidence volume rise monotonically with `k` (42→70→87 evidence; mean latency 11.8s→12.9s→15.9s); `rejected_risk_lexicon` does not fall with `k` (36→29→41) | [x] | `REV-016`(d) confound guard (single-variable — only `--k` varies, prompt/population/config held constant). **Recommendation: keep the certified default `k=3`** — ratified by critic `REV-017` §4, which additionally notes a larger `k` retrieves more chunks and hence more surface area for the (then-open) Finding-1 blind spot, a further reason not to raise `k` while that finding was open. Source: `result.md` EXP-010, `discussion.md` REV-017 §4 |
| T1-F2-VER-017 | VER | `EXP-011` — clean re-verification of the REV-017 Finding 1 fix (VP-001~004 n=2, RAG mode, `k=3`, on `T1-F2-DEV-015`'s fixed tree): NEW chunk-level audit 0/33 shipped disease candidates cite a risk-lexicon-flagged source chunk; `case_card:664`/`case_card:563` (the two prior leak instances) both retrieved again this batch but neither cited/shipped; benign personas (VP-001/VP-002/VP-004) still ship legitimate, risk-clean candidate sets (no over-blocking) | [x] | `T1-F2-DEV-015`, `ADR-018` standing audits. Critic `REV-018` independently re-derived this batch's evidence and ruled the RAG-mode Track B populated path **SHIPPABLE for user-facing reporting**, subject to 5 mandatory caveats (two-hop-proxy, `VAL-010`-open, single-batch-evidence, provenance-enforcement-fragility follow-up, no-diagnosis-language/`is_diagnostic: False`) — full list under `T1-F2-DEV-011`'s status note below. `VAL-011` closes as resolved, final. `ADR-018`'s RAG-arm certification confirmed unaffected (no prompt change, `domain_candidates` shape unchanged). `VAL-010` stays open — this batch does not close it. Source: `result.md` EXP-011, `discussion.md` REV-018, `error.md` VAL-011 (closed, final) |

**Status update on `T1-F2-DEV-011` (unchanged ID, status/note revised — not a new entry):** the original entry's "게이팅: 컨테이너는 지금 구축됐다(`mode="experimental_unpopulated"`)... 라이브 실채움은 RAG-arm 인증에 게이트된다(`ADR-016`, EXPERIMENTAL 잔류)" framing is superseded — the RAG arm has since been certified in full (`ADR-018`) and this mission (`PLAN-2026-W28-K`) shipped live population on top of that certification. `mode` now transitions `experimental_unpopulated`→`rag_live` on a live run (`T1-F2-DEV-014`/`015` above). **Wording discipline (binding, `REV-018`):** the populated path is described as "shippable" (licensed), never "certified" — the RAG arm's certification (`ADR-018`) already exists and is unaffected by Track B; population is a shippable feature built on that certified arm, not a second certification event. Any user-facing statement about the populated path must carry all 5 of `REV-018` §2's mandatory caveats: (1) `similarity_score` is a chunk-to-Stage-1-query cosine proxy, NOT patient-to-disease similarity, NOT a calibrated probability (two-hop proxy); (2) `VAL-010` remains OPEN — VP-003's `chief_complaint`/HPI queries are risk-worded by persona design, reproducing for a 6th consecutive live batch; not implied resolved by the populated-path shippability ruling; (3) single-batch evidence (`EXP-011`, n=2/VP, `k=3`) — the standing secondary taxonomy audit AND the new chunk-level audit continue on every future populated-path batch, not a one-time gate; (4) provenance-enforcement fragility (qa `VAL-011` item 3, `REV-017` §5) — a shipped candidate cannot currently reach a missing-provenance state only because of an incidental upstream `KeyError` guarantee, not a designed invariant of the population function itself; adequate to ship today, non-blocking hardening follow-up remains open; (5) `is_diagnostic: Literal[False]` — a retrieval-derived candidate list, not a diagnosis; no diagnosis language on any user-facing surface. Source: `result.md` EXP-009/EXP-010/EXP-011, `discussion.md` REV-016/ADR-020/ADR-021/REV-017/REV-018, `error.md` VAL-011 (closed, final)/VAL-010 (open), `development_report.md` DR-012.

**Path2 DB-handoff item (not a checklist item — filed for the record):** `ADR-020` defers path2 (`rag.disease` embedding + ontology-graph similarity) as a DB-handoff action item, not reachable this mission: `ALTER TABLE rag.disease ADD COLUMN embedding vector(4096)` + a new backfill loader for the existing 26 rows + migration `0008_*`, gated on the external DB engineer (`development_report.md` DR-010 §6 item 2, restated DR-012 §8). Path1 (shipped, `T1-F2-DEV-014`) remains the implemented disease-source mechanism; path2 is not scheduled.

---

## Phase 2 — PLAN-2026-W28-N: PR #38 (F1 STT+OCR, Seohyunjho) integration — merged as additive input-modality plumbing, 3 residual defects disclosed — 2026-07-10

> Source: `discussion.md` PLAN-2026-W28-N, REV-021; `result.md` EXP-013; `error.md` BUG-020/BUG-021 (open)/VAL-012 (open); `development_report.md` DR-014. True merge `feat/f1-stt-ocr-integration`@`5bdd379f6eed9f340a9878edd02bbc0f073fd04a` (parents Master `411d6a1` + PR #38 head `15423baf`, Seohyunjho's 6 commits reachable). New IDs below never reuse `T1-F1-DEV-003/004/007/008` (retained above as historical scope records per REV-021 wording constraints; see their own pointer notes) — max ID confirmed by grep before filing: `T1-F1-DEV-029`, `T1-F1-VER-016`. **No "인증"/"통과"/"certified"/"shippable"/"passed" wording is used anywhere in this section** for the InputNormalizer correction feature or for the merge's overall safety posture (REV-021 binding).

| ID | Type | Item | Status | Preconditions/notes |
|---|---|---|---|---|
| T1-F1-DEV-030 | DEV | STT Agent + SKT A.X adapter + `POST /ai/stt/transcribe` route — as-built landing (supersedes the vendor-contract-pending scope of `T1-F1-DEV-003`/`007`) | [~] | Merged: `agents/stt.py`, `adapters/skt_ak_stt.py`, `routes/stt.py`, mounted in `main.py`. Batch mode only (streaming → HTTP 501). **No authentication** (`VAL-012`, major, open, non-blocking — unauthenticated multipart route proxying a paid vendor API, cost/DoS exposure; partial mitigation: 100MB size cap). Missing-vendor-credential path (`SKT_A_X_API_KEY` unset) surfaces as an unhandled, application-message-free 500 (`dependencies.py:90-108`, `VAL-012` §3 sub-issue — qa BUG recommended, not yet filed). **Live vendor path declared UNTESTED this mission** — `SKT_A_X_API_KEY` confirmed empty (`result.md` EXP-013 Task 4). `[~]` not `[x]`: code lands, live behavior unverified. Source: `error.md` VAL-012, `result.md` EXP-013 |
| T1-F1-DEV-031 | DEV | OCR Agent + Upstage Document Parse adapter + `POST /ai/ocr/parse` route — as-built landing (supersedes the vendor-contract-pending scope of `T1-F1-DEV-004`/`008`) | [~] | Merged: `agents/ocr.py`, `adapters/solar_document_parse.py`, `routes/ocr.py`, mounted in `main.py`. **No authentication** (`VAL-012`, same posture as STT/`chat.py`'s existing convention; 50MB size cap). `UPSTAGE_API_KEY` already registered for this project's Solar Pro3 usage, so (unlike STT) the dependency-resolution fail-fast path is not triggered in this repo's current environment — untested by absence of failure, not by a passing live call. **No live vendor call was made against this route this mission** (`continuous_test.py` smoke ran STT/OCR-off). `[~]` not `[x]`: code lands, live behavior unverified. Source: `error.md` VAL-012, `result.md` EXP-013 |
| T1-F1-VER-017 | VER | Post-merge safety-matrix regression check (SM-01..08b, TEXT modality, merged `f1.py`) vs `EXP-002`/`EXP-003` baselines — required because InputNormalizer now sits live upstream of safety classification for the first time (PLAN-2026-W28-N critic ruling (a)) | [x] | **Complete (`EXP-013`, `result.md`, 2026-07-10):** 11/11 scenarios executed on the merged tree; **0 verdict regressions among the 9 EXP-002 v2-comparable scenarios** (all 9 `all_passed=True` both runs). SM-08b fails as the first-ever v2 data point — explained by v2's own pre-existing BUG-007 calibration (v2 never implemented ADR-010 rule 5), not a regression from this merge; always reported paired with this explanation, never broadened beyond "the 9 EXP-002-comparable scenarios." A live `.env` `PROMPTS_BASE_DIR` misconfiguration (`BUG-021`) corrupted the first attempt (3 spurious FAILs); diagnosed, config-fixed (no `src/` edit), re-run — only the corrected run's numbers are cited above. `BUG-011`/`VAL-001` open, pre-existing, **not exercised** this batch (0 turn-0-crisis scenarios, 0 plan-disclosure clause-split hits) — not to be read as "fixed." Source: `result.md` EXP-013 Task 1, `discussion.md` REV-021 §1/§3.1 |
| T1-F1-VER-018 | VER | `continuous_test.py` F1→F2 chaining smoke with STT/OCR structurally off (by construction, no CLI flags exist) + STT live-vendor-path untested disclosure | [x] | **Complete (`EXP-013`, `result.md`, 2026-07-10):** VP-001 smoke PASS, `exit_code=0` (F1 `[PASS]`, F2 `[PASS]` mode=`rag`); 0 "prompt not found" warnings this run (`BUG-021` fix holds). STT live smoke **declared untested** — `SKT_A_X_API_KEY` confirmed empty, no call attempted, no provisioning performed. This item does not exercise `T1-F1-DEV-030`/`031`'s live vendor paths — it confirms only that the existing F1→F2 chain is unaffected when STT/OCR inputs are absent. Source: `result.md` EXP-013 Task 3/4 |

**Filed defects, disclosed unsoftened (REV-021 binding, not resolved by this section):** `BUG-020` (open, major — InputNormalizer correction feature confirmed 100%-conditional no-op, not merge-gating per critic ruling (d)); `BUG-021` (open, **critical** — `.env` `PROMPTS_BASE_DIR` silently bypasses the unset-only auto-correction guard across every prompt-driven agent; REV-021 independently confirmed this **predates PR #38 entirely**, present on Master's own `.env` today — not merge-gating, but a standing critical defect requiring prominent disclosure); `VAL-012` (open, major, non-blocking — `/ai/stt/transcribe`+`/ai/ocr/parse` unauthenticated, cost/DoS exposure, rate-limit/size-cap follow-up recommended). REV-021 overall verdict: **non-blocking** for opening the superseding PR (0 blocking, 1 major, 2 minor).

---

## Phase 2 — PLAN-2026-W28-V: F3 quick development — F2-driven questionnaire administration (design v1, pre-implementation) — 2026-07-12

> Source: `discussion.md` PLAN-2026-W28-V, ADR-031; `docs/ai/f3_quick_dev_plan.md`; PRD: `PRD_task1_v2.md` §4.1 (v2.10). User redefinition: F3 administers exactly the one questionnaire F2's `ai_predicted_disease.recommended_questionnaire` already named — a **second, separate** administration path from the pre-existing, unchanged `OrchestratorAgent.plan_surveys`/`score_and_check_safety` planner (`T1-F3-DEV-006` above, now annotated superseded-by-redefinition, not deleted). All 13 items below were design-only at filing time — **no code existed yet**, all statuses `[ ]`; implementation was a separate dispatch (`PLAN-2026-W28-V` step 5, gated on critic's pre-implementation review of the plan doc + this doc fold, step 4). No "구현 완료"/"검증됨"/"통과" wording was used for any item in this section at filing time.
>
> **갱신(2026-07-12, PLAN-2026-W28-V step 10 doc fold):** critic pre-implementation review complete (`REV-036`, non-blocking-with-conditions — 4 major/3 minor, dispositioned `ADR-032`); implementation landed + qa **GATE:PASS** (suite 1342+2, mutation-checked, `docs/ai/workflow_checklist_f1f2.md` F3 qa-gate-record row); live functional validation complete (`EXP-019`, `result.md`); evidence/clinical review complete (`CVR-015` adequate-with-findings ∥ `REV-037` evidence-sound-with-corrections, never merged). Statuses below updated per that evidence — DEV items `[x]` (qa-gate-verified); VER items marked per what `EXP-019`/`REV-037`/`CVR-015` actually evidenced, not per design intent — items with AUDIT-C- or GAD-7-live-dependent scope stay `[~]`/open with a note (AUDIT-C and GAD-7 were never administered live this battery). "구현 완료"/"통과" wording below is licensed only where the cited gate record actually says so — no 인증/certified wording anywhere.
>
> **갱신(2026-07-13, item bank v1, `PLAN-2026-W29-A`):** the item-bank v1 mission (`T1-F3-DEV-014`..`016`/`T1-F3-VER-014`..`017` below) answers this section's own open user question on v1 sourcing. PHQ-9/GAD-7/PHQ-4/AUDIT-C are now sourced-verbatim and appendix-audited (`CVR-016` adequate-with-conditions, 5 binding conditions all closed per `CVR-017`§4); WHO-5 stays 0/5 unpopulated. qa **GATE:PASS** — recorded after a critical process incident (`BUG-038`, qa's own `git checkout --` destroyed uncommitted `src/f3.py`) and its byte-faithful recovery. `EXP-020` live-re-validated Cell A/B/C; `CVR-017` adequate-with-conditions ∥ `REV-039` evidence-sound-with-corrections, never merged. **Binding disclosure carried into every row below that touches v1 content:** sourcing-accuracy improvement does NOT extend to simulator answer-LLM fidelity — item-8's elicitation artifact PERSISTS (not resolved), and a NEW whole-instrument answer-LLM over-endorsement pattern is OPEN and WORSE than v0 (`CVR-017` binding condition 2 / `REV-039` final wording table).

| ID | Type | Item | Status | Preconditions/notes |
|---|---|---|---|---|
| T1-F3-DEV-008 | DEV | Item bank v0 module (`src/scoring/item_bank.py`): `ScaleItem`/`ItemBankEntry` dataclasses, `ITEM_BANK` registry, `get_item_bank()` loader, `validate_item_bank_completeness()` fail-fast check. PHQ-9/AUDIT-C populated from persona-file construct labels (`provenance="construct-labels-v0, persona-file-sourced, non-validated"`); GAD-7/PHQ-4/WHO-5 unpopulated (`provenance="unpopulated-v0"`); `response_anchors=None` for every item in v0 (no anchor wording exists repo-wide) | [x] | Design: `docs/ai/f3_quick_dev_plan.md` §2.2. **갱신(2026-07-12):** landed + qa `GATE:PASS` — PHQ-9/AUDIT-C `text_ko` diffed byte-for-byte against VP-001/003/012 persona tables (exact match); GAD-7/PHQ-4/WHO-5 unpopulated, administration structurally impossible (test-proven). Item bank v1 (validated instrument text) remains blocked on a user decision (§2.3 of the plan doc), still out of scope. |
| T1-F3-DEV-009 | DEV | `src/f3.py` production administration engine: `administer_survey(scale_name, answer_fn, item_bank, patient_sex=...)` — resolves the item-bank entry, iterates items calling the injected `answer_fn` per item (zero LLM calls in `src/f3.py` itself; `answer_fn` mirrors `f1.py`'s `patient_input_fn` seam), reuses `src.scoring.survey_scorer.score_survey()` unmodified. `save_f3_result()` writes `<vp_id>_<ts>_survey.json` + `_survey.md` (mirrors `f2.py`'s `save_f2_result` pair); new schema module `src/schemas/survey_result.py` (`extra="forbid"`, `is_diagnostic: Literal[False]` fixed at the type level) | [x] | Design: `docs/ai/f3_quick_dev_plan.md` §1/§4. Depends on `T1-F3-DEV-008`. **갱신(2026-07-12):** landed + qa `GATE:PASS`; `EXP-019` live-exercised 3/4 administered/skip cells, `exit_code=0` throughout, 0 unhandled raises. |
| T1-F3-DEV-010 | DEV | `scale_scores` projection for the F2 seam: `save_f3_result()` derives `<vp_id>_<ts>_scale_scores.json` (bare list, `[{"scale_name","total_score","severity"}]`) — the exact shape `f2.py::_load_scale_scores` already parses — written only when `outcome=="administered"` | [x] | Design: `docs/ai/f3_quick_dev_plan.md` §5.1. Written by `src/f3.py` itself, not the harness (product-design-precedes-validation-design invariant). Depends on `T1-F3-DEV-009`. **갱신(2026-07-12):** landed + qa `GATE:PASS`; `EXP-019` VP-001 2-session chain live-confirmed the projection round-trips into session 2's `f2.py` call (code-path-traced, `REV-037` independently confirmed, not a runtime-assertion claim). |
| T1-F3-DEV-011 | DEV | Simulator score-selection mode (harness territory, `tests/simulation/`, never `src/`): `SurveyAnswerLLM` (new, K-EXAONE via friendli.ai, in-persona per-item score selection, one-shot bare-integer-range prompt, strict-parse + one re-prompt + clamp-with-WARNING-log on repeated failure) and `expected_answer_fn` (deterministic, zero LLM, reads the persona markdown's own "### {SCALE} 예상 항목별 점수" table, raises loudly — does not guess — if absent for the requested scale) | [x] | Design: `docs/ai/f3_quick_dev_plan.md` §7. `--answer-mode {llm,expected}` CLI selector, default `llm`. Depends on `T1-F3-DEV-008`/`009`. **갱신(2026-07-12):** landed + qa `GATE:PASS`; `EXP-019` exercised `llm` mode only — 6 parse retries (0 unresolved), 0 clamps across 27 responses; `expected`-mode path not live-exercised this battery. |
| T1-F3-DEV-012 | DEV | `continuous_test.py` F3 stage: `STAGE_REGISTRY`'s F3 stub replaced with `Stage("F3", True, run_f3_stage)`, positioned after F2/before F4's stub. `run_f3_stage(ctx)` reads `ctx.domain_inference_path`, resolves the `administered`/`no_questionnaire_indicated`/`item_bank_unpopulated` outcomes (never force-picks a scale). `ChainContext` gains `f3_survey_path`/`f3_scale_scores_path`/`answer_mode` fields; `run_multi_session_chain` appends the `"f3"` ledger sub-object (schema per plan doc §5) to the existing per-VP session ledger entry | [x] | Design: `docs/ai/f3_quick_dev_plan.md` §3/§6 items 1–3. Ledger writing stays harness-only (REV-022 standing rule) — `src/f3.py` never touches the ledger file. Depends on `T1-F3-DEV-009`/`010`/`011`. **갱신(2026-07-12):** landed + qa `GATE:PASS`; `EXP-019` all 3 outcomes' branch logic exercised live (`administered` ×3, `no_questionnaire_indicated` ×1; `item_bank_unpopulated` code-verified only, 0/4 live). |
| T1-F3-DEV-013 | DEV | `run_chain` (single-session, `--sessions 1` default path) ledger gap fix: add the missing `_append_ledger_entry` call after chain completion (`session_index=1`, `is_revisit=False`, mirroring `run_multi_session_chain`'s entry shape) — today `run_chain` writes no ledger entry at all | [x] | Design: `docs/ai/f3_quick_dev_plan.md` §6 item 5 ("single-session ledger gap, flagged not fixed"). Needed because the validation design (§8) requires ledger records for VP-003/VP-012, both single-session cells. Depends on `T1-F3-DEV-012`. **갱신(2026-07-12):** landed + qa `GATE:PASS` (`TestSingleSessionLedgerGapFix`); `EXP-019` VP-012 produced its first-ever session-ledger entry live — direct, incidental proof the gap fix works end-to-end. |
| T1-F3-VER-007 | VER | Scoring correctness vs hand-computed: artifact `score_result.total_score == sum(responses)` (code-computed, never LLM-computed); `expected`-mode cells hand-verified against the persona's documented per-item table; `llm`-mode cells verified equal to `sum(responses)` from the artifact itself only (LLM-mode responses are not expected to match the persona table exactly — only in-range and internally consistent) | [x] | Pass-criterion source: `docs/ai/f3_quick_dev_plan.md` §8 "Code-computed totals correct". **갱신(2026-07-12):** `EXP-019`/`REV-037` — critic hand-summed all 27 individual PHQ-9 responses across the 3 administered cells independently, exact match to artifact `total_score` in all 3 (13, 15, 13). `expected`-mode not exercised this battery (VER-007's own `expected`-mode clause stays untested). |
| T1-F3-VER-008 | VER | Severity band correctness: PHQ-9 (5/10/15/20) and AUDIT-C (threshold 4 male/unknown, 3 female) bands recomputed independently by the validator from the artifact's raw `responses` (never trusted from `score_result` alone), matching `survey_scorer.py`'s own published bands | [~] | Pass-criterion source: `docs/ai/f3_quick_dev_plan.md` §8 "Severity band correct". **갱신(2026-07-12):** PHQ-9 half `[x]` — `REV-037` independently recomputed `_PHQ9_SEVERITY` bands from hand-summed totals, exact match to artifact `severity` in all 3 cases (moderate, moderately_severe, moderate). AUDIT-C half stays **open** — AUDIT-C was never administered live this battery (VP-012 resolved to PHQ-9 instead, a live `VAL-014` instance, `CVR-015` Finding 3); AUDIT-C severity-band correctness remains code-verified-populated but live-untested. |
| T1-F3-VER-009 | VER | Response range validation: every response ∈ `[item.response_min, item.response_max]` for the administered scale; `score_survey()` raises nothing (a raise is a fail, not a warn) | [x] | Pass-criterion source: `docs/ai/f3_quick_dev_plan.md` §8 "Score range validity". **갱신(2026-07-12):** `EXP-019`/`REV-037` — all 27 individual PHQ-9 responses hand-checked ∈ `[0,3]`, 27/27 in range; all 3 `status.json` `exit_code=0`, no raise. |
| T1-F3-VER-010 | VER | Ledger record completeness: `"f3"` sub-object present with all schema fields (plan doc §5) non-null for `outcome="administered"`; present with `outcome` + null score fields for the two skip outcomes (`no_questionnaire_indicated`/`item_bank_unpopulated`) — never absent from a session's ledger entry | [x] | Pass-criterion source: `docs/ai/f3_quick_dev_plan.md` §5/§8 "Ledger record complete". **갱신(2026-07-12):** `EXP-019`/`REV-037` — all 4 relevant ledger entries read directly, `"f3"` present in all 4, fields non-null for the 3 `administered` entries, null-but-present for the 1 `no_questionnaire_indicated` entry. `item_bank_unpopulated` outcome not live-exercised (0/4). **Disclosed caveat (`REV-037`):** `(session_index, simulated_date)` is not a globally unique ledger key even within one persona's file (a pre-existing harness property, not an F3 defect) — disambiguation requires `written_at`/`conversation_path`, already the documented workaround. |
| T1-F3-VER-011 | VER | Simulator persona consistency: `llm`-mode responses fall within ±1 of the persona's documented expected per-item score for VP-001/VP-003/VP-012's PHQ-9 items and VP-012's AUDIT-C items (the only VPs/scales with documented anchors in v0); a wider gap is flagged, not auto-failed (LLM sampling variance) | [~] | Pass-criterion source: `docs/ai/f3_quick_dev_plan.md` §8 "Simulator answers persona-consistent". **갱신(2026-07-12):** mechanism executed correctly per design intent — a wider gap was flagged, not auto-failed. But `CVR-015` Finding 1 (clinical-validator's independent interpretation, not `EXP-019`'s own charter): PHQ-9 item 8 (정신운동 지연/초조) landed documented-0→LLM-selected+2 in **3/3** administered instances, a systematic elicitation-format artifact (bare construct label + bare integer ask, no anchors) — `REV-037` correctly left this uninterpreted at n=2 independent personas (VP-001's two sessions share one documented anchor table). VP-012's AUDIT-C items were never live-exercised (AUDIT-C not administered this battery) — that clause of this item stays untested. Re-test required once v1 anchors land (`CVR-015` recommendation 2), not assumed fixed. |
| T1-F3-VER-012 | VER | Clinical-note/HPI isolation: `grep` the F3 artifact and the same-session F2/F1 artifacts confirms no F3 field value appears inside `slots.history_of_present_illness`, `slots.chief_complaint`, or any other narrative `SlotData` string field (`src/schemas/handoff.py:11-32`) | [x] | Pass-criterion source: `docs/ai/f3_quick_dev_plan.md` §8 "Clinical-note/HPI isolation holds" (binding HPI red line, `PLAN-2026-W28-V`). **갱신(2026-07-12):** `EXP-019`/`REV-037` — critic independently re-ran the grep with the plan's own exact-numbered-label instrument against all 4 sessions' F1/F2 artifacts, 0/4 hits, reproducing the entry's claim exactly; reverse direction (F3→F1/F2) also checked, 0/4 hits, structurally absent from the `survey.json` schema as populated. `REV-036` condition 4's adversarial unit-test suite (`tests/test_f3_hpi_isolation.py`, 9 tests + a 4th ledger-subobject class) independently confirmed to have genuinely landed, not merely claimed. |
| T1-F3-VER-013 | VER | No-harness-deps-in-production: `src/f3.py` imports nothing from `tests/` (zero LLM calls in the production path — `answer_fn` is the sole variability point); `git diff` for the mission shows zero changes to `src/agents/orchestrator.py`, `src/routes/chat.py`, `docs/ai/prompts/safety/**`, `docs/ai/prompts/dialogue/**` | [x] | Pass-criterion source: `docs/ai/f3_quick_dev_plan.md` §1/§8 "No safety-wiring touch"; qa code gate (`PLAN-2026-W28-V` step 6). **갱신(2026-07-12):** qa's `GATE:PASS` record (`docs/ai/workflow_checklist_f1f2.md` F3 section) directly confirms `src/f3.py` — 0 `tests/` imports, 0 `session_ledger` references, 0 LLM/vendor call sites; pins (safety v2, dialogue v4) recomputed exact-match at `EXP-019` launch; `git diff` empty for `orchestrator.py`/`chat.py`/`f1.py`/`f2.py`/`docs/ai/prompts/**`. `REV-037` corroborated this via triangulation (pre-run checklist + pin match + qa's own record) rather than an independent `git diff` re-run of its own. |
| T1-F3-DEV-014 | DEV | Item bank v1 module: PHQ-9(Pfizer 한국어판 primary + 정부 별지14호 alternate 보존)/GAD-7/PHQ-4(GAD-7 first-2 + PHQ-9 first-2, Kim et al. 2021 method)/AUDIT-C 4개 척도 — sourced-verbatim item text + response anchors + timeframe/instruction 문구 populated (`docs/ai/item_bank_v1_sources.md` 출처, 23개 항목 전건 appendix 인용 대조 byte-exact); WHO-5는 저장소 전역 원문 부재로 0/5 unpopulated 유지(exhaustive 재시도 로그, 허구생성 0) | [x] | `CVR-016` adequate-with-conditions (0 blocking, 5 binding conditions, 전건 `CVR-017`§4 종결); `ADR-033` decisions 1/7. Source: `discussion.md` CVR-016, ADR-033, CVR-017 §4; `error.md` BUG-038 qa re-gate (item-bank byte-diff re-confirmed post-incident) |
| T1-F3-DEV-015 | DEV | `SurveyResultOutput.threshold_caveat` 구조적 필드: AUDIT-C(Seong 2009/Woo 2017 한국인구 근거 미조정 caveat) + GAD-7(Korean-language band-sourcing citation retracted this mission, Spitzer 2006 international-convention-only caveat) 양 척도 populated, PHQ-9는 null(무해당) — `continuous_test.py`의 원장 재투영까지 관통 | [x] | `CVR-016` binding condition 3(AUDIT-C) + `CVR-017` binding condition 1(GAD-7 parity, implemented proactively) + `REV-039` correction D(validity-side rationale). Source: `error.md` BUG-038 qa re-gate "Named GAD-7-caveat test set" (12/12 passed); `discussion.md` CVR-017 Finding 3 |
| T1-F3-DEV-016 | DEV | Forced-questionnaire 하네스 경로: `continuous_test.py --force-questionnaire <SCALE>`, `administration_mode`("natural"/"forced") 구조적 필드가 `survey_result.py` 스키마 + 원장 `"f3"` sub-object 양쪽에 실장 — natural-chain/F2-linkage 증거와 별개의 evidence class로 loudly labeled(프로덕션 `f3.py`/`f2.py` 동작 자체는 무변경); ledger append-only 충돌-안전성 확인 | [x] | `ADR-033` decision 6; `REV-038` §2 (Resolution 1/6/8 — labeling field, ledger-collision safety, N/A selection-correctness marking). Source: `error.md` BUG-038 qa re-gate "Named forced-mode test set" (14/14 passed); `result.md` EXP-020 Cell B/C (natural+forced VP-001 records survive as distinct ledger entries, live-confirmed) |
| T1-F3-VER-014 | VER | `EXP-020` 라이브 재검증: Cell A(VP-001 natural PHQ-9 ×2 세션)/Cell B(VP-001 forced GAD-7)/Cell C(VP-012 forced AUDIT-C) — 3 cells, exit 0/0/0; 28/28 응답 in-range, 4/4 총점·중증도 밴드 독립 재계산 일치, HPI-isolation grep forward 0/4 + reverse 0/4, F2 Pydantic 4/4 PASS | [x] | Pass-criterion source: `docs/ai/f3_quick_dev_plan.md` §8 (v0에서 확립된 동일 기준을 v1 콘텐츠에 재적용). **갱신(2026-07-13):** `REV-039`가 모든 수치를 독립 재도출, 예외 없이 정확 일치(총점/밴드/range/retry·clamp/HTTP call count/VAL-014 근접치/isolation grep). Source: `result.md` EXP-020 (+ 2026-07-13 correction addendum); `discussion.md` REV-039 §(1) |
| T1-F3-VER-015 | VER | Item-8(정신운동 지연/초조) v1 재검사 — 사전등록 결정규칙(`REV-038`§1) 적용: Cell A 양 세션 v8=2(문서치 0) → "artifact persists in this instance(s)"(n=2, VP-001-only, bundled-change qualifier). **전체-도구(whole-instrument) 과다-인정 패턴 신규 발견**(`CVR-017` Finding 2, `REV-039` §(3) 확인): PHQ-9/GAD-7 총점 모두 v0보다 문서치에서 더 멀어짐(PHQ-9 9개 항목 중 5개 더 이탈/4개 동률/0개 근접) | [x] | Pass-criterion: `REV-038`§1 결정규칙(사전등록, no post-hoc discretion). **"item-8 resolved"/"item-8 fixed" 문구 금지 — binding (`CVR-017`/`REV-039`).** `CVR-017` binding condition 2: 이 발견을 인용하는 모든 향후 보고서는 whole-instrument 과다-인정 패턴을 open/worsened로 동반 공시해야 함. Source: `result.md` EXP-020 "Item-8 decision rule"/"v1-vs-v0" 절; `discussion.md` CVR-017 Q1/Q2, REV-039 §(4) 최종 wording table |
| T1-F3-VER-016 | VER | PHQ-9 문항9 결정론적 safety-pathway 소비자-seam 배선 — `_route_phq9_safety_pathway` 코드 확인됨(stub 아님), Cell A 양 세션에서 `safety_pathway_invoked=true`/`safety_triggered=false` 기록 | [~] | `PLAN-2026-W29-A` step 3; `REV-038`§4 row 6; `REV-039`§(4) row 8. **라이브 양성(triggered=true) 사례는 3개 연속 배터리(`EXP-018`/`019`/`020`)에서 0건 — "the safety pathway was validated/exercised" 문구 금지, "invoked, not triggered; live-unverified"만 licensed.** Source: `result.md` EXP-020 Cell A "Item-9 safety pathway" 절; `discussion.md` REV-039 per-criterion table §(1) item-9 row |
| T1-F3-VER-017 | VER | qa gate — item bank v1 implementation (`PLAN-2026-W29-A` step 4a) + `BUG-038` incident 대응: 원본 게이트(CI-mirror 1415 passed/2 skipped, mutation-check GAD-7-caveat 로드베어링 확인) 수행 직후 qa 자신의 `git checkout --` 오류로 미커밋 `src/f3.py` 전체 소실(`BUG-038`, critical process incident — 검토 대상 코드의 결함 아님); developer가 byte-faithful 복원(캡처된 fragment 2개 diff 0, 나머지는 무결한 테스트 스위트로 재구성) 후 qa 독립 재게이트 **GATE:PASS**(1415 passed/2 skipped 정확 재현, other-file drift 0, bands byte-frozen, named test set 12+14+6=32/32 전건 통과, `EXP-020` frozen artifact 필드별 cross-check) | [x] | 이 항목이 `REV-039` §(6) item C("qa GATE:PASS verdict가 아직 formal record로 착지하지 않음")를 종결한다 — formal record는 이 checklist 행 + `docs/ai/workflow_checklist_f1f2.md` F3 section의 "Item bank v1" qa gate record 행. GAD-7 `threshold_caveat` 구조적 필드(`REV-039` correction D)는 `T1-F3-DEV-015`에서 이미 구현·qa검증 완료. Source: `error.md` BUG-038 (전체 사고 기록 + 재게이트); `discussion.md` REV-039 §(6) item C Resolution |

| T1-F3-DEV-017 | DEV | AUDIT-C v2 implementation (`PLAN-2026-W29-B` Track 1): `_score_audit_c` Korean-primary cutoffs male/unknown≥6·female≥5 (Lee JH et al. 2018 KNHANES) drive `severity`/`recommended_action`; international cutoff (Bush et al. 1998, 4/3) ships as non-action-driving structured metadata (`audit_c_international_threshold`, never triggers `clinician_review` independently); item text moved to sourced-verbatim 별지 제15호의3서식 소주-트랙 (item 1 mirror-1 5-anchor set incl. "전혀 안 마신다(0점)", provenance discloses the mirror-2 discrepancy; items 2/3 byte-identical both mirrors); composed administration-note rejected as superseded; `AUDIT_C_THRESHOLD_CAVEAT` rewritten per `CVR-018` Q4's 5 content elements; VP-012 ground truth re-derived (`VP-012_first_visit_alcohol.md` §9, `[4,1,3]`/total 8/range 7-9, supersedes the old `[4,3,4]`/11 table) | [x] | `CVR-018` (adequate-with-conditions, 8 binding conditions, all closed per `CVR-019` Q1) → `ADR-034` decisions 1-3/5. Source: `discussion.md` CVR-018, ADR-034; commit `99c2f45` |
| T1-F3-DEV-018 | DEV | `ISS-F2V-028` factorial harness (`PLAN-2026-W29-B` Track 3): `tests.simulation.factorial_driver` — CLI driver over the pre-existing `item_bank` override seam on `src.f3.administer_survey`/`resolve_outcome`; `instruction_ko_override` sentinel-typed param on `SurveyAnswerLLM.__init__`; Cell-3/Cell-7-shaped golden leak test (`test_cell3_shaped_instruction_override_prevents_leak`) proving no live-registry instruction leaks into an anchor-on/instruction-off cell | [x] | `REV-040` non-blocking-with-conditions (Resolution 1's golden test is blocking-scoped on qa's gate — satisfied below). Source: `discussion.md` REV-040; `_archive/plans/exp021_factorial_design.md`; commit `2351e07` |
| T1-F3-DEV-019 | DEV | Fix wave (`PLAN-2026-W29-B`, post-evidence): `BUG-039` (item 2's secondary "기타 술" track — populated in the item bank but never rendered into any consumer's prompt — `build_item_prompt`/`SurveyAnswerLLM._build_prompt` gained 4 new optional kwargs, both tracks + conversion note now render); `BUG-040` (`threshold_caveat` OpenAPI `Field(description=...)` rewritten from the stale international-only framing to the adopted Korean-primary 6/5 framing); `REV-041` Resolution 1 (`expected`-mode fixture made supersession-aware — asserts against `VP-012` §9's re-derived total, not the superseded §3 table); `REV-041` Resolution 2 (`patient_sex` wired into `continuous_test.py`'s F3 stage — female threshold branch test-proven; live female-persona administration remains open, `CVR-019` binding condition 3) | [x] | `error.md` BUG-039 (resolved)/BUG-040 (resolved). Source: `error.md` BUG-039, BUG-040; `discussion.md` REV-041 Resolution 1/2; commit `2351bdc` |
| T1-F3-VER-018 | VER | qa gate — `ISS-F2V-028` factorial harness (`2351e07`) | [x] | **GATE:PASS.** CI-mirror suite **1483 passed / 2 skipped**. Blocking-scoped condition satisfied: `test_cell3_shaped_instruction_override_prevents_leak` (`REV-040` Resolution 1) present, passing, mutation-verified load-bearing. Production path (`f3.py`/`item_bank.py`/`survey_scorer.py`/`continuous_test.py`/`f2.py`/`f1.py`/`agents`/`schemas`/`routes`) confirmed untouched by this driver commit. Source: `result.md` EXP-021 Setup/pre-run checklist; `docs/ai/workflow_checklist_f1f2.md` "Trustworthy-direction F3 decisions" section |
| T1-F3-VER-019 | VER | qa gate — Track-1 AUDIT-C v2 implementation (`99c2f45`) | [x] | **GATE:PASS.** Byte-fidelity **8/8** vs. `docs/ai/audit_c_korean_research.md` §1+§3.2 (both mirrors); thresholds boundary-verified. CI-mirror suite **1505 passed / 2 skipped**. Found and filed `BUG-039` (major) and `BUG-040` (minor) this gate — both resolved in `T1-F3-VER-020` below. Source: `result.md` EXP-022 Setup/Depends-on; `error.md` BUG-039, BUG-040; `docs/ai/workflow_checklist_f1f2.md` "Trustworthy-direction F3 decisions" section |
| T1-F3-VER-020 | VER | qa re-gate — fix wave (`2351bdc`) | [x] | **GATE:PASS.** CI-mirror suite **1529 passed / 2 skipped**. `BUG-039` independently re-verified resolved (live-built item-2 v2 prompt renders both tracks + conversion note + unified instruction line, byte-for-byte match vs. `audit_c_korean_research.md:298`; non-regression on secondary-less items; 3 EXP-021 factorial golden tests unchanged). `BUG-040` independently re-verified resolved (`threshold_caveat` description now states the adopted 6/5 framing; repo-wide stale-4/3-as-primary sweep 0 hits). Regression sweep matches the claimed 13-file set exactly; `survey_scorer.py` AUDIT-C thresholds **zero** diff. Source: `error.md` BUG-039 "Independent qa re-verification", BUG-040 "Independent qa re-verification" |
| T1-F3-VER-021 | VER | `EXP-021` — `ISS-F2V-028` factorial decomposition (VP-001 PHQ-9, item-text v0/v1 × anchor × instruction, bypass-mode): 10/10 pre-registered administrations exit 0. Verdict **"no dominant factor/inconclusive"** (Tier-1-licensed) — anchor-menu main effect largest (Effect(F_anchor)=2.6875) but fails the pre-registered margin (margin_m=3, actual 1.5); 3-way interaction is the single largest contrast overall (4.75), also fails margin (2.0625). Baseline cell (v0/off/off, pooled n=4) already cell-mean D=7.25 (~2x documented total 7) — forecloses format-reversion as a fix | [x] | Pass-criterion source: `REV-040` §4.3 decision rule (amended, Tier-1 minimum). **No dominant-factor or interaction-driven claim beyond "inconclusive" is licensed — `REV-040`/`REV-042` wording tables bind all citations of this row.** Source: `result.md` EXP-021; `discussion.md` REV-040, REV-042 §1 |
| T1-F3-VER-022 | VER | `EXP-022` — VP-012 forced AUDIT-C v2 re-verification (fresh F1→F2→F3(forced) chain, `--answer-mode llm`): 1/1 administration exit 0. Item vector `[4,4,4]`=12/12 (scale ceiling, first in F3 validation history) vs. §9-derived expected `[4,1,3]`/total 8/range 7-9. Item 2 out-of-band (Δ+3) while items 1/3 in-band — licensed framing only: "the item-2-specific deviation pattern recurs even under native soju-track administration; the working unit-confound hypothesis is not supported by this instance" (never "fixed"/"resolved"). Korean-primary 12≥6 → `hazardous_drinking`/`clinician_review` (robust across the entire disclosed range floor); international metadata 12≥4 → `crossed_international_threshold=true`; `threshold_caveat` present, all 5 `CVR-018` Q4 elements verbatim. Disclosures: `BUG-039` gap (this run's rendered prompt showed only the primary soju track — the correct track for this soju-native persona, ruled orthogonal to the ceiling response by both `CVR-019` and `REV-042`), `patient_sex` non-wiring at run time (numerically inert, VP-012 is documented male), ground-truth supersession (§9 cited throughout, never §3) | [x] | Pass-criterion source: `REV-041` §(1)-(2) decision rules. **No AUDIT-C v2 severity/band output is clinically load-bearing evidence of instrument behavioral fidelity until `CVR-019` binding conditions 1 and 3 land and at least one additional confirmatory administration runs (`CVR-019` binding condition 5).** Source: `result.md` EXP-022; `discussion.md` REV-041, CVR-019, REV-042 §2 |

**Blocking note (carried from the plan doc, not resolved by this filing):** instrument item TEXT (PHQ-9/GAD-7/PHQ-4/WHO-5/AUDIT-C stems + response anchors) is absent repo-wide (`docs/ai/f3_quick_dev_plan.md` §2.1) — v0 ships with only PHQ-9/AUDIT-C construct labels populated (`T1-F3-DEV-008`); GAD-7/PHQ-4/WHO-5 stay `item_bank_unpopulated` until a v1 item bank is authored, gated on a user decision (plan doc §2.3). This does not block `T1-F3-DEV-008`~`013` (v0-scoped) but does bound `T1-F3-VER-007`~`013`'s achievable coverage to PHQ-9/AUDIT-C cells.

---

## Phase 2 — 통합 (Gate G-E)

| ID | Type | 항목 | 상태 | 선행 조건 |
|---|---|---|---|---|
| T1-F0-VER-003 | VER | 통합 E2E: F1→F3→F4→F5 전 체인, 4VP, EvidenceVerifier passed, 날조 0건 | [ ] | G-D 전체 |
| T1-F0-VER-004 | VER | QA/Critic 통합 감사 이슈(ISS-024~040) 전건 소거 확인 | [ ] | 전 Stage |

---

## 요약 통계

| 구분 | Phase 1 | Phase 2 신규 | 합계 |
|---|---|---|---|
| DEV | 35 | 54 | 89 |
| VER | 28 | 47 | 75 |
| CFG | 3 | 1 | 4 |
| DOC | 4 | 3 | 7 |
| SEC | 0 | 4 | 4 |
| **계** | **70** | **109** | **179** |

Phase 1 상태 분포 (Stage 0 완료 후, 2026-07-08 T1-F0-CFG-001 전환 반영): `[x]` 33 · `[~]` 11 · `[!]` 10 · `[ ]` 16
**Gate 순서: ~~G-0(긴급 복구)~~ 완료(2026-07-06) → G-F(grounding 재검증) ← 다음 → G-A/B/C → G-D → G-D-F2 → G-E. G-F 전에는 어떤 신규 "pass" 주장도 금지. G-D-F2는 추가로 ADR-014에 따라 v2 remediation 통과 전까지 진행 불가(아래 2026-07-08 갱신 참조).**

> **2026-07-13 addition (v2.12, 171→179 items):** `PLAN-2026-W29-B` — trustworthy-direction resolution of the three open F3 decisions (Korean AUDIT-C localization, WHO-5 retry-2, `ISS-F2V-028` factorial decomposition) — DEV 3 items (`T1-F3-DEV-017`~`019`) + VER 5 items (`T1-F3-VER-018`~`022`) new; 8 new IDs continuing the `T1-F3-*` sequence (prior F3 maxima: `T1-F3-DEV-016`, `T1-F3-VER-017`), non-colliding by construction (grep-confirmed). Recorded per actual evidence: `T1-F3-DEV-017` (AUDIT-C v2 cutoffs/item-text/metadata, `99c2f45`) `[x]`; `T1-F3-DEV-018` (factorial harness, `2351e07`) `[x]`; `T1-F3-DEV-019` (fix wave — `BUG-039`/`BUG-040` resolved, `REV-041` Resolutions 1-2 landed, `2351bdc`) `[x]`; `T1-F3-VER-018`~`020` (the three separate qa gates, formal records — closes `REV-042` Issue 1's recurring qa-gate-formal-record traceability gap, previously flagged for `EXP-020` by `REV-039` and recurring twice more before this filing) `[x]`; `T1-F3-VER-021` (`EXP-021` factorial decomposition — "no dominant factor/inconclusive," Tier-1-licensed) `[x]`; `T1-F3-VER-022` (`EXP-022` VP-012 forced AUDIT-C v2 re-verification — first-ever scale-ceiling response, `[4,4,4]`=12/12, item-2 unit-confound hypothesis "not supported by this instance") `[x]`. **Binding disclosure carried into every row above and into any future citation (`CVR-017` binding condition 2 / `CVR-019` binding condition 2):** the whole-instrument answer-LLM over-endorsement pattern is open, unmitigated, and now confirmed on a third, structurally different instrument (AUDIT-C, substance-use) beyond PHQ-9/GAD-7 — no row in this addition may be read as implying the pattern is resolved, explained, or fixed, and neither `CVR-019` nor `REV-042` licenses any mechanism-targeted or format-reversion fix from this evidence. Source: `discussion.md` PLAN-2026-W29-B, CVR-018, ADR-034, REV-040, REV-041, CVR-019, REV-042; `result.md` EXP-021, EXP-022; `error.md` BUG-039 (resolved), BUG-040 (resolved).

> **2026-07-13 addition (v2.11, 164→171 items):** `PLAN-2026-W29-A` — item bank v1 (research-based reproduction of official Korean screening instruments) — DEV 3 items (`T1-F3-DEV-014`~`016`) + VER 4 items (`T1-F3-VER-014`~`017`) new; 7 new IDs continuing the `T1-F3-*` sequence (prior F3 maxima: `T1-F3-DEV-013`, `T1-F3-VER-013`), non-colliding by construction. Recorded per actual evidence, not retroactively marked complete where live behavioral fidelity remains open: `T1-F3-DEV-014`~`016` `[x]` (item bank v1 content + `threshold_caveat` fields + forced-questionnaire harness path, all qa-gate-verified, incl. the `BUG-038` incident recovery); `T1-F3-VER-014` `[x]` (`EXP-020` mechanical checks, all matched exactly); `T1-F3-VER-015` `[x]` (item-8 pre-registered decision rule correctly applied — outcome is "artifact persists," never "resolved"; the new whole-instrument over-endorsement pattern is flagged, open); `T1-F3-VER-016` `[~]` (safety-pathway wiring code-verified; live-positive case still 0/3 consecutive batteries); `T1-F3-VER-017` `[x]` (qa gate formal record, incl. `BUG-038` and its byte-faithful recovery — closes `REV-039` §(6) item C's formal-record gap). `T1-F3-VER-005`'s note is revised in place (not a new entry) to cross-reference the new path's item-9 invoked-not-triggered status. **Behavioral-fidelity wording discipline (binding, `CVR-017`/`REV-039`):** item-bank v1's sourcing-accuracy improvement (`CVR-016` conditions all closed) does NOT extend to simulator answer-LLM fidelity — the whole-instrument over-endorsement finding is open and worsened-not-resolved; no row in this addition may be read as implying the opposite. Source: `discussion.md` PLAN-2026-W29-A, CVR-016, ADR-033, REV-038, CVR-017, REV-039; `result.md` EXP-020; `error.md` BUG-038.

> **2026-07-12 addition (v2.10, 151→164 items):** `PLAN-2026-W28-V` — F3 quick development (F2-driven questionnaire administration), design v1 pre-implementation checklist items — DEV 6 items (`T1-F3-DEV-008`~`013`) + VER 7 items (`T1-F3-VER-007`~`013`) new; 13 new IDs confirmed non-colliding with existing IDs by grep repo-wide (prior F3 maxima: `T1-F3-DEV-007`, `T1-F3-VER-006`). All 13 recorded `[ ]` (TODO — design-only mission, no code exists yet; implementation is `PLAN-2026-W28-V` step 5, gated on critic's pre-implementation review of the plan doc + this doc fold). `T1-F3-DEV-006` (the old F1-artifact-driven survey-planner-concept scope, `[ ]`) is annotated superseded-by-redefinition in place — not deleted, not renumbered, not silently repurposed; see its own row note. No "구현 완료"/"검증됨"/"통과" wording used anywhere in this addition. Source: `discussion.md` PLAN-2026-W28-V, ADR-031; `docs/ai/f3_quick_dev_plan.md`.

> **2026-07-10 addition (v2.9, 147→151 items):** `PLAN-2026-W28-N` — PR #38 (F1 STT+OCR, Seohyunjho) integration merged (`feat/f1-stt-ocr-integration`@`5bdd379`) — DEV 2 items (`T1-F1-DEV-030`/`031`, STT/OCR agent+adapter+route as-built landing) + VER 2 items (`T1-F1-VER-017`/`018`, post-merge safety-matrix regression check + `continuous_test.py` smoke/STT-untested disclosure) new; 4 new IDs confirmed non-colliding by grep (prior maxima: `T1-F1-DEV-029`, `T1-F1-VER-016`). `T1-F1-DEV-003/004/007/008` (the pre-merge vendor-contract-pending scope) are retained unchanged as historical records with pointer notes to the new IDs — not reused, not deleted. `T1-F1-DEV-002` (InputNormalizer) is a status-note revision in place (not a new entry), applying critic ruling (d)'s wording precision: dead-code banner retired, `BUG-020`'s live 100%-conditional-no-op finding disclosed unsoftened. All 4 new items recorded `[~]`/`[x]` per their own evidence, not retroactively marked complete where live-vendor verification is outstanding (`T1-F1-DEV-030`/`031` stay `[~]` — code lands, STT/OCR live paths remain unverified this mission). No "인증"/"통과"/"certified"/"shippable"/"passed" wording used anywhere in this addition, per `REV-021`. Source: `discussion.md` PLAN-2026-W28-N/REV-021, `result.md` EXP-013, `error.md` BUG-020/BUG-021/VAL-012, `development_report.md` DR-014.

> **2026-07-09 addition (v2.8, 141→147 items):** PLAN-2026-W28-K — F2 completion: Track B live disease-population SHIPPABLE (`ADR-020`/`REV-018`, path1), k-sweep (enhancement #1), evidence-source-type reporting (enhancement #4 code-side, `ADR-021`) — DEV 3 items (T1-F2-DEV-014/015/016) + VER 3 items (T1-F2-VER-015/016/017) new; 6 new IDs confirmed non-colliding with existing IDs by grep (prior maxima: T1-F2-DEV-013, T1-F2-VER-014). All 6 recorded `[x]` (retroactive filing after code-verification/live-batch completion, same convention as prior missions). `T1-F2-DEV-011`'s status note is revised in place (not a new entry) to reflect the RAG arm's certification (`ADR-018`, superseding the `ADR-016` EXPERIMENTAL text it previously cited) and the populated path's shippable-with-5-caveats disposition. The leak-and-fix arc (`EXP-009`/`EXP-010` shipped a live risk-lexicon leak → critic `REV-017` NOT SHIPPABLE, blocking, `VAL-011` reopened → fix → `EXP-011` clean re-verification → critic `REV-018` SHIPPABLE) is recorded across `T1-F2-DEV-014/015` and `T1-F2-VER-015/016/017`, not smoothed into a single "done" line. `VAL-010` stays open in `error.md` — this addition does not close it. Path2 (`rag.disease` embedding + migration `0008_*`) remains a filed DB-handoff item, not a checklist item, gated on the external DB engineer. Source: `docs/ai/development_report.md` DR-012, `discussion.md` PLAN-2026-W28-K/RES-001/REV-016/ADR-020/ADR-021/REV-017/REV-018, `result.md` EXP-009/EXP-010/EXP-011, `error.md` VAL-011 (closed, final)/VAL-010 (open).
>
> **2026-07-09 addition (v2.7, 133→141 items):** PLAN-2026-W28-H Track A/B/C/D + BUG-019 diagnosis/fix — SEC 1 item (T1-F2-SEC-004) + DEV 4 items (T1-F2-DEV-011/012/013, T1-F0-DEV-011) + VER 2 items (T1-F2-VER-013/014) + DOC 1 item (T1-F0-DOC-007) = 8 new IDs total, confirmed non-colliding with existing IDs by grep (prior maxima: T1-F2-SEC-003, T1-F2-DEV-010, T1-F0-DEV-010, T1-F2-VER-012, T1-F0-DOC-006). All 8 recorded `[x]` (completed and qa-verified at filing time — instrumentation/fix/container/harness/handoff-doc all code-verified or doc-complete; the one item whose full scope is NOT yet complete, the live RAG-arm re-verification, is tracked under the pre-existing `T1-F2-VER-012`, whose status is updated `[ ]`→`[~]` in the section above rather than given a new ID). Source: `docs/ai/development_report.md` DR-010, `discussion.md` PLAN-2026-W28-H/ADR-016/ADR-017/REV-012/REV-013, `result.md` EXP-007, `error.md` BUG-019/BUG-017/VAL-005.
>
> **2026-07-09 addition (v2.6, 127→133 items):** F2 RAG-arm remediation & certification (`PLAN-2026-W28-G`) — DEV 3 items (T1-F2-DEV-008~010) + VER 3 items (T1-F2-VER-010~012) new. The 6 new IDs were confirmed non-colliding with existing IDs by grep (prior F2 maximum: DEV-007, VER-009). T1-F2-DEV-008/009/010 and T1-F2-VER-010 are recorded `[x]` (completed and qa/critic-verified at filing time, same retroactive-filing convention as T1-F0-DOC-006). T1-F2-VER-011 is recorded `[~]` (executed and measured in full — the certification question it was designed to answer resolved to "not lifted" rather than "lifted"; this is a measurement-complete, decision-negative outcome, not a partial/blocked execution). T1-F2-VER-012 is recorded `[ ]` (not started — the successor-mission precondition for any future RAG-arm certification attempt). Source: `docs/ai/development_report.md` DR-009, `discussion.md` PLAN-2026-W28-G/REV-011/REV-012/ADR-016, `result.md` EXP-006, `error.md` BUG-016(resolved)/BUG-017(open)/BUG-019(open, new)/VAL-010(open).

> **2026-07-09 추가 (v2.5, 126→127 items):** Agent spec 문서 총검증·정비(`PLAN-2026-W28-F`) — DOC 1건(T1-F0-DOC-006) 신규. 신규 ID는 grep으로 기존 ID와 충돌 없음을 확인했다(기존 DOC 최대 ID: T1-F0-DOC-005). T1-F0-DOC-006은 신설과 동시에 `[x]`로 기재한다(Stage 1~3 완료·qa GATE: PASS 확인 후 소급 신설이기 때문 — 다른 항목들처럼 `[ ]`로 먼저 신설한 뒤 나중에 전환한 사례와 다름, 근거: `development_report.md` DR-008). 이 미션은 `docs/ai/agents/` 스펙 14종(00~13) 3-way 감사 + 신규 `14_domain_inference.md` 저작 + 기존 11개 스펙 as-built 정정을 포함하나, 이들은 개별 체크리스트 ID로 분해하지 않고 T1-F0-DOC-006 1건으로 묶어 기재한다(스펙 파일 자체는 어떤 기존 T1-* 항목의 "산출물"로도 명시적으로 지정되어 있지 않았기 때문). 근거: `discussion.md` PLAN-2026-W28-F, `development_report.md` DR-008, `error.md` BUG-018.

> **2026-07-08 갱신 (DR-007 반영, `PLAN-2026-W28-E` 완료):** T1-F2-DEV-006/007 `[ ]`→`[x]`(코드 강제 위험-어휘 필터+cascade, prompt v2 — 3회 게이트 반복 후 qa GATE:PASS, 스위트 687). T1-F2-VER-008 `[ ]`→`[x]`(`EXP-005` llm_only arm, REV-008 위반 재발 0/42 — critic REV-010, `ADR-015` (1) llm_only 비인증 해제 조건부[상시 수동감사]). T1-F2-VER-009 `[ ]`→`[~]`(`EXP-005` RAG arm 실행·측정 완료 — mode=rag 8/8, fabrication 0/35, top-1 5/8·top-3 6/8 — 그러나 인증 판정은 아니다: RAG arm은 `ADR-015` (2)에 따라 EXPERIMENTAL 잔류, 해제 경로는 BUG-016/BUG-017 수정 + VP-003 RAG 재검증 + VAL-010 상시화 3조건). 4항목 모두 상태 변경만이며 항목 신설·삭제 없음 — 요약 통계 표의 항목 합계(126)는 불변이다. 근거: `docs/ai/development_report.md` DR-007, `discussion.md` PLAN-2026-W28-E/REV-009/REV-010/ADR-015, `result.md` EXP-005, `error.md` VAL-006(resolved)/VAL-009(open, 별도 흡수 기록 없음)/VAL-010(open-narrowed)/BUG-014·BUG-015(resolved)/BUG-016·BUG-017(open).

> **2026-07-08 추가 (v2.4, 122→126 items):** F2 v2 remediation(`PLAN-2026-W28-E`) — DEV 2건(T1-F2-DEV-006/007) + VER 2건(T1-F2-VER-008/009) 신규. 신규 ID 4종은 grep으로 기존 ID와 충돌 없음을 확인했다(기존 F2 최대 ID: DEV-005, VER-007). 동일 갱신에서 T1-F0-CFG-001을 `[ ]`→`[x]`로 전환(`PLAN-2026-W28-D`/PR #36 반영, 근거는 해당 행 참조) — Phase 1 상태 분포 `[x]` 32→33, `[ ]` 17→16으로 갱신. T1-F2-VER-006의 "RAG arm BLOCKED-awaiting-DB" 주석에도 DB 연결 라이브 검증 완료 갱신을 추가했다(상태 `[~]` 자체는 불변 — RAG arm 미실행). PR #35(redis 제거)/#37(ai-server lint 21건)에 직접 대응하는 기존 체크리스트 항목은 확인되지 않아 신규 항목을 만들지 않았다. 근거: `discussion.md` PLAN-2026-W28-E, ADR-014, REV-008, VAL-006, PLAN-2026-W28-D; `error.md` BUG-013. 위 표 수치는 이 추가분을 반영한다.

> **2026-07-08 갱신 (DR-006 반영, F2 구현 미션 완료):** T1-F2-DEV-001/002/004/005·SEC-001~003·VER-005 `[x]`로 전환(구현 완료·qa GATE:PASS 확인, DR-006 §1/§4). T1-F2-DEV-003(신규 프로덕션 라우트)도 `[x]`로 전환 — **정정:** 이전 버전의 "본 미션 범위에 포함되지 않아 `[ ]` 유지" 판정은 증거 부족에 따른 오류였다. 직접 확인 결과 `apps/ai-server/src/routes/domain.py`가 존재하고 `apps/ai-server/src/main.py:24`가 이를 import, `main.py:51`이 `app.include_router(domain_router)`로 등록해 서버 부팅 시 실제 마운트된다(qa GATE:PASS, DR-006 §1). 라우트 신규 구현·등록 자체는 완료이며, orchestrator.py 프로덕션 연동만 항목 텍스트가 명시한 대로 G-D-F2 게이트로 계속 보류된다(범위 밖, 미착수와 다름). T1-F2-CFG-002 `[~]`(프리플라이트 메커니즘은 구현·검증됐으나 `DATABASE_URL`/`ENCRYPTION_KEY` env 배선 자체는 미완 — RAG arm 차단의 직접 원인). T1-F2-VER-006 `[~]`(llm_only arm만 라이브 완료, RAG arm은 BLOCKED-awaiting-DB). T1-F2-VER-007 `[x]`이나 **중요 단서 포함**: fabrication=0 하드 게이트(본 항목 고유 범위)는 충족했으나, 별도의 risk≠domain 절대 규칙(prompt rule 2)이 VP-003 2/2 런에서 라이브 위반이 확인됐다(VAL-006, REV-008) — 롤백 트리거가 발동해 `domain_inference` v1이 **비인증(non-certification)** 처분됐다(ADR-014). **G-D-F2 게이트 진행 및 RAG-arm 활성화는 v2 remediation(코드 강제 risk-lexicon evidence filter + 프롬프트 정교화 + 재검토)이 전체 게이트 체인을 통과할 때까지 불가**(ADR-014 (1)). 본 갱신 어디에도 risk≠domain 규칙에 대해 "통과"/"유지" 표현을 사용하지 않는다. 근거: `docs/ai/development_report.md` DR-006, `discussion.md` REV-007/REV-008/ADR-013/ADR-014, `error.md` VAL-006/VAL-007/VAL-008/BUG-012.

> **2026-07-07 추가 (v2.1, 114→115 items):** ISS-049 정책 최종 반영(ADR-010) — safety_classifier v3 프롬프트 DEV 1건(T1-F1-DEV-029) 신규. 근거: `discussion.md` PLAN-2026-W28-B/ADR-010. 위 표 수치는 이 추가분을 반영한다.

> **2026-07-07 정정 (v2.2, DR-005):** T1-F1-DEV-029 셀이 라이브 검증 실패로 `[~]` → `[!]`로 갱신되었다(ADR-012, `result.md` EXP-003) — 안전 프롬프트 v3의 SM-04a/SM-04b 프로브 회귀로 롤백. 셀은 developer가 갱신했으나 위 상태 분포 문구는 그 시점에 갱신되지 않았던 것을 여기서 정정한다: `[~]` 12→11, `[!]` 9→10 (총계 70·115는 변동 없음 — 상태만 변경, 항목 추가/삭제 없음). 본 미션(PLAN-2026-W28-B)이 건드린 체크리스트 행은 T1-F1-DEV-029 1건뿐임을 확인했으며, 이 미션에서 유래한 다른 카운트 drift는 없다. `[x]`/`[ ]` 값은 이번 정정 대상이 아니며(본 미션과 무관한 이전 시점 값), 별도 재집계가 필요하면 이는 본 항목 범위 밖이다.

> **2026-07-08 추가 (v2.3, 115→122 items):** F2를 DomainInferenceAgent로 재정의(PLAN-2026-W28-C 승인, ADR-013) — 기존 T1-F2-DEV-001~005 및 VER-001~004는 문구만 신 정의 기준으로 갱신(카운트 불변, 상태는 착수 전 `[ ]` 유지). 신규 항목: CFG 1건(T1-F2-CFG-002, DB env 배선/프리플라이트), SEC 3건(T1-F2-SEC-001~003, VAL-005/ADR-013 보안 조치 — 본 문서 최초의 SEC 타입), VER 3건(T1-F2-VER-005~007, 골든라벨 DATASET/라이브 n≥2 배치/grounding 감사). §9.2 게이트 테이블에 G-D-F2 신설(REV-006 조건 4) 반영해 위 Gate 순서 문구에도 추가. 근거: `discussion.md` PLAN-2026-W28-C, REV-006, ADR-013; `PRD_task1_v2.md` §3(v2.2), §9.2. 신규 ID 7종(T1-F2-CFG-002/SEC-001/SEC-002/SEC-003/VER-005/VER-006/VER-007 — CFG 1 + SEC 3 + VER 3)은 grep으로 기존 ID와 충돌 없음을 확인했다.
>
> **부수 정정 (본 항목과 별개, 신규 추가분과 무관):** 표 갱신 과정에서 전 항목을 실제로 행 단위 재계수(grep)한 결과, 기존 "Phase 2 신규" 열의 DEV/VER 분배가 부정확했음을 발견했다 — 기존 표는 DEV 26 / VER 18로 표기했으나 실제 물리적 행 수는 DEV 28 / VER 16이다(두 오차가 서로 상쇄되어 기존 대분류 합계 115는 우연히 정확했다). 본 갱신에서 DEV/VER Phase 2 분배를 실측치(28/16, 본 미션 VER 신규 3건 반영 후 19)로 정정했다 — Phase 1 열과 전체 합계(122)는 실측과 일치하며 이번 정정으로 변하지 않는다. 이 오차의 발생 시점은 특정하지 않는다(본 미션 범위 밖).

> **2026-07-07 추가 (v2, 107→114 items):** 프롬프트 아키텍처 v3(Gate G-F 확장) — DEV 5건(T1-F1-DEV-024~028) + VER 2건(T1-F1-VER-015~016) 신규. 근거: `PRD_task1_v2.md` §11, `docs/ai/prompt_redesign_v3.md` v3.1, `discussion.md` PLAN-2026-W28(B1)/REV-002/ADR-006/ADR-007. 위 표 수치는 이 추가분을 반영한다.
